---
phase: 06-product-event-inventory-and-typed-contract
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - crates/pi-coding-agent/src/coding_session/public_event.rs
  - crates/pi-coding-agent/tests/product_event_contract.rs
  - crates/pi-coding-agent/tests/event_boundary_guards.rs
  - docs/product-event-contract.md
autonomous: true
requirements: [EVENT-01, EVENT-02, EVENT-03]
must_haves:
  truths:
    - "D-01: The checked inventory names exactly the 11 current families and 45 variants derived from internal emitters, including metadata-only events."
    - "D-02/D-04: Contract tests identify events through public typed variants and the boundary guard rejects Debug-string identity or a public legacy-event payload."
    - "D-03: Documentation classifies every public operation/outcome as root-terminal-associated, synchronous/eventless, or currently unassociated while preserving the five current root-operation mappings and valid absence semantics."
    - "D-05: Focused receiver tests prove monotonic sequence, one-to-one projection, and unchanged pending/committed/skipped durability behavior."
    - "D-06: All inventory, serialization, projection, and source-audit checks run offline with existing dependencies."
  artifacts:
    - crates/pi-coding-agent/tests/product_event_contract.rs
    - crates/pi-coding-agent/tests/event_boundary_guards.rs
    - docs/product-event-contract.md
  key_links:
    - "Internal 45-variant exhaustive conversion <-> checked public inventory prevents silent emitter drift."
    - "Public typed fixture <-> Serde assertions lock Rust matching and wire names to the same contract."
    - "Public operation/outcome inventory <-> documented association matrix and drift guard classify every variant while preserving the current five root mappings."
---

<objective>
Freeze the full emitted event inventory and protect the typed public contract against payload, serialization, identity, ordering, and terminal-association drift.

Purpose: Complete EVENT-03 and provide the verification evidence needed before Phase 7 migrates first-party consumers.
Output: Exhaustive fixtures, public integration tests, a source boundary guard, and current-contract documentation.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-product-event-inventory-and-typed-contract/06-CONTEXT.md
@.planning/phases/06-product-event-inventory-and-typed-contract/06-RESEARCH.md
@.planning/phases/06-product-event-inventory-and-typed-contract/06-01-SUMMARY.md
@crates/pi-coding-agent/src/coding_session/event.rs
@crates/pi-coding-agent/src/coding_session/event_service.rs
@crates/pi-coding-agent/src/coding_session/public_event.rs
@crates/pi-coding-agent/tests/event_boundary_guards.rs

<interfaces>
Consume the public types and accessors created by Plan 06-01 through `pi_coding_agent::api`; do not import `coding_session::event` or construct ProductEvent externally.

Inventory baseline from Phase 6 research:
- 11 families: Session, Profile, Agent, Team, Message, Tool, Runtime, Delegation, Workflow, Diagnostic, Capability.
- 45 variants total: 5 session, 1 profile, 6 agent, 6 team, 4 message, 4 tool, 1 runtime, 7 delegation, 9 workflow, 1 diagnostic, 1 capability.
- Root terminal associations currently exist only for Prompt, AgentInvocation, AgentTeam, SelfHealingEdit, and Compact.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze all emitted variants and typed payload conversions</name>
  <read_first>crates/pi-coding-agent/src/coding_session/public_event.rs; crates/pi-coding-agent/src/coding_session/event.rs; .planning/phases/06-product-event-inventory-and-typed-contract/06-RESEARCH.md</read_first>
  <files>crates/pi-coding-agent/src/coding_session/public_event.rs</files>
  <behavior>
    - A test fixture constructs and converts all 45 internal variants, and every result matches its expected public family/kind and selected payload fields.
    - The family distribution is exactly 5/1/6/6/4/4/1/7/9/1/1 in source order and totals 45.
    - Events without operation identity, terminal status, or durable storage retain `None`/live-only semantics instead of fabricated values.
    - Every new internal CodingAgentEvent variant forces an explicit conversion decision at compile time.
  </behavior>
  <action>Expand the co-located test module with a deterministic fixture covering each of the 45 `CodingAgentEvent` variants and assert its public family/kind plus representative payload/correlation fields. Make the conversion match remain exhaustive with no wildcard (D-01), and add a checked family distribution/total so review exposes inventory drift. Cover all valid metadata combinations: no-operation SessionOpened/ProfileChanged/CapabilityChanged, optional-operation Diagnostic, all operation-bearing variants, all terminal statuses, all durability variants, and OperationRecovered. Test that conversion clones/owns payload data and never exposes the compatibility enum (D-04). Preserve the existing terminal-operation boundary rather than expanding delegation/tool/message/session-write associations (D-03; Phase 9 deferred).</action>
  <verify><automated>cargo test -p pi-coding-agent --lib public_event --quiet</automated></verify>
  <acceptance_criteria>All 45 variants are individually converted and asserted; inventory totals/family counts are explicit; conversion cannot silently ignore a new internal variant.</acceptance_criteria>
  <done>The implementation and test inventory provide exhaustive compile-time and behavioral evidence for every current emitter variant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify the downstream typed and serialized contract</name>
  <read_first>crates/pi-coding-agent/tests/support/mod.rs; crates/pi-coding-agent/tests/agent_invocation.rs; crates/pi-coding-agent/tests/delegation_execution.rs; crates/pi-coding-agent/src/coding_session/public_event.rs</read_first>
  <files>crates/pi-coding-agent/tests/product_event_contract.rs</files>
  <behavior>
    - A deterministic offline public session subscription observes monotonically increasing typed events without loss or duplication.
    - Representative operation-bearing, operation-less, terminal, progress, pending-write, committed-write, and skipped-write events serialize to stable snake_case names.
    - The public receiver preserves operation IDs and does not conflate event terminal status, root terminal association, and durability.
  </behavior>
  <action>Create a dedicated integration target importing only `pi_coding_agent::api` and reusing existing deterministic offline session/provider guards. Drive representative real operations through `CodingAgentSession::run`, subscribe through `subscribe_product_events_public`, and assert typed patterns rather than string comparisons (D-02). Check strictly monotonic sequence and one output projection per received event without introducing a second counter or changing replay retention (D-05). Assert deterministic JSON for representative variants from every family that the available offline flows emit, including explicit null/absence semantics and independent terminal/durability fields (D-03). Keep adapter output assertions in their existing Phase 7-owned suites and do not unwrap the internal compatibility event.</action>
  <verify><automated>cargo test -p pi-coding-agent --test product_event_contract --quiet</automated></verify>
  <acceptance_criteria>External-style callers match typed variants and serialize stable metadata; live subscription order and durability behavior remain unchanged under deterministic fixtures.</acceptance_criteria>
  <done>The stable event contract is proven at the public crate boundary without internal imports or compatibility-event access.</done>
</task>

<task type="auto">
  <name>Task 3: Document both inventories and enforce the replacement boundary</name>
  <read_first>.planning/phases/06-product-event-inventory-and-typed-contract/06-RESEARCH.md; crates/pi-coding-agent/tests/event_boundary_guards.rs; crates/pi-coding-agent/src/coding_session/public_event.rs; crates/pi-coding-agent/src/coding_session/public_projection.rs; crates/pi-coding-agent/src/coding_session/public_operation.rs</read_first>
  <files>docs/product-event-contract.md; crates/pi-coding-agent/tests/event_boundary_guards.rs</files>
  <action>Write the current stable contract document from the verified emitter inventory: list all 11 families and 45 variants with payload/correlation fields, specify sequence ownership, valid operation-ID absence, terminal-status semantics, pending/durable/live-only semantics, and Serde shape. Add a second exhaustive matrix covering every variant of `CodingAgentOperation` and `CodingAgentOperationOutcome`; pair each request with its outcome and classify the current runtime behavior as root-terminal-associated, synchronous/eventless, or currently unassociated. Name the exact associated terminal event variants for Prompt, Compact, SelfHealingEdit, InvokeAgent, and InvokeTeam, and explicitly record why BranchSummary, plugin operations, profile/delegation mutations, session navigation, and exports do not currently have a distinct root-terminal association. This matrix is evidence of current behavior, not permission to create missing associations: preserve the five existing mappings and leave association closure to Phase 9 (EVENT-03). Explicitly document that tool/message/delegation/session-write family completion is not root-operation completion. State that session-log order is distinct from live ProductEvent sequence and that `PartialCommit` remains an operation error/outcome with its operation ID rather than a new event state (D-03/D-05). Extend `event_boundary_guards.rs` with a deterministic drift check that extracts the public operation and outcome variant names from `public_operation.rs` and requires each to appear exactly once in the documented matrix, so adding or renaming either enum cannot silently stale EVENT-03 evidence. Also fail if public event modules derive family/kind identity through Debug formatting, expose `CodingAgentEvent` as a public payload field/variant, use a wildcard in the compatibility-to-public conversion, or omit a documented event family. Region-scope scans to production conversion/type declarations so test fixtures and the intentionally private transition match do not create false positives. Do not migrate RPC/interactive consumers or implement any currently missing terminal association in this phase.</action>
  <verify><automated>cargo test -p pi-coding-agent --test event_boundary_guards --quiet &amp;&amp; cargo test -p pi-coding-agent --test product_event_contract --quiet</automated></verify>
  <acceptance_criteria>The document accounts for every event family/variant and every public operation/outcome variant; each operation is classified into exactly one current terminal-association category; the guard fails on operation/outcome inventory drift, Debug identity, public legacy payload leakage, wildcard conversion, or missing event families with actionable diagnostics.</acceptance_criteria>
  <done>EVENT-03 has exhaustive event and operation/outcome evidence without expanding the five current root-terminal mappings, and Phase 7 has an authoritative typed target plus migration boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Emitter inventory -> contract evidence | New or changed internal variants must not bypass public conversion, tests, or documentation. |
| Runtime event stream -> public subscriber | Order, identity, and durability claims are externally observable and must remain trustworthy. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-05 | Tampering | inventory/conversion drift | high | mitigate | Exhaustive no-wildcard match, 45-variant fixture, family counts, and documented inventory guard. |
| T-06-06 | Spoofing | serialized event identity | high | mitigate | Public integration tests bind typed variants to explicit snake_case wire names. |
| T-06-07 | Repudiation | terminal/durability claims | high | mitigate | Association matrix and fixtures distinguish event-terminal, root-terminal, pending, durable, and live-only facts. |
| T-06-08 | Information Disclosure | compatibility payload | high | mitigate | Boundary test rejects a publicly stored or returned CodingAgentEvent payload. |
| T-06-SC | Tampering | dependency supply chain | high | mitigate | No package installation; all checks use Cargo, Serde, and deterministic repository fixtures. |
</threat_model>

<source_coverage>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 6 | Freeze inventory and implement stable typed public event model | 06-01, 06-02 | COVERED | Contract first, exhaustive evidence second.
REQ | EVENT-01 | Stable typed event inspection | 06-01, 06-02 | COVERED | Typed hierarchy, facade tests, and no-Debug guard.
REQ | EVENT-02 | Sequence, operation, terminal, durability, payload semantics | 06-01, 06-02 | COVERED | Public metadata plus receiver/Serde tests.
REQ | EVENT-03 | Complete emitter inventory and terminal/outcome documentation | 06-02 | COVERED | 45-event fixture plus exhaustive public operation/outcome classification and drift guard.
RESEARCH | Inventory | 11 families / 45 variants | 06-01, 06-02 | COVERED | Exhaustive mapper and checked counts.
RESEARCH | Terminal matrix | Event terminal differs from root-operation terminal | 06-01, 06-02 | COVERED | Five mappings preserved; every public operation/outcome classified without implementing Phase 9 closure.
RESEARCH | Runtime state | No stored-data, service-config, OS-state, secret, or package migration | 06-01, 06-02 | COVERED | Projection-only work; no external migration.
RESEARCH | Validation gaps | Public Serde, exhaustive conversion, API types, no-Debug source guard | 06-01, 06-02 | COVERED | Each Wave 0 gap has an automated task.
CONTEXT | D-01 | Derive taxonomy from current internal event model | 06-01, 06-02 | COVERED | Family-oriented exhaustive conversion.
CONTEXT | D-02 | Typed kind rather than string parsing | 06-01, 06-02 | COVERED | Typed public matching is authoritative.
CONTEXT | D-03 | Preserve metadata and absence semantics | 06-01, 06-02 | COVERED | Independent typed fields and tests.
CONTEXT | D-04 | Legacy event remains internal conversion source | 06-01, 06-02 | COVERED | Owned public payloads and leakage guard.
CONTEXT | D-05 | Preserve ordering, replay, PartialCommit, adapter behavior | 06-01, 06-02 | COVERED | Pure projection, receiver regression, no adapter migration.
CONTEXT | D-06 | Existing patterns, no dependency | 06-01, 06-02 | COVERED | Rust/Serde/current fixtures only.
</source_coverage>

<verification>
Run `cargo fmt --check`, `cargo test -p pi-coding-agent --lib public_event`, `cargo test -p pi-coding-agent --test public_api`, `cargo test -p pi-coding-agent --test product_event_contract`, `cargo test -p pi-coding-agent --test event_boundary_guards`, `cargo test -p pi-coding-agent`, `cargo check -p pi-coding-agent --all-targets`, and `git diff --check`.
</verification>

<success_criteria>
The checked contract accounts for all 45 current events, external callers match typed variants and stable serialization, terminal/outcome boundaries are explicit, and source guards fail closed on identity or exposure drift.
</success_criteria>

<output>
Create `.planning/phases/06-product-event-inventory-and-typed-contract/06-02-SUMMARY.md` when done.
</output>
