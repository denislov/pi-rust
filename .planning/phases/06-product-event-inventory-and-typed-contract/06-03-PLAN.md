---
phase: 06-product-event-inventory-and-typed-contract
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - crates/pi-coding-agent/src/coding_session/public_event.rs
  - crates/pi-coding-agent/tests/event_boundary_guards.rs
  - docs/product-event-contract.md
autonomous: true
gap_closure: true
requirements: [EVENT-03]
must_haves:
  truths:
    - "D-01/D-02: Every one of the 45 internal fixture events is paired in source order with an explicit expected public family and snake_case kind, so a wrong typed conversion target fails deterministically."
    - "D-03/D-04: Typed per-variant assertions verify representative correlation or payload fields without exposing CodingAgentEvent through the public contract or changing terminal associations."
    - "D-05/D-06: A source guard ties the internal CodingAgentEvent enum, fixture constructors, the checked public inventory, and all 45 documented variants together using deterministic offline checks."
  artifacts:
    - crates/pi-coding-agent/src/coding_session/public_event.rs
    - crates/pi-coding-agent/tests/event_boundary_guards.rs
    - docs/product-event-contract.md
  key_links:
    - "Internal CodingAgentEvent variant names <-> exhaustive fixture constructor names prevents a converted event from bypassing the fixture."
    - "Fixture expected (family, kind) inventory <-> projected results prevents a wrong public conversion arm from passing family-count checks."
    - "Checked 45-entry inventory <-> marked documentation rows prevents EVENT-03 evidence from silently becoming stale."
---

<objective>
Close the Phase 6 verification gap by making all 45 event mappings and documented variants fail closed against source, fixture, typed projection, and documentation drift per D-01 through D-06.

Purpose: Turn the current count/family-level inventory evidence into an exact per-variant contract before Phase 7 consumes it.
Output: A 45-entry expected public inventory, typed payload assertions, and a full event-variant documentation drift guard.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-product-event-inventory-and-typed-contract/06-CONTEXT.md
@.planning/phases/06-product-event-inventory-and-typed-contract/06-RESEARCH.md
@.planning/phases/06-product-event-inventory-and-typed-contract/06-VALIDATION.md
@.planning/phases/06-product-event-inventory-and-typed-contract/06-01-SUMMARY.md
@.planning/phases/06-product-event-inventory-and-typed-contract/06-02-SUMMARY.md
@.planning/phases/06-product-event-inventory-and-typed-contract/06-VERIFICATION.md
@crates/pi-coding-agent/src/coding_session/event.rs
@crates/pi-coding-agent/src/coding_session/public_event.rs
@crates/pi-coding-agent/tests/event_boundary_guards.rs
@docs/product-event-contract.md

<interfaces>
Existing gap seam in `public_event.rs`:
- `exhaustive_inventory_fixture() -> Vec<CodingAgentProductEvent>` constructs the 45 current internal variants in source-family order.
- `CodingAgentProductEvent::family()` and `kind_name()` expose explicit typed identity.
- `CodingAgentProductEvent::event()` exposes family-specific public payload enums for typed matching.

Existing boundary seam in `event_boundary_guards.rs`:
- `region(source, start, end)` scopes source/document scans.
- `enum_variants(source, enum_name)` extracts public enum inventories.
- The operation/outcome matrix already demonstrates marked-region parsing plus unique set equality.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind every fixture event to exact typed identity and payload</name>
  <read_first>crates/pi-coding-agent/src/coding_session/public_event.rs; crates/pi-coding-agent/src/coding_session/event.rs; .planning/phases/06-product-event-inventory-and-typed-contract/06-VERIFICATION.md</read_first>
  <files>crates/pi-coding-agent/src/coding_session/public_event.rs</files>
  <behavior>
    - RED: changing one expected kind, reordering one expected row, or mapping one fixture input to another same-family public variant makes the inventory test fail.
    - GREEN: exactly 45 projected events zip one-to-one with an explicit source-order `(CodingAgentProductEventFamily, snake_case kind)` inventory.
    - Each of the 45 projected items is matched through its family-specific public enum variant and checks at least one stable correlation or payload fact from that input.
    - Existing family distribution, sequence, absence, durability, event-terminal, and five root-terminal assertions remain intact.
  </behavior>
  <action>Start with a failing assertion that expects an explicit 45-entry inventory not yet present, commit the RED gate, then add a test-only `EXPECTED_PUBLIC_EVENT_INVENTORY` in a clearly marked source region. List every current event in the same order as `exhaustive_inventory_fixture`, with the public family enum and exact `kind_name()` snake_case value; zip projected values with this array and assert sequence position, family, and kind for every row per D-01/D-02. Add typed per-family assertion helpers or an exhaustive index/variant match that visits all 45 public variants and validates representative owned data: session/operation/turn/child/tool/message/recovery identifiers, profile/target kinds, task/result/error/reason text, usage, self-healing replacements/diagnostics/check output, compaction fields, and capability generation/revocation. The assertions must fail if a conversion arm maps to a sibling variant with the same family. Preserve the current public enums and conversion behavior, terminal mappings, transitional fields, and runtime code; this is test evidence only per D-03/D-04 and the verifier gap.</action>
  <verify><automated>cargo test -p pi-coding-agent --lib public_event::tests::exhaustive_inventory_covers_all_current_variants --quiet</automated></verify>
  <acceptance_criteria>The test contains 45 explicit expected family/kind rows, compares all projected events in source order, and typed-matches all 45 payload variants with representative field assertions; no production contract or event classification changes.</acceptance_criteria>
  <done>A wrong identity, same-family conversion target, reordered fixture, or corrupted stable payload fails the focused inventory test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tie internal, fixture, checked, and documented inventories together</name>
  <read_first>crates/pi-coding-agent/tests/event_boundary_guards.rs; crates/pi-coding-agent/src/coding_session/event.rs; crates/pi-coding-agent/src/coding_session/public_event.rs; docs/product-event-contract.md</read_first>
  <files>crates/pi-coding-agent/tests/event_boundary_guards.rs; docs/product-event-contract.md</files>
  <behavior>
    - RED: the guard fails before the document has a machine-checkable 45-row event inventory and reports the missing or extra variant.
    - GREEN: the internal CodingAgentEvent enum, fixture constructor region, 45-entry expected inventory, and documentation each contain the same 45 source events/public family-kind mappings exactly once.
    - Adding, renaming, removing, duplicating, or omitting an event in any one layer fails with an actionable inventory mismatch.
    - Existing no-Debug, privacy, no-wildcard, family, and operation/outcome matrix checks continue to pass.
  </behavior>
  <action>First extend `event_boundary_guards.rs` with a failing full-event drift assertion and commit the RED gate. Add stable source markers around the fixture constructors and expected inventory only where needed for deterministic extraction; keep scans region-scoped so the conversion and other tests do not create false positives. Parse the internal `CodingAgentEvent` enum variant names from `event.rs`, extract each `CodingAgentEvent::Variant` constructor exactly once from the marked fixture region, and assert unique set equality so a new converted event cannot bypass the fixture. Parse the 45 expected public family/kind tuples from the marked test inventory. Replace or augment the grouped documentation inventory with a marked 45-row table containing internal event variant, public family, and public snake_case kind; parse it and assert ordered or unique tuple equality with the checked test inventory. Also assert 45 rows and uniqueness in every layer. Retain the grouped explanatory table if useful, but make the marked row table authoritative. Do not alter RPC/interactive consumers, public receiver behavior, compatibility storage, replay/client APIs, or root-terminal associations; those remain Phase 7-9 scope per D-05/D-06.</action>
  <verify><automated>cargo test -p pi-coding-agent --test event_boundary_guards typed_public_event_boundary_is_fail_closed --quiet &amp;&amp; cargo test -p pi-coding-agent --lib public_event::tests::exhaustive_inventory_covers_all_current_variants --quiet</automated></verify>
  <acceptance_criteria>The guard proves exact 45-entry equality across the internal enum, fixture, expected public inventory, and marked documentation table; mutation of any one layer fails without relying on broad counts alone.</acceptance_criteria>
  <done>The verifier's inventory-freeze gap is closed by deterministic, fail-closed code-to-fixture-to-document wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Internal emitter inventory -> public contract evidence | A new or remapped event must not bypass fixture, typed mapping, or documentation review. |
| Test inventory -> stable documentation | Human-readable EVENT-03 evidence must remain synchronized with executable expectations. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-G01 | Tampering | 45-event typed mapping | high | mitigate | Explicit source-order family/kind expectations plus typed per-variant payload assertions. |
| T-06-G02 | Repudiation | inventory documentation | high | mitigate | Marked 45-row document table compared to the executable expected inventory. |
| T-06-G03 | Tampering | fixture coverage | high | mitigate | Internal enum and marked fixture constructor set equality with uniqueness and exact count. |
| T-06-G04 | Information Disclosure | public payload tests | medium | mitigate | Match existing public-owned payload enums only; do not widen or expose compatibility types. |
| T-06-GSC | Tampering | dependency supply chain | high | mitigate | Add no dependency; use current Rust tests and source parsing helpers only. |
</threat_model>

<source_coverage>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GAP | 06-VERIFICATION | Explicit 45-entry family/kind inventory | 06-03 Task 1 | COVERED | Source-order zip assertion.
GAP | 06-VERIFICATION | Representative correlation/payload assertions or typed per-variant matches | 06-03 Task 1 | COVERED | All 45 public variants visited.
GAP | 06-VERIFICATION | Documentation guard compares all 45 variants | 06-03 Task 2 | COVERED | Four-layer inventory equality.
REQ | EVENT-03 | Complete emitted inventory and association evidence | 06-03 | COVERED | Closes only the failed inventory evidence.
CONTEXT | D-01/D-02 | Existing taxonomy and typed identity | 06-03 Task 1 | COVERED | No new taxonomy.
CONTEXT | D-03/D-04 | Metadata semantics and private compatibility source | 06-03 Task 1 | COVERED | Existing public payloads only.
CONTEXT | D-05/D-06 | Preserve behavior, use existing offline patterns | 06-03 Task 2 | COVERED | Test/docs-only change, no dependency.
</source_coverage>

<verification>
Run `cargo fmt --check`, the focused exhaustive inventory unit test, `cargo test -p pi-coding-agent --test event_boundary_guards`, `cargo test -p pi-coding-agent --test product_event_contract`, `cargo check -p pi-coding-agent --all-targets`, and `git diff --check`.
</verification>

<success_criteria>
All 45 current events are individually bound to exact public typed identity and representative payload facts, and deterministic guards prove that internal source, fixture, expected inventory, and documentation cannot drift independently.
</success_criteria>

<output>
Create `.planning/phases/06-product-event-inventory-and-typed-contract/06-03-SUMMARY.md` when done.
</output>
