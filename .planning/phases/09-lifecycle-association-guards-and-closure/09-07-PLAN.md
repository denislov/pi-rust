---
phase: 09-lifecycle-association-guards-and-closure
plan: 07
type: execute
wave: 5
depends_on: [09-05]
files_modified:
  - crates/pi-coding-agent/src/interactive/app.rs
  - crates/pi-coding-agent/src/interactive/loop.rs
  - crates/pi-coding-agent/src/interactive/prompt_task.rs
  - crates/pi-coding-agent/src/interactive/mod.rs
  - crates/pi-coding-agent/src/lib.rs
  - crates/pi-coding-agent/tests/interactive_mode.rs
autonomous: true
requirements: [CLIENT-04, COMPAT-03]
must_haves:
  truths:
    - "D-17: UI loop exit detaches only; explicit top-level runtime owner performs final shutdown after owner restoration."
    - "D-18: Interactive applies one typed lifecycle transition and never silently reconnects/retries/retargets."
  artifacts:
    - path: crates/pi-coding-agent/src/interactive/loop.rs
      provides: client detach boundary
    - path: crates/pi-coding-agent/src/interactive/app.rs
      provides: top-level owner shutdown boundary
  key_links:
    - from: run_interactive_loop_with_input
      to: connection detach
      via: loop cleanup
      pattern: detach
    - from: process-level Interactive exit while operation is running
      to: CodingAgentRuntimeShutdownHandle::request_shutdown
      via: handle captured before PromptTask owner transfer
      pattern: request_shutdown
---
<objective>
Separate Interactive client detach from top-level runtime-owner shutdown.

Purpose: Prevent closing one UI from terminating shared session work while preserving final process drain.
Output: Stable UI connection lifecycle, explicit transition projection, owner-restored shutdown, and deterministic tests.
</objective>
<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/09-lifecycle-association-guards-and-closure/09-05-SUMMARY.md
@crates/pi-coding-agent/src/interactive/app.rs
@crates/pi-coding-agent/src/interactive/loop.rs
@crates/pi-coding-agent/src/interactive/prompt_task.rs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement and prove the embedded UI detach boundary</name>
  <files>crates/pi-coding-agent/src/interactive/loop.rs, crates/pi-coding-agent/tests/interactive_mode.rs</files>
  <read_first>crates/pi-coding-agent/tests/interactive_mode.rs, crates/pi-coding-agent/src/interactive/app.rs, crates/pi-coding-agent/src/interactive/loop.rs, crates/pi-coding-agent/src/interactive/prompt_task.rs</read_first>
  <behavior>
    - Embedded/UI loop exit detaches the connection and leaves admitted operation/runtime alive.
    - Stale/detached/shutdown rejection produces one explicit lifecycle transition without automatic recovery.
  </behavior>
  <action>Add deterministic D-17/D-18 embedded-loop cases and implement the matching `run_interactive_loop_with_input` cleanup in this same task. Use injected input and a channel-gated admitted operation to prove every loop exit calls public detach only, leaves runtime work alive, emits one explicit lifecycle transition for stale/detached/shutdown rejection, and never silently reconnects, retries, retargets control, or requests runtime shutdown. Do not require a manual terminal session or reference process-owner behavior that Task 2 has not installed.</action>
  <acceptance_criteria>The embedded loop lifecycle filter passes immediately and proves detach-only ownership without hidden recovery.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent --test interactive_mode embedded_interactive_lifecycle --quiet</automated></verify>
  <done>The UI/client ownership boundary is implemented and green independently of process shutdown.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement and prove the process-owner shutdown boundary</name>
  <files>crates/pi-coding-agent/src/interactive/app.rs, crates/pi-coding-agent/src/interactive/loop.rs, crates/pi-coding-agent/src/interactive/prompt_task.rs, crates/pi-coding-agent/src/interactive/mod.rs, crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/tests/interactive_mode.rs</files>
  <read_first>crates/pi-coding-agent/src/interactive/app.rs, crates/pi-coding-agent/src/interactive/loop.rs, crates/pi-coding-agent/src/interactive/prompt_task.rs, crates/pi-coding-agent/src/interactive/mod.rs, crates/pi-coding-agent/src/lib.rs</read_first>
  <behavior>
    - `run_interactive_mode` invokes shutdown only when it owns final process runtime and after all task results restore the owner.
    - A process-level exit signal may request Phase A through the opaque handle while the task owns the session, but the loop/result envelope must retain and restore the task owner before `shutdown()` performs Phase B.
    - Typed lifecycle state is projected once, separately from transcript/product workflow events.
  </behavior>
  <action>Add process-owner lifecycle cases to `interactive_mode` in this task, then implement D-17 at `run_interactive_mode` and the existing owner-return envelopes until they pass. Capture `CodingAgentRuntimeShutdownHandle` before spawning any owner task and reserve runtime shutdown for process-facing final exit. If final process exit occurs during a task, request Phase A immediately, prove admission/control rejection before releasing the task, retain and drain the task instead of dropping its join/result channels, restore the same `CodingAgentSession` through every `PromptTaskResult` success/error envelope, and evolve `LoopResult` (or the equivalent loop exit envelope) so every successful/error exit path carries that restored unique owner from `run_interactive_loop_with_input` back to the process-facing `run_interactive_mode`. Only after receiving that owner may `run_interactive_mode` call owner `shutdown()` for Phase B. Assert exact terminal-before-lifecycle-before-close ordering. Preserve Task 1's detach-only embedded boundary; never turn loop exit into implicit Abort, owner drop, auto-reconnect, retry, or handle retarget.</action>
  <acceptance_criteria>Interactive lifecycle tests and all existing transcript/navigation/event-order assertions pass; every loop exit returns the restored unique `CodingAgentSession` to `run_interactive_mode`, which alone performs Phase B shutdown, without weakening Task 1's detach-only semantics.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent --test interactive_mode --quiet && cargo test -p pi-coding-agent --lib interactive --quiet</automated></verify>
  <done>Interactive honors client/runtime ownership boundaries with deterministic lifecycle projection.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| UI loop -> shared runtime | Closing one client must not gain authority to terminate runtime work. |
| Background task -> top-level owner | Shutdown must wait until unique session owner is restored. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Elevation of Privilege | UI exit ownership | high | mitigate | Separate detach and explicit owner shutdown boundaries. |
| T-09-02 | Denial of Service | owner restoration/drain | high | mitigate | Preserve owner envelopes and deterministic active-work tests. |
</threat_model>
## Artifacts this phase produces
- Stable Interactive client connection ownership and detach cleanup.
- Process-owner-only shutdown wiring.
- `interactive_lifecycle` deterministic tests.
<verification>Interactive integration and lib tests pass with existing projections unchanged.</verification>
<success_criteria>Closing a UI never kills shared work; final owner exit drains and closes the runtime exactly once.</success_criteria>
<output>Create `.planning/phases/09-lifecycle-association-guards-and-closure/09-07-SUMMARY.md` when done.</output>
