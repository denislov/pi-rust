---
phase: 09-lifecycle-association-guards-and-closure
plan: 04
type: execute
wave: 3
depends_on: [09-03]
files_modified:
  - crates/pi-coding-agent/src/coding_session/mod.rs
  - crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs
  - crates/pi-coding-agent/src/coding_session/public_projection.rs
  - crates/pi-coding-agent/src/coding_session/public_operation.rs
  - crates/pi-coding-agent/src/coding_session/operation_control.rs
  - crates/pi-coding-agent/src/coding_session/intent_router.rs
  - crates/pi-coding-agent/src/coding_session/manual_compaction_flow.rs
  - crates/pi-coding-agent/src/coding_session/flow_service.rs
  - crates/pi-coding-agent/src/coding_session/manual_compaction_service.rs
  - crates/pi-coding-agent/src/coding_session/error.rs
  - crates/pi-coding-agent/src/coding_session/event_service.rs
  - crates/pi-coding-agent/tests/public_api.rs
  - crates/pi-coding-agent/tests/operation_association.rs
autonomous: true
requirements: [CONTROL-02, COMPAT-03]
must_haves:
  truths:
    - "D-06/D-07: TerminalAssociated finalization consumes exactly one matching root-event evidence before returning, and canonical Compact cancellation remains a typed `CodingSessionError::Cancelled` production path."
    - "D-08: PartialCommit retains the original id and records exact uncertain event evidence or TerminalUncertain without retry fabrication."
    - "D-09: OutcomeOnly uses a distinct acknowledgement path and no guessed event sequence."
  artifacts:
    - path: crates/pi-coding-agent/src/coding_session/mod.rs
      provides: evidence-driven submission finalization
    - path: crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs
      provides: exhaustive submitted terminal anchors
    - path: crates/pi-coding-agent/src/coding_session/operation_control.rs
      provides: cloneable crate-private Compact cancellation request handle bound to the active admitted identity
  key_links:
    - from: EventService::emit return value
      to: submitted terminal anchor
      via: exact operation-id/kind evidence
      pattern: terminal_operation
---
<objective>
Replace global latest-sequence guessing with exact root-event, outcome-only, and terminal-uncertain finalization.

Purpose: Close operation id -> submitted state -> terminal outcome/event correlation without inventing evidence.
Output: Generic 15-operation submission provenance, typed anchors, exact acknowledgement, and PartialCommit recovery.
</objective>
<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/09-lifecycle-association-guards-and-closure/09-RESEARCH.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-03-SUMMARY.md
@crates/pi-coding-agent/src/coding_session/session_log/transaction.rs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Generalize submission provenance and terminal anchors</name>
  <files>crates/pi-coding-agent/src/coding_session/public_operation.rs, crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/tests/public_api.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/public_operation.rs, crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/tests/public_api.rs</read_first>
  <behavior>
    - Preparation supports all 15 operations; only Prompt applies draft fingerprint/matching/acceptance clearing.
    - ProductEvent, OutcomeOnly, and TerminalUncertain anchors are exhaustive and cleared only by their matching acknowledgement contract.
    - No path represents OutcomeOnly with an event cursor.
  </behavior>
  <action>Add `submission_association` and `outcome_acknowledgement` cases to `public_api` in this task, then use Plan 02's descriptor to replace Prompt-only provenance with exhaustive submitted kind/classification while preserving Prompt-specific draft rules until those cases pass. Introduce D-08/D-09 typed terminal anchors and a generation-scoped outcome acknowledgement. Event acknowledgement clears only exact event anchors; outcome acknowledgement clears only its matching operation/ack identity; uncertainty remains visible until recovery resolves it.</action>
  <acceptance_criteria>All operation classes enter submitted state under the admitted id, and acknowledgement domains cannot clear one another.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent submission_association --quiet && cargo test -p pi-coding-agent outcome_acknowledgement --quiet</automated></verify>
  <done>Submitted provenance and terminal anchors represent all 15 operations exactly.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Finalize from exact root evidence and preserve PartialCommit uncertainty</name>
  <files>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/operation_control.rs, crates/pi-coding-agent/src/coding_session/intent_router.rs, crates/pi-coding-agent/src/coding_session/manual_compaction_flow.rs, crates/pi-coding-agent/src/coding_session/flow_service.rs, crates/pi-coding-agent/src/coding_session/manual_compaction_service.rs, crates/pi-coding-agent/src/coding_session/error.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/tests/operation_association.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/operation_control.rs, crates/pi-coding-agent/src/coding_session/intent_router.rs, crates/pi-coding-agent/src/coding_session/manual_compaction_flow.rs, crates/pi-coding-agent/src/coding_session/flow_service.rs, crates/pi-coding-agent/src/coding_session/manual_compaction_service.rs, crates/pi-coding-agent/src/coding_session/error.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/coding_session/event.rs, crates/pi-coding-agent/src/coding_session/session_log/transaction.rs, crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/tests/operation_association.rs</read_first>
  <behavior>
    - Finalization validates root operation id/kind and cardinality from `EventService::emit` evidence or a coordinator exact index.
    - Canonical outcome returns only after matching terminal publication/commit for TerminalAssociated operations.
    - Compact success emits both same-id `CompactionCompleted` and compatibility `PromptCompleted`, but only the descriptor-permitted Compact-kind event counts as its root.
    - `OperationControl::begin` binds the admitted operation id plus a monotonic internal generation to the active guard, creates a cancellation token only for Compact, and carries its clone on `OperationPermit`; canonical Compact passes that token through `ManualCompactionOptions` into `FlowRunOptions.cancel` rather than calling the uncancellable default `Flow::run` path.
    - A cloneable crate-private `CompactCancellationHandle` captured from `CodingAgentSession` before owner transfer can request cancellation only for the exact currently active Compact operation id; no-active, non-Compact, stale, and mismatched identities fail closed and shutdown never calls it.
    - `ManualCompactionFlow` maps `FlowError::Cancelled` directly to `CodingSessionError::Cancelled`; Compact provider failure and cancellation both use the existing `PromptTurnOutcome::Failed`/`PromptFailed` compatibility path, while the typed error distinguishes cancellation and no aborted compatibility event is introduced.
    - PartialCommit yields the same operation id, zero-or-one root event, exact durability uncertainty when event exists, and TerminalUncertain otherwise.
  </behavior>
  <action>Create `tests/operation_association.rs` in this task, then refactor `SubmissionCommitGuard::finish` and canonical run finalization per D-06 through D-08 until its focused tests pass. First close the live Compact cancellation path identified by CodeGraph. Replace the kind-only active slot behind `OperationState` with an internal active identity containing `OperationKind`, the admission `capability_snapshot.operation_id`, a monotonically increasing generation, and an optional `tokio_util::sync::CancellationToken`; change `OperationControl::begin`/`IntentRouter::admit_operation` to receive that admitted id, create a token only for `OperationKind::Compact`, put its clone on `OperationPermit`, and make `OperationGuard::drop` clear only its exact generation so an old guard cannot affect a later operation. Add cloneable crate-private `CompactCancellationHandle` plus a typed crate-private rejection enum. `CodingAgentSession::compact_cancellation_handle()` returns a clone before the unique session owner is moved; `CompactCancellationHandle::cancel(operation_id: &str)` locks the shared active identity and cancels only when both the supplied id and current kind Compact match. It must return distinct typed rejection for no active operation, a non-Compact active operation, and a stale/mismatched id, disclose no token or generic dispatch/control, and remain completely separate from detach and Plan 05 shutdown. This is unconditional production-shaped internal plumbing required to make canonical Flow cancellation reachable; no first-party adapter or public facade consumes it in this phase.

Pass the admitted Compact permit token into `ManualCompactionOptions`, make `FlowService::run_manual_compaction_graph` call `ManualCompactionFlow::run_with_options` with `FlowRunOptions { cancel: Some(token), ..Default::default() }`, and change `manual_compaction_flow::flow_error` to exhaustively map `FlowError::Cancelled` to `CodingSessionError::Cancelled` while retaining `CodingSessionError::Flow` for other graph errors. Keep `ManualCompactionService::run_persistent` converting that typed cancellation into the existing same-id `manual_compaction_failed_outcome` and `EventService::emit_prompt_outcome` path. Put the deterministic canonical cancellation fixture in the existing `#[cfg(test)]` module in `coding_session/mod.rs`, where crate-private access is legitimate: prepare the Compact submission through Task 1's generalized connection path, retain that cloneable connection, clone `CompactCancellationHandle`, then move the unique session into a spawned task running the real canonical Compact with a deterministic faux-provider/Flow gate. While `run(&mut self)` owns the moved session, read the exact admitted id from `connection.state().submitted_operation` after its status becomes `Running`, call `handle.cancel(&operation_id)` between the gated Flow nodes, then restore the session from the task and prove the returned failed outcome plus emitted `Workflow::PromptFailed` carry that id and the typed `cancelled` error code. In the same colocated family, prove wrong/stale id, no-active, and non-Compact requests reject without cancelling the current/later operation. Do not use sleeps, parse error text, construct synthetic event evidence, expose a public Compact control, or bind runtime shutdown to cancellation.

Keep `tests/operation_association.rs` external-only: it owns public success/provider-failure root cardinality and PartialCommit/TerminalUncertain fixtures and must not claim to invoke or import `CompactCancellationHandle`; it may assert cancellation-shaped public values only if a public fixture can observe them without private calls. Remove the `current_event_sequence()` latest-global guess entirely. Have root-emission helpers return their exact `ProductEvent` and propagate that value directly to finalization; do not add a second retained-history/index authority. Validate descriptor-permitted event variant, operation id, admitted kind, outcome branch, and one-time cardinality. For Compact success, assert the same id emits `Session::CompactionCompleted` and `Workflow::PromptCompleted`, count only `CompactionCompleted` because descriptor matching requires both id and admitted Compact kind, and leave `PromptCompleted` as compatibility evidence for Prompt kind. For Compact provider failure and operation-scoped Flow cancellation, preserve `PromptTurnOutcome::Failed` plus `Workflow::PromptFailed`, normalizing that exact failed branch to admitted Compact only inside association and using `CodingSessionError::Cancelled` to distinguish cancellation. Finalize OutcomeOnly from typed outcome. Exercise existing append/manifest fault seams; never retry under the same id to manufacture terminal evidence and never scan bounded retained history as authority.</action>
  <acceptance_criteria>Exact cardinality tests pass for success/failure/cancellation and injected PartialCommit records correct zero-or-one evidence under the original id.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent compact_cancellation --lib --quiet && cargo test -p pi-coding-agent --test operation_association terminal_association --quiet && cargo test -p pi-coding-agent --test operation_association partial_commit_association --quiet</automated></verify>
  <done>Canonical finalization is evidence-driven and durable uncertainty remains attributable.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Event publication -> submitted terminal state | Correlation evidence crosses bounded event transport into client state. |
| Persistence transaction -> public recovery | Partial durability must not be reported as definite completion. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-03 | Tampering / Repudiation | finalization/PartialCommit | high | mitigate | Exact id/kind evidence, cardinality checks, typed uncertainty. |
</threat_model>
## Artifacts this phase produces
- Exhaustive submitted terminal anchor and outcome acknowledgement types.
- Generic 15-operation submission provenance.
- Evidence-driven `SubmissionCommitGuard` finalization with no global latest-sequence guess.
- `CompactCancellationHandle` -> exact admitted Compact identity -> operation-permit token -> `FlowRunOptions.cancel` wiring with explicit `FlowError::Cancelled` -> `CodingSessionError::Cancelled` mapping and colocated canonical-run tests.
- `partial_commit_association` fixtures.
<verification>`cargo test -p pi-coding-agent submission_association --quiet && cargo test -p pi-coding-agent outcome_acknowledgement --quiet && cargo test -p pi-coding-agent compact_cancellation --lib --quiet && cargo test -p pi-coding-agent --test operation_association terminal_association --quiet && cargo test -p pi-coding-agent --test operation_association partial_commit_association --quiet` passes at the end of this plan; private cancellation reachability is proven only in the colocated lib test, while the external integration target remains public-contract-only.</verification>
<success_criteria>Every terminal semantic has exact evidence or explicit uncertainty; none uses a fabricated sequence.</success_criteria>
<output>Create `.planning/phases/09-lifecycle-association-guards-and-closure/09-04-SUMMARY.md` when done.</output>
