---
phase: 09-lifecycle-association-guards-and-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pi-coding-agent/src/coding_session/public_projection.rs
  - crates/pi-coding-agent/src/coding_session/error.rs
  - crates/pi-coding-agent/src/lib.rs
  - crates/pi-coding-agent/tests/public_api.rs
  - crates/pi-coding-agent/tests/api_boundary_guards.rs
autonomous: true
requirements: [CLIENT-04, CONTROL-02]
must_haves:
  truths:
    - "D-01/D-03/D-04/D-14: compile-ready lifecycle outcomes and rejection categories are typed and externally observable without private authority."
    - "D-05/D-09: compile-ready submitted terminal anchors distinguish exact events, outcome acknowledgement, and terminal uncertainty."
  artifacts:
    - path: crates/pi-coding-agent/tests/public_api.rs
      provides: external value/serialization contract tests that pass in Wave 1
    - path: crates/pi-coding-agent/tests/api_boundary_guards.rs
      provides: public surface privacy assertions
  key_links:
    - from: crates/pi-coding-agent/tests/public_api.rs
      to: pi_coding_agent::api
      via: external-only imports
      pattern: pi_coding_agent::api
---

<objective>
Install the compile-ready public lifecycle, rejection, and submitted-terminal value contracts before runtime implementation.

Purpose: Give Waves 2-4 stable types they can compile against without introducing cross-wave failing integration targets, and prevent lifecycle authority leakage.
Output: Curated public value types, deterministic serialization/import tests, and facade/privacy assertions covering D-01, D-03, D-04, D-08, D-09, D-13, D-14, and D-18.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-CONTEXT.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-RESEARCH.md
@crates/pi-coding-agent/src/coding_session/public_projection.rs
@crates/pi-coding-agent/src/lib.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Install compile-ready lifecycle and terminal-anchor values</name>
  <files>crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/coding_session/error.rs, crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/tests/public_api.rs</files>
  <read_first>crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/lib.rs, .planning/phases/09-lifecycle-association-guards-and-closure/09-VALIDATION.md</read_first>
  <behavior>
    - Detach, shutdown, and lifecycle rejection categories are exhaustive stable values per D-03/D-04/D-14/D-18.
    - Submitted terminal projections distinguish exact event sequence plus durability, OutcomeOnly acknowledgement identity, and TerminalUncertain recovery per D-08/D-09.
    - Public serialization and error codes omit coordinator state, raw generation authority, receipt signatures, and recovery implementation details.
  </behavior>
  <action>Define the curated public enums/newtypes and explicit stable code/Serde mappings required by D-01, D-03, D-04, D-08, D-09, D-14, and D-18, then re-export them only through `pi_coding_agent::api`. Add external import, exhaustive value, and serialization tests that are green in this task; do not reference detach/shutdown methods or runtime behavior that Waves 2-4 have not implemented. Keep opaque acknowledgement construction private and preserve all Phase 8 value shapes.</action>
  <acceptance_criteria>Focused tests compile through the stable facade, cover every value/anchor category, and pass before lifecycle runtime wiring begins.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent --test public_api lifecycle_values --quiet</automated></verify>
  <done>Wave 1 publishes a compile-ready lifecycle and anchor vocabulary without references that require later implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze facade authority and serialization privacy</name>
  <files>crates/pi-coding-agent/tests/api_boundary_guards.rs, crates/pi-coding-agent/tests/public_api.rs</files>
  <read_first>crates/pi-coding-agent/tests/api_boundary_guards.rs, crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/src/coding_session/error.rs</read_first>
  <behavior>
    - Public lifecycle methods derive identity/generation from the connection and expose no arbitrary cross-client mutation parameters.
    - Stable codes and serialized values omit coordinator/service/channel/map, receipt signature, retained-capacity, and internal durability/recovery details.
    - CodingAgentClientConnection has no ordinary-operation dispatcher.
  </behavior>
  <action>Extend public signature/source and Serde snapshot tests for D-13 against Task 1's compile-ready values. Require only curated lifecycle/association types in `api`; reject raw sender/receiver, coordinator/service/control transport, maps/queues, arbitrary authority selectors, Debug-derived status codes, and a connection dispatch method. Add positive adjacent facade imports so privacy failure cannot mask a broken stable API; defer method-level detach/shutdown authority assertions to the tasks that implement those methods.</action>
  <acceptance_criteria>Tests prove the additive API is usable while all internal authority and implementation detail remain private.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent --test api_boundary_guards public_lifecycle_values --quiet && cargo test -p pi-coding-agent --test public_api lifecycle_serialization --quiet</automated></verify>
  <done>The implementation has a fail-closed public authority envelope.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| External Rust consumer -> public facade | Untrusted caller may attempt cross-client or raw-channel authority. |
| Internal lifecycle state -> public values | Private generation, receipt, and durability implementation details must not leak. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Spoofing / Elevation | public connection mutation | high | mitigate | Connection-derived authority and external signature guards. |
| T-09-04 | Information Disclosure | lifecycle serialization/errors | high | mitigate | Exact snapshots and curated export ledger. |
</threat_model>

## Artifacts this phase produces
- Wave 1 `lifecycle_values` and `lifecycle_serialization` public test families.
- Public facade privacy ledger for lifecycle outcomes, rejections, and terminal anchors.

<verification>`cargo test -p pi-coding-agent --test public_api lifecycle_values --quiet && cargo test -p pi-coding-agent --test public_api lifecycle_serialization --quiet && cargo test -p pi-coding-agent --test api_boundary_guards public_lifecycle_values --quiet` passes at the end of this plan.</verification>
<success_criteria>The public contract is exhaustive, typed, additive, and authority-safe.</success_criteria>
<output>Create `.planning/phases/09-lifecycle-association-guards-and-closure/09-01-SUMMARY.md` when done.</output>
