---
phase: 09-lifecycle-association-guards-and-closure
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs
  - crates/pi-coding-agent/src/coding_session/client_service.rs
  - crates/pi-coding-agent/src/coding_session/public_projection.rs
  - crates/pi-coding-agent/src/coding_session/error.rs
  - crates/pi-coding-agent/src/coding_session/event_service.rs
  - crates/pi-coding-agent/src/lib.rs
  - crates/pi-coding-agent/tests/public_api.rs
  - crates/pi-coding-agent/tests/api_boundary_guards.rs
autonomous: true
requirements: [CLIENT-04, CONTROL-02]
must_haves:
  truths:
    - "D-01/D-02/D-03: detach invalidates and wakes exactly one generation while preserving reconnectable client facts and active work."
    - "D-14/D-18: every old-handle mutation/replay/control path fails closed with the correct typed lifecycle state."
  artifacts:
    - path: crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs
      provides: sole lifecycle state machine
    - path: crates/pi-coding-agent/src/coding_session/public_projection.rs
      provides: typed detach and detach-aware receiver
  key_links:
    - from: CodingAgentClientConnection::detach
      to: SnapshotCoordinator
      via: derived private handle
      pattern: detach
---
<objective>
Implement generation-scoped detach and lifecycle-aware receiver closure under the sole SnapshotCoordinator authority.

Purpose: Make disconnect recoverable and idempotent without cancelling session-owned operations.
Output: Typed public detach, shared validation, receiver wake-up, and reconnect preservation.
</objective>
<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/09-lifecycle-association-guards-and-closure/09-CONTEXT.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-PATTERNS.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-01-SUMMARY.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-02-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement coordinator lifecycle transitions and fail-closed validation</name>
  <files>crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/src/coding_session/client_service.rs, crates/pi-coding-agent/src/coding_session/error.rs, crates/pi-coding-agent/tests/public_api.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/src/coding_session/client_service.rs, crates/pi-coding-agent/src/coding_session/error.rs, crates/pi-coding-agent/tests/public_api.rs</read_first>
  <behavior>
    - Current attached generation returns Detached, same detached generation AlreadyDetached, older generation StaleGeneration.
    - Detach retains acknowledgement cursor, all drafts, submitted terminal state, accepted receipts, and active operation/control channel state needed for new-generation rebind.
    - State, ack, outcome ack, draft, submission, replay, and control validate attached/runtime lifecycle through one shared gate.
  </behavior>
  <action>In the same task, add deterministic `public_api` lifecycle behavior fixtures and implement the coordinator transitions needed to make them green. Extend `SnapshotState`/`ClientRecord` with exhaustive runtime and connection lifecycle state per D-01 through D-03 and D-14. Cover first/repeat/stale detach plus state, acknowledgement, outcome acknowledgement, draft, submission, replay, and control rejection without referencing shutdown methods that Plan 05 has not implemented. Preserve client records and recoverable contents; keep `ClientService` zero-authority. Centralize lifecycle validation so stale, detached, shutting-down, and shut-down remain distinct. Never hold the standard mutex across await or notification.</action>
  <acceptance_criteria>Detach outcomes are exact, reconnect restores preserved facts, and each mutation family returns typed lifecycle rejection.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent detach --quiet && cargo test -p pi-coding-agent lifecycle_rejection --quiet</automated></verify>
  <done>The sole coordinator owns an idempotent, recoverable detach state machine.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Publish the facade and wake blocked receivers</name>
  <files>crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/tests/api_boundary_guards.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/tests/api_boundary_guards.rs</read_first>
  <behavior>
    - Connection detach derives authority from self and returns the stable typed outcome.
    - A receiver blocked in recv wakes on detach; try_recv/recv cannot return a business event after lifecycle invalidation wins.
    - Detaching during Prompt leaves the operation running; old control rejects and reconnect can rebind control to the active Prompt.
  </behavior>
  <action>Add the public detach method over Plan 01's curated D-18 values and a private lifecycle notifier/epoch selected by the receiver wrapper. Extend `public_api` and `api_boundary_guards` in this same task so the exact method signature, blocked receiver wake-up, and connection-derived authority become green immediately. Transition under coordinator lock, release it, notify, and validate before delivery. Keep raw broadcast receiver/sender private and preserve the atomic replay/live cut. Prove D-02 with a deterministically gated active Prompt and no sleep.</action>
  <acceptance_criteria>Blocked receivers close promptly, no post-detach event leaks, active work continues, and facade guards from Plan 01 pass.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent --test public_api detach --quiet && cargo test -p pi-coding-agent --test api_boundary_guards public_lifecycle --quiet</automated></verify>
  <done>Public detach and receiver behavior are complete without a second authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Connection handle -> coordinator | Generation-scoped authority crosses into shared client state. |
| Coordinator -> event receiver | Lifecycle invalidation races event delivery. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Spoofing / Elevation | connection generation | high | mitigate | Shared validator and connection-derived handle. |
| T-09-02 | Denial of Service | blocked receiver/lifecycle race | high | mitigate | Explicit notification and before/after validation. |
</threat_model>
## Artifacts this phase produces
- Runtime/connection lifecycle enums and coordinator transitions.
- Public detach outcome/rejections and detach-aware receiver wrapper.
- `detach` and `lifecycle_rejection` green test families.
<verification>`cargo test -p pi-coding-agent detach --quiet && cargo test -p pi-coding-agent lifecycle_rejection --quiet && cargo test -p pi-coding-agent --test api_boundary_guards public_lifecycle --quiet` passes at the end of this plan; no task depends on later shutdown/event APIs to compile.</verification>
<success_criteria>Detach is typed, idempotent, state-preserving, receiver-visible, and never cancels admitted work.</success_criteria>
<output>Create `.planning/phases/09-lifecycle-association-guards-and-closure/09-03-SUMMARY.md` when done.</output>
