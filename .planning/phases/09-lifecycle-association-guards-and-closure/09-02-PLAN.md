---
phase: 09-lifecycle-association-guards-and-closure
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/pi-coding-agent/src/coding_session/public_operation.rs
autonomous: true
requirements: [CONTROL-02]
must_haves:
  truths:
    - "D-05: all 15 public operations occur exactly once in a fail-closed TerminalAssociated/OutcomeOnly/NotApplicable matrix."
    - "D-06/D-07: root terminal evidence is defined by operation id and root kind, never generic terminal status."
  artifacts:
    - path: crates/pi-coding-agent/src/coding_session/public_operation.rs
      provides: exhaustive association descriptor
    - path: crates/pi-coding-agent/src/coding_session/public_operation.rs
      provides: closed-set descriptor unit fixtures colocated with the implementation
  key_links:
    - from: CodingAgentOperation
      to: operation association descriptor
      via: exhaustive match
      pattern: TerminalAssociated|OutcomeOnly|NotApplicable
---
<objective>
Create the executable 15-operation association ledger and its immediately green closed-set tests.

Purpose: Make omission, duplication, renaming, or false root-terminal promotion fail closed before finalization changes.
Output: One source-of-truth descriptor plus exhaustive unit fixtures implementing D-05; Plan 04 owns runtime cardinality fixtures beside finalization.
</objective>
<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/09-lifecycle-association-guards-and-closure/09-RESEARCH.md
@crates/pi-coding-agent/src/coding_session/public_operation.rs
@crates/pi-coding-agent/src/coding_session/event.rs
@docs/product-event-contract.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Install the closed association descriptor</name>
  <files>crates/pi-coding-agent/src/coding_session/public_operation.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/public_operation.rs, crates/pi-coding-agent/src/coding_session/operation.rs, crates/pi-coding-agent/src/coding_session/event.rs, .planning/phases/09-lifecycle-association-guards-and-closure/09-RESEARCH.md</read_first>
  <behavior>
    - Prompt, Compact, SelfHealingEdit, InvokeAgent, and InvokeTeam are TerminalAssociated.
    - Compact success accepts only `Session::CompactionCompleted`; its same-id `Workflow::PromptCompleted` compatibility emission remains a Prompt-kind terminal and is excluded from the admitted Compact root count.
    - Compact provider failure and the canonical operation-scoped Flow cancellation path both retain the live `PromptTurnOutcome::Failed` plus same-id `Workflow::PromptFailed` evidence; cancellation is distinguished by the typed outcome error, not by widening the descriptor or adding an aborted compatibility variant.
    - The remaining ten current operations are OutcomeOnly; NotApplicable has zero current operation rows and controls remain outside the matrix.
    - Each descriptor supplies stable submitted kind, outcome family, and permitted root evidence without relying on optional internal static_kind.
  </behavior>
  <action>Extend the existing exhaustive 15-case contract table into the executable D-05 descriptor using the corrected matrix in `09-RESEARCH.md`. Encode branch-specific permitted root evidence: Compact success admits only `Session::CompactionCompleted`; the additional same-id `Workflow::PromptCompleted` emitted for compatibility remains Prompt-kind and therefore does not count for the admitted Compact descriptor. Both provider failure and Plan 04's canonical operation-scoped Flow cancellation produce `PromptTurnOutcome::Failed` and `Workflow::PromptFailed`; normalize that exact failed variant to admitted kind Compact only when operation id, admitted descriptor, and failed outcome branch match. Keep cancellation-versus-provider-failure discrimination in the typed `CodingSessionError` carried by the failed outcome rather than making error state an association-descriptor input. Use exhaustive matches and exact set/cardinality assertions so variant drift fails compilation or tests. Preserve Abort/Steer/FollowUp outside `CodingAgentOperation`; do not add synthetic control rows, a public generic cancellation API, a compaction abort event, new wire variants, or wider root associations for symmetry.</action>
  <acceptance_criteria>Every current operation maps exactly once to its researched class/outcome/submitted kind and the descriptor rejects drift.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent association_matrix --quiet</automated></verify>
  <done>The 15-operation taxonomy is executable and fail closed.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Operation admission -> event correlation | A related event must not be accepted as the operation terminal. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-03 | Tampering / Repudiation | operation/event association | high | mitigate | Closed descriptor plus exact id/kind/cardinality tests. |
</threat_model>
## Artifacts this phase produces
- Public operation association descriptor and exhaustive 15-row tests.
- Closed-set descriptor unit fixtures that are green in Wave 1; Plan 04 creates the runtime `operation_association` target together with finalization.
<verification>`cargo test -p pi-coding-agent association_matrix --quiet` passes at the end of this plan without compiling a future integration target.</verification>
<success_criteria>No operation can be unclassified and no non-root event can satisfy terminal association.</success_criteria>
<output>Create `.planning/phases/09-lifecycle-association-guards-and-closure/09-02-SUMMARY.md` when done.</output>
