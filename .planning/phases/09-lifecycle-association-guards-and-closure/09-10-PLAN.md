---
phase: 09-lifecycle-association-guards-and-closure
plan: 10
type: execute
wave: 7
depends_on: [09-08]
files_modified:
  - crates/pi-coding-agent/src/coding_session/session_service.rs
  - crates/pi-coding-agent/tests/operation_association.rs
autonomous: true
gap_closure: true
requirements: [COMPAT-03, CONTROL-02]
must_haves:
  truths:
    - "D-08: a failed non-leaf transaction that flushed events and then failed manifest update returns PartialCommit with the original admitted operation id."
    - "The same failure records TerminalUncertain association evidence without a replacement operation or duplicate terminal event."
  artifacts:
    - path: crates/pi-coding-agent/src/coding_session/session_service.rs
      provides: PartialCommit mapping for post-fail manifest-update errors
    - path: crates/pi-coding-agent/tests/operation_association.rs
      provides: deterministic failed transaction manifest-failure fixture
  key_links:
    - from: PromptTurnTransaction::fail
      to: CodingSessionError::PartialCommit
      via: manifest update failure map carrying transaction.operation_id()
      pattern: PartialCommit
    - from: failed durable transaction
      to: submitted terminal state
      via: original operation id and TerminalUncertain recovery marker
      pattern: TerminalUncertain
---
<objective>
Preserve operation identity and explicit uncertainty when failed non-leaf transaction finalization partially commits.

Purpose: Close the durability/association gap in `fail_non_leaf_transaction` while preserving successful commit behavior.
</objective>
<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-CONTEXT.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-VERIFICATION.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-REVIEW.md
@crates/pi-coding-agent/src/coding_session/session_service.rs
@crates/pi-coding-agent/tests/operation_association.rs
</context>
<interfaces>
`fail_non_leaf_transaction` obtains `transaction.operation_id()`, calls `transaction.fail(...)` to flush failed-operation events, then updates the manifest. `CodingSessionError::PartialCommit` is the typed uncertainty error used by the neighboring successful commit path. Existing operation-association tests use temp sessions, faux providers, and `StoreFailurePoint::UpdateManifest`.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Map failed non-leaf manifest errors to PartialCommit</name>
  <files>crates/pi-coding-agent/src/coding_session/session_service.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/session_service.rs, crates/pi-coding-agent/src/coding_session/error.rs, crates/pi-coding-agent/src/coding_session/session_log/store.rs</read_first>
  <behavior>
    - After `transaction.fail` flushes envelopes, a manifest error becomes PartialCommit.
    - The error carries the transaction admitted operation id, not a fallback or replacement id.
    - Missing-transaction and successful finalization behavior remain unchanged.
  </behavior>
  <action>Capture the transaction operation id before the fail/manifest sequence and replace raw manifest-error propagation in `fail_non_leaf_transaction` with the typed PartialCommit construction used by `commit_non_leaf_transaction`, preserving store error context. Keep already-flushed events authoritative, do not retry or allocate a replacement operation, and leave the no-transaction skipped-write path intact.</action>
  <verify><automated>cargo test -p pi-coding-agent --lib session_service --quiet</automated></verify>
  <done>Every post-flush manifest failure from failed non-leaf finalization returns attributable PartialCommit uncertainty.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add failed-transaction TerminalUncertain association coverage</name>
  <files>crates/pi-coding-agent/tests/operation_association.rs</files>
  <read_first>crates/pi-coding-agent/tests/operation_association.rs, crates/pi-coding-agent/tests/support/mod.rs, crates/pi-coding-agent/src/coding_session/session_service.rs, crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs</read_first>
  <behavior>
    - The canonical failed path is `CodingAgentSession::run(CodingAgentOperation::Prompt(...))` with `FauxProvider::text_call("partial", StopReason::Error)`; `StoreFailurePoint::UpdateManifest` is armed for the manifest update after `transaction.fail` flushes exactly one same-id `Workflow::PromptFailed` root event.
    - The existing canonical Compact manifest-failure case is the no-terminal-event control: manifest replacement fails after durable append but before any descriptor-permitted Compact root is published, so its exact root-terminal cardinality is zero.
    - Both cases return PartialCommit with their exact admitted id and project that id through TerminalUncertain; replay contains exactly one PromptFailed root for the failed Prompt case and exactly zero descriptor-permitted roots for the Compact control, never a duplicate or fabricated terminal event.
  </behavior>
  <action>Extend the deterministic Plan 09-04 association fixture with one concrete public failed operation: seed the persistent session, prepare a submitted lease for `CodingAgentOperation::Prompt`, script its next faux-provider call with `StopReason::Error`, and make the session manifest unwritable at the point corresponding to the test-only `StoreFailurePoint::UpdateManifest` hook so the real `fail_non_leaf_transaction` first flushes the failed Prompt envelopes and then fails manifest update. Restore permissions before assertions. Require `PartialCommit.operation_id` to equal the prepared admitted id, require submitted state to carry the same id under `TerminalUncertain`, and reconnect/replay by operation id to count exactly one descriptor-permitted durable `Workflow::PromptFailed` root. Strengthen the existing Compact PartialCommit fixture as the explicit no-terminal-event control: assert the same id/TerminalUncertain invariants and exactly zero descriptor-permitted Compact roots because manifest failure precedes terminal publication. Keep `StoreFailurePoint` and its arming helpers crate-private/test-only per Plan 09-08; do not expose them through `api`, call private helpers from the integration test, retry finalization, or construct synthetic evidence.</action>
  <verify><automated>cargo test -p pi-coding-agent --test operation_association failed_transaction_manifest_failure_preserves_operation_id_and_terminal_uncertain --quiet -- --exact</automated></verify>
  <done>The failed Prompt case has exactly one durable PromptFailed root, the Compact control has exactly zero descriptor-permitted roots, and both manifest-update failures remain attributable through PartialCommit and TerminalUncertain without fabrication.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Session log flush -> manifest update | A failure after event flush must not lose operation identity. |
| Durability error -> public recovery projection | Only typed PartialCommit and TerminalUncertain evidence cross the boundary. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-03 | Tampering / Repudiation | failed transaction identity | high | mitigate | Preserve operation id through PartialCommit and replay assertions. |
| T-09-04 | Information Disclosure | durability errors | high | mitigate | Keep store internals private while retaining stable typed context. |
</threat_model>
<verification>Run the exact failed-transaction test plus operation-association and focused coding-session tests.</verification>
<success_criteria>Post-flush manifest failure reports PartialCommit for the original operation and TerminalUncertain recovery without duplicate terminal evidence.</success_criteria>
<output>Create `.planning/phases/09-lifecycle-association-guards-and-closure/09-10-SUMMARY.md` when done.</output>
