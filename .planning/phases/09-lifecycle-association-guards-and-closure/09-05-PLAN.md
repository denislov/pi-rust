---
phase: 09-lifecycle-association-guards-and-closure
plan: 05
type: execute
wave: 4
depends_on: [09-04]
files_modified:
  - crates/pi-coding-agent/src/coding_session/mod.rs
  - crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs
  - crates/pi-coding-agent/src/coding_session/operation_control.rs
  - crates/pi-coding-agent/src/coding_session/event_service.rs
  - crates/pi-coding-agent/src/coding_session/event.rs
  - crates/pi-coding-agent/src/coding_session/public_event.rs
  - crates/pi-coding-agent/src/coding_session/public_projection.rs
  - crates/pi-coding-agent/src/interactive/event_bridge.rs
  - crates/pi-coding-agent/src/protocol/events.rs
  - crates/pi-coding-agent/src/lib.rs
  - crates/pi-coding-agent/tests/public_api.rs
  - crates/pi-coding-agent/tests/event_boundary_guards.rs
  - docs/product-event-contract.md
autonomous: true
requirements: [CLIENT-04, COMPAT-03]
must_haves:
  truths:
    - "D-04: shutdown closes admission/control first, drains admitted work, emits its terminal before one lifecycle event, then closes receivers."
    - "D-16: lifecycle event/type additions preserve all existing event variants and serialized fields."
  artifacts:
    - path: crates/pi-coding-agent/src/coding_session/mod.rs
      provides: two-phase idempotent runtime shutdown
    - path: crates/pi-coding-agent/src/coding_session/public_event.rs
      provides: additive typed runtime-shutdown event
    - path: crates/pi-coding-agent/src/interactive/event_bridge.rs
      provides: compile-ready non-transcript handling for the additive runtime lifecycle variant
  key_links:
    - from: CodingAgentRuntimeShutdownHandle::request_shutdown
      to: SnapshotCoordinator runtime lifecycle
      via: shared Arc authority while CodingAgentSession is moved into run
      pattern: request_shutdown
    - from: CodingAgentSession::shutdown
      to: EventService
      via: restored-owner drain then lifecycle publication
      pattern: shutdown
---
<objective>
Implement the two-phase runtime shutdown boundary and final additive lifecycle event.

Purpose: Close runtime ownership without aborting admitted work, deadlocking coordinator state, or truncating event publication.
Output: Opaque shared Phase A request handle, typed idempotent owner shutdown, admission/control closure, drain ordering, lifecycle event, and receiver closure.
</objective>
<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/09-lifecycle-association-guards-and-closure/09-CONTEXT.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-04-SUMMARY.md
@crates/pi-coding-agent/src/coding_session/operation_control.rs
@docs/product-event-contract.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add the typed lifecycle event and compile-ready shutdown projection</name>
  <files>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/event.rs, crates/pi-coding-agent/src/coding_session/public_event.rs, crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/interactive/event_bridge.rs, crates/pi-coding-agent/src/protocol/events.rs, crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/tests/event_boundary_guards.rs, docs/product-event-contract.md</files>
  <read_first>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/event.rs, crates/pi-coding-agent/src/coding_session/public_event.rs, crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/interactive/event_bridge.rs, crates/pi-coding-agent/src/protocol/events.rs, crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/tests/event_boundary_guards.rs, crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/tests/protocol_events.rs, crates/pi-coding-agent/tests/interactive_event_bridge.rs, docs/product-event-contract.md</read_first>
  <behavior>
    - Runtime shutdown is one additive typed lifecycle event with no operation id and no root-terminal association.
    - Existing 45 event representations and fields remain unchanged; the closed inventory becomes 46 exact rows.
    - Stable shutdown outcome/event exports disclose no internal lifecycle authority; `CodingProtocolEventAdapter::push_typed` and `CodingEventBridge::handle_typed` remain exhaustive and intentionally produce no legacy protocol, Prompt, compaction, or transcript event for the lifecycle value.
  </behavior>
  <action>Add D-16 as a new variant inside the existing Runtime family through the closed internal-to-public mapping and extend colocated event contract tests in this task. Atomically update every exhaustive inner-variant consumer affected by that addition: both `CodingAgentProductEventKind::as_str` and `legacy_variant_name` in `public_event.rs`, the cfg(test) `typed_event_kind` helper in `coding_session/mod.rs`, `CodingProtocolEventAdapter::push_typed` in `protocol/events.rs`, and `CodingEventBridge::handle_typed` in `interactive/event_bridge.rs`. The protocol arm must emit no legacy `ProtocolEvent`; the interactive bridge arm must return no `UiEvent` and must not masquerade as compaction, Prompt, system notice, or transcript content. Add a colocated interactive bridge test for that empty projection. Lifecycle-specific RPC wire projection remains Plan 06 ownership, while connection/process ownership-specific Interactive lifecycle behavior remains Plan 07 ownership. Treat family-level/wildcard consumers as read-only evidence: `CodingAgentProductEventKind::family`, operation-id/terminal helpers, and `tests/public_api.rs::typed_event_family` already cover the existing Runtime top-level family; `tests/protocol_events.rs` and `tests/interactive_event_bridge.rs` only construct fixtures and do not exhaustively match the runtime inner enum. In the same task, add the public fixture and executable inventory row in `public_event.rs`, change every hard-coded event count and exact-set assertion in `event_boundary_guards.rs` from 45 to 46, and add the matching authoritative row/count/semantics to `docs/product-event-contract.md`. Add/update the protocol adapter's colocated contract case so the new variant compiles and cannot masquerade as Prompt completion. Export Plan 01's stable outcome/event values, but defer the opaque shutdown-request handle and methods to Task 2 where their authority exists. Do not export coordinator, lifecycle epoch, notifier, client/generation selectors, append lifecycle fields to existing event payloads, or leave executable and documented inventories for a later plan.</action>
  <acceptance_criteria>The internal enum, public fixture, executable ledger, public naming matches, cfg(test) exhaustive helper, protocol adapter, interactive bridge, boundary-guard counts, and documented inventory all agree on exactly 46 rows in this task; event contract, interactive bridge, serialization, and boundary guards pass immediately with one additive non-operation Runtime lifecycle value, no synthetic protocol/UI content, and unchanged prior rows.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent event_contract --quiet && cargo test -p pi-coding-agent --lib interactive::event_bridge --quiet && cargo test -p pi-coding-agent --test event_boundary_guards --quiet</automated></verify>
  <done>The lifecycle event projection is compile-ready before shutdown orchestration references it.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement and prove two-phase runtime drain and final closure</name>
  <files>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/src/coding_session/operation_control.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/tests/public_api.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/src/coding_session/operation_control.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/tests/public_api.rs</read_first>
  <behavior>
    - Phase A atomically rejects connect/admission/control/mutations and marks attached generations shutting down without cancelling active work.
    - Phase B waits outside standard mutexes, emits exactly one non-operation lifecycle event after terminal evidence, marks ShutDown, and wakes/closes receivers.
    - `CodingAgentRuntimeShutdownHandle` carries only cloned coordinator shutdown-request authority; it cannot dispatch operations, finalize shutdown, select clients/generations, or publish events.
    - Idle/repeated and active shutdown deterministically prove terminal-before-lifecycle-before-receiver-close ordering without wall-clock sleeps.
  </behavior>
  <action>First add deterministic D-04 cases to `public_api` using oneshot/Notify/barrier gates, then implement shared lifecycle/admission state visible to every async and sync canonical `run` admission until the same task is green. Add a curated opaque `CodingAgentRuntimeShutdownHandle` returned by the session before owner transfer; `request_shutdown()` performs only idempotent Phase A through the shared `Arc<SnapshotCoordinator>`, captures a monotonic lifecycle epoch, revokes connection/control authority, and wakes lifecycle waiters without waiting or publishing the final event. `OperationGuard::drop` already clears active operation through this coordinator; extend that transition to notify drain waiters. The restored unique owner calls `CodingAgentSession::shutdown()` to request Phase A if needed, wait for active-none outside standard mutexes, publish Task 1's lifecycle event exactly once, mark ShutDown, then close/wake receivers. Cover idle, repeat, and a gated admitted operation: invoke Phase A while `run(&mut self)` owns the session, prove new admission/control reject, release normal completion, restore the same owner, and assert root terminal before lifecycle before closure with no implicit Abort. Never clone a second owner, expose coordinator/client selectors, hold `SnapshotCoordinator` mutex across await/send, or turn shutdown into Drop/Abort.</action>
  <acceptance_criteria>Idle/active ordering and lifecycle rejection tests pass through the drain boundary with no early receiver closure or owner duplication.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent shutdown --quiet && cargo test -p pi-coding-agent --test public_api shutdown --quiet</automated></verify>
  <done>Runtime shutdown drains safely, publishes exactly one final lifecycle event, and closes delivery last.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Shutdown request -> runtime admission | Concurrent request changes permission for new work while admitted work owns session. |
| Runtime drain -> event subscribers | Terminal and shutdown ordering must remain complete and non-deadlocking. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-02 | Denial of Service / Tampering | shutdown drain and receivers | high | mitigate | Two phases, no lock across await, deterministic ordering tests. |
| T-09-03 | Repudiation | terminal-before-shutdown evidence | high | mitigate | Exact association from Plan 04 and ordered event assertion. |
</threat_model>
## Artifacts this phase produces
- `CodingAgentSession` idempotent shutdown API and stable shutdown outcome.
- Opaque runtime shutdown-request handle for immediate Phase A during moved-owner execution.
- Runtime lifecycle event internal/public variants and inventory row.
- Deterministic idle/active `shutdown` test family.
<verification>`cargo test -p pi-coding-agent event_contract --quiet && cargo test -p pi-coding-agent --lib interactive::event_bridge --quiet && cargo test -p pi-coding-agent --test event_boundary_guards --quiet && cargo test -p pi-coding-agent shutdown --quiet && cargo test -p pi-coding-agent --test public_api shutdown --quiet` passes; no task runs final-event assertions before that event and its publisher exist.</verification>
<success_criteria>Shutdown closes authority immediately, preserves admitted terminal publication, publishes one final event, then closes delivery.</success_criteria>
<output>Create `.planning/phases/09-lifecycle-association-guards-and-closure/09-05-SUMMARY.md` when done.</output>
