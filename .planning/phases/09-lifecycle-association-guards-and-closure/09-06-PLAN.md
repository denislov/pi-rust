---
phase: 09-lifecycle-association-guards-and-closure
plan: 06
type: execute
wave: 5
depends_on: [09-05]
files_modified:
  - crates/pi-coding-agent/src/protocol/types.rs
  - crates/pi-coding-agent/src/protocol/rpc.rs
  - crates/pi-coding-agent/src/protocol/rpc/state.rs
  - crates/pi-coding-agent/src/protocol/rpc/commands.rs
  - crates/pi-coding-agent/src/protocol/rpc/wire.rs
  - crates/pi-coding-agent/tests/rpc_mode.rs
  - crates/pi-coding-agent/tests/protocol_events.rs
autonomous: true
requirements: [CLIENT-04, COMPAT-03]
must_haves:
  truths:
    - "D-15: explicit RPC detach and every transport exit share one idempotent public detach cleanup path."
    - "D-16/D-18: lifecycle wire messages are additive; every prior RPC/JSON shape/code is unchanged and old handles are not auto-recovered."
  artifacts:
    - path: crates/pi-coding-agent/src/protocol/types.rs
      provides: additive typed detach/shutdown commands and status responses
    - path: crates/pi-coding-agent/src/protocol/rpc/state.rs
      provides: shared lifecycle cleanup path
  key_links:
    - from: explicit detach and RPC loop exit
      to: CodingAgentClientConnection::detach
      via: one RpcState method
      pattern: detach
    - from: RPC shutdown command
      to: CodingAgentRuntimeShutdownHandle::request_shutdown
      via: handle captured before session owner moves into CodingOperationTask
      pattern: request_shutdown
---
<objective>
Project lifecycle through RPC with one cleanup path and byte-compatible existing wire behavior.

Purpose: Make normal and abnormal client departure observable without adapter-local lifecycle truth.
Output: Additive commands/responses/events, EOF cleanup, stable rejection codes, and exact compatibility tests.
</objective>
<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/09-lifecycle-association-guards-and-closure/09-05-SUMMARY.md
@crates/pi-coding-agent/src/protocol/types.rs
@crates/pi-coding-agent/src/protocol/rpc.rs
@crates/pi-coding-agent/src/protocol/rpc/state.rs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add and freeze compile-ready additive lifecycle wire values</name>
  <files>crates/pi-coding-agent/src/protocol/types.rs, crates/pi-coding-agent/tests/protocol_events.rs</files>
  <read_first>crates/pi-coding-agent/tests/rpc_mode.rs, crates/pi-coding-agent/tests/protocol_events.rs, crates/pi-coding-agent/src/protocol/types.rs, crates/pi-coding-agent/src/protocol/rpc.rs</read_first>
  <behavior>
    - Dedicated detach/shutdown response/event status codes serialize independently.
    - Existing prompt/state/replay/control/JSON fields, omission rules, error codes, ordering, overflow, and PartialCommit payloads remain exact.
    - Standalone lifecycle payload/status values are compile-ready without extending exhaustive RPC command/response routers before Task 2.
  </behavior>
  <action>Add standalone lifecycle request/response/event payload and stable status value types plus exact `serde_json::Value` snapshots for D-15/D-16/D-18 in this task. Assert all existing prompt/state/replay/control/JSON fields, omission rules, error codes, ordering, overflow, and PartialCommit value snapshots remain exact and no lifecycle field is added to an old envelope. Do not add variants to the exhaustive `RpcCommand`/`RpcResponse` routers until Task 2 adds their match arms in the same task; this keeps the focused crate target compile-green.</action>
  <acceptance_criteria>Lifecycle wire values and all existing snapshots compile and pass immediately; no runtime cleanup assertion is introduced early.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent --test protocol_events lifecycle_wire --quiet</automated></verify>
  <done>RPC migration has a green additive-wire compatibility contract before command routing.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Route explicit and transport cleanup through public lifecycle</name>
  <files>crates/pi-coding-agent/src/protocol/types.rs, crates/pi-coding-agent/src/protocol/rpc.rs, crates/pi-coding-agent/src/protocol/rpc/state.rs, crates/pi-coding-agent/src/protocol/rpc/commands.rs, crates/pi-coding-agent/src/protocol/rpc/wire.rs, crates/pi-coding-agent/tests/rpc_mode.rs, crates/pi-coding-agent/tests/protocol_events.rs</files>
  <read_first>crates/pi-coding-agent/src/protocol/types.rs, crates/pi-coding-agent/src/protocol/rpc.rs, crates/pi-coding-agent/src/protocol/rpc/state.rs, crates/pi-coding-agent/src/protocol/rpc/commands.rs, crates/pi-coding-agent/src/protocol/rpc/wire.rs</read_first>
  <behavior>
    - Explicit detach returns typed lifecycle status and invokes the same cleanup function as EOF/transport/loop exit.
    - Shutdown is invoked only with restored runtime owner and yields its dedicated response/status.
    - While an operation owns the session, an RPC shutdown command invokes Phase A immediately through the pre-captured opaque shutdown-request handle; the response completes only after owner restoration and Phase B.
    - Lifecycle rejection maps to stable dedicated codes with no implicit recovery behavior.
  </behavior>
  <action>Add deterministic `rpc_mode` and protocol behavior cases in this task, extend the exhaustive `RpcCommand`/`RpcResponse` tagged enums and all router match arms with Task 1's lifecycle payloads, then route them until the cases pass. Replace `clear_client_state` drop-only behavior with one idempotent async/public detach path used by all loop exits and explicit command. Cover explicit detach, EOF, transport failure, and loop error as the same semantic cleanup without cancel/reconnect/retry/control retarget. Capture `CodingAgentRuntimeShutdownHandle` before moving the session into `CodingOperationTask`; when shutdown arrives during an admitted run, call `request_shutdown()` immediately so admission/control close in Phase A, record only the pending response correlation (not lifecycle truth), continue draining the admitted task, restore the unique session owner from `CodingOperationTaskResult`, then call `CodingAgentSession::shutdown()` for Phase B and send the final response. Do not defer Phase A until owner restoration, add lifecycle fields to old wire types, add a second dispatcher, or maintain adapter lifecycle flags.</action>
  <acceptance_criteria>All exit paths detach exactly once semantically, lifecycle commands are typed/additive, and full RPC parity tests pass.</acceptance_criteria>
  <verify><automated>cargo test -p pi-coding-agent --test rpc_mode --quiet && cargo test -p pi-coding-agent --test protocol_events --quiet && cargo test -p pi-coding-agent --lib protocol::rpc --quiet</automated></verify>
  <done>RPC is a thin lifecycle projection over the public connection/runtime owner.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| JSONL command/transport -> public lifecycle | Untrusted commands and disconnects request scoped detach/shutdown. |
| Typed lifecycle -> JSON response | Internal lifecycle state must map to stable codes only. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Spoofing / Elevation | RPC lifecycle command | high | mitigate | Connection-owned detach and owner-restored shutdown only. |
| T-09-02 | Denial of Service | transport cleanup | high | mitigate | Idempotent converged cleanup and active-work test. |
| T-09-04 | Information Disclosure | wire errors/status | high | mitigate | Dedicated stable codes and exact JSON snapshots. |
</threat_model>
## Artifacts this phase produces
- Additive RPC detach/shutdown command, response, status, and lifecycle-event wire variants.
- Shared explicit/EOF/transport cleanup method.
- `rpc_mode lifecycle` and protocol lifecycle fixtures.
<verification>Full `rpc_mode`, `protocol_events`, and RPC lib tests pass.</verification>
<success_criteria>RPC lifecycle is explicit and idempotent while all prior wire shapes remain byte/field compatible.</success_criteria>
<output>Create `.planning/phases/09-lifecycle-association-guards-and-closure/09-06-SUMMARY.md` when done.</output>
