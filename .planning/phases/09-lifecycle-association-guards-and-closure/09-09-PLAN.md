---
phase: 09-lifecycle-association-guards-and-closure
plan: 09
type: execute
wave: 7
depends_on: [09-08]
files_modified:
  - crates/pi-coding-agent/src/coding_session/public_projection.rs
  - crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs
  - crates/pi-coding-agent/src/coding_session/event_service.rs
  - crates/pi-coding-agent/tests/public_api.rs
autonomous: true
gap_closure: true
requirements: [COMPAT-03, CLIENT-04]
must_haves:
  truths:
    - "D-04: a reconnect receiver drains every event published before the authoritative shutdown boundary, then delivers Runtime.ShutDown, then closes."
    - "A slow receiver cannot lose an admitted terminal event merely because Phase B has transitioned the coordinator to ShutDown."
  artifacts:
    - path: crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs
      provides: authoritative shutdown sequence/drain boundary owned by SnapshotCoordinator
    - path: crates/pi-coding-agent/src/coding_session/public_projection.rs
      provides: reconnect delivery gate that accepts queued pre-boundary events and closes after shutdown delivery
    - path: crates/pi-coding-agent/tests/public_api.rs
      provides: deterministic no-sleep slow reconnect-consumer ordering test
  key_links:
    - from: CodingAgentSession::shutdown
      to: CodingAgentReconnectReceiver::ensure_delivery_live
      via: committed shutdown event sequence or equivalent coordinator drain boundary
      pattern: shutdown.*sequence|drain.*boundary
---
<objective>
Close the shutdown/reconnect delivery gap without changing detach ownership or ordinary lifecycle rejection semantics.

Purpose: Preserve D-04 and compatibility ordering for a receiver that is behind while Phase B completes.
Output: An authoritative coordinator-owned drain boundary and deterministic reconnect-receiver coverage proving terminal event, Runtime.ShutDown, then closure.
</objective>
<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-CONTEXT.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-VERIFICATION.md
@.planning/phases/09-lifecycle-association-guards-and-closure/09-REVIEW.md
@crates/pi-coding-agent/src/coding_session/public_projection.rs
@crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs
@crates/pi-coding-agent/src/coding_session/event_service.rs
</context>
<interfaces>
From `public_projection.rs`: `CodingAgentReconnectReceiver::recv`, `try_recv`, and private `ensure_delivery_live` validate each retained/broadcast `CodingAgentProductEvent` against connection lifecycle; events carry the sequence used by the receiver gate.

From `snapshot_coordinator.rs`: `SnapshotCoordinator` is the sole lifecycle authority; `request_shutdown`, `finish_shutdown`, and `validate_receiver` are coordinator-owned and must remain free of a mutex held across await.

From `mod.rs`: `CodingAgentSession::shutdown` waits for admitted work, emits `CodingAgentEvent::RuntimeShutDown`, then finishes shutdown. Preserve this two-phase owner boundary while exposing the committed boundary needed by reconnect delivery.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add an authoritative shutdown drain boundary</name>
  <files>crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/coding_session/public_projection.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/coding_session/mod.rs</read_first>
  <behavior>
    - The boundary records the exact event sequence (or equivalent committed cursor) for Runtime.ShutDown before lifecycle state becomes terminal.
    - Delivery validation permits events at or before that boundary, including admitted terminal events, while rejecting events published after the boundary and ordinary post-shutdown mutations.
    - The implementation retains one SnapshotCoordinator lifecycle authority and never holds the standard mutex across await.
  </behavior>
  <action>Extend the existing coordinator-owned lifecycle snapshot and EventService publication bookkeeping with a private authoritative shutdown drain marker. Set it atomically with the committed Runtime.ShutDown publication/transition in the existing Phase B path, then make reconnect delivery compare each queued event's sequence to that marker before applying the final lifecycle close rule. Keep the marker private, do not reopen admission, do not infer ordering from receiver timing or latest-history scans, and preserve detach/stale-generation rejection for events outside the shutdown boundary.</action>
  <verify><automated>cargo test -p pi-coding-agent --lib public_projection --quiet &amp;&amp; cargo test -p pi-coding-agent --test public_api shutdown --quiet</automated></verify>
  <done>Reconnect delivery has a coordinator-authoritative pre-shutdown boundary and no longer drops queued events published before Runtime.ShutDown.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove slow reconnect shutdown ordering and closure</name>
  <files>crates/pi-coding-agent/tests/public_api.rs</files>
  <read_first>crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/src/coding_session/public_projection.rs, crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/tests/support/mod.rs</read_first>
  <behavior>
    - A receiver is created before an admitted operation completes and is intentionally not polled while Phase B runs.
    - After shutdown completes, the receiver yields the admitted terminal event, then Runtime.ShutDown, then reports closure; no sleep or wall-clock race is used.
    - A post-boundary event or mutation remains rejected with typed RuntimeShutDown behavior.
  </behavior>
  <action>Add a deterministic channel/oneshot-gated public API fixture that uses the actual CodingAgentReconnectReceiver gate, holds the receiver slow while an admitted faux-provider operation publishes its terminal event, invokes owner shutdown, and only then drains the receiver. Assert exact event identities and ordering, then assert the next receive is closed. Keep the existing non-reconnect shutdown test and add a regression assertion that ordinary post-shutdown mutation still fails closed; use no sleeps, polling timeouts, synthetic event injection, or alternate product receivers.</action>
  <verify><automated>cargo test -p pi-coding-agent --test public_api shutdown_drains_slow_reconnect_terminal_event_before_runtime_shutdown_and_close --exact --quiet</automated></verify>
  <done>The verification gap is closed by a deterministic receiver-path test proving terminal event, Runtime.ShutDown, then closure.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| EventService -> reconnect receiver | A delayed consumer must receive only events committed before the shutdown boundary. |
| Client connection -> SnapshotCoordinator | A receiver cannot use the drain exception to regain mutation/control authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-02 | Denial of Service | shutdown drain | high | mitigate | Coordinator-owned boundary plus deterministic slow-consumer test. |
| T-09-03 | Tampering / Repudiation | terminal event ordering | high | mitigate | Sequence-bounded delivery and exact event identity assertions. |
| T-09-01 | Elevation of Privilege | reconnect lifecycle gate | high | mitigate | Boundary exception is receive-only; post-boundary mutation remains rejected. |
</threat_model>
<verification>Run the named slow reconnect test and existing public API shutdown suite; retain exact ordering evidence in the execution summary.</verification>
<success_criteria>A slow CodingAgentReconnectReceiver drains all queued pre-shutdown events, receives Runtime.ShutDown, and closes without weakening lifecycle guards.</success_criteria>
<output>Create `.planning/phases/09-lifecycle-association-guards-and-closure/09-09-SUMMARY.md` when done.</output>
