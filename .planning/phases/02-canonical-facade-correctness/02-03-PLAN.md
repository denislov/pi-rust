---
phase: 02-canonical-facade-correctness
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - crates/pi-coding-agent/src/coding_session/mod.rs
  - crates/pi-coding-agent/src/coding_session/session_service.rs
  - crates/pi-coding-agent/src/coding_session/delegation_confirmation_service.rs
  - .planning/phases/02-canonical-facade-correctness/02-VALIDATION.md
autonomous: true
requirements:
  - FACADE-01
  - FACADE-02
  - FACADE-03
  - FACADE-04
  - FACADE-05
must_haves:
  truths:
    - "Per D-11, canonical run evidence covers fork, active-leaf switch, branch-summary reuse, plugin load and command, default-profile mutation, and delegation approval and rejection."
    - "Per D-12, each operation applies the relevant outcome/state/error/reopen-replay/event-sequence/PartialCommit checklist rather than compile-only evidence."
    - "Per D-13, navigation/tree, summary reuse, plugin effects, profile state, and delegation queue/decision assertions remain operation-specific and visible."
    - "Per D-14, all new canonical correctness evidence invokes CodingAgentSession::run directly and reuses deterministic fixtures without treating compatibility methods as oracles."
    - "Per D-15, persistence-sensitive cases distinguish pre-append no-mutation, post-append PartialCommit, and reopen/replay authority using existing test-only failure controls."
    - "Per D-16, tests stop at existing append/manifest/publication boundaries and do not become crash-consistency or atomic-filesystem redesign work."
  artifacts:
    - path: crates/pi-coding-agent/src/coding_session/mod.rs
      provides: "Focused canonical FACADE-05 behavior and failure-boundary evidence at the runtime owner layer."
    - path: .planning/phases/02-canonical-facade-correctness/02-VALIDATION.md
      provides: "Final task mapping, measured feedback latency, Nyquist status, and phase verification record."
  key_links:
    - from: CodingAgentSession::run navigation operations
      to: session log replay and EventService
      via: "Outcome, immediate owner state, durable facts, reopened state, and monotonically sequenced semantic events agree."
    - from: plugin/profile/delegation public operations
      to: plugin registry, profile registry/session facts, and pending delegation queue
      via: "Each public result is checked against its operation-specific state effect and unchanged error semantics."
    - from: deterministic store failure controls
      to: CodingSessionError::PartialCommit and replay
      via: "Failure timing determines whether durable mutation is absent or authoritative after reopen."
---

<objective>
Prove the high-risk canonical operations preserve the durable, event, and error contract that later adapters will rely on.

Purpose: Satisfy FACADE-05 and close the Phase 2 verification gate using D-11 through D-16, while rechecking FACADE-01 through FACADE-04 after all changes are combined.
Output: Focused owner tests for navigation/summary and plugin/profile/delegation behavior, deterministic partial-commit/replay evidence, and a fully reconciled validation record.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-canonical-facade-correctness/02-CONTEXT.md
@.planning/phases/02-canonical-facade-correctness/02-RESEARCH.md
@.planning/phases/02-canonical-facade-correctness/02-PATTERNS.md
@.planning/phases/02-canonical-facade-correctness/02-VALIDATION.md
@.planning/phases/01-evidence-based-baseline/01-AUDIT.md
@.planning/phases/02-canonical-facade-correctness/02-01-SUMMARY.md
@.planning/phases/02-canonical-facade-correctness/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete canonical navigation and branch-summary durability evidence</name>
  <files>crates/pi-coding-agent/src/coding_session/mod.rs</files>
  <read_first>
    - crates/pi-coding-agent/src/coding_session/mod.rs: `canonical_run_switches_active_leaf`, `canonical_run_forks_current_session`, `canonical_fork_preserves_owner_runtime_and_event_stream`, `canonical_switch_reports_partial_commit_after_durable_leaf_change`, and `canonical_run_reuses_branch_summary_when_requested`
    - crates/pi-coding-agent/src/coding_session/session_service.rs: fork/switch/branch-summary durable ownership
    - crates/pi-coding-agent/src/coding_session/session_log/transaction.rs: append-before-manifest ordering, InDoubt, and PartialCommit
    - crates/pi-coding-agent/src/coding_session/session_log/replay.rs: durable-log authority on reopen
    - .planning/phases/02-canonical-facade-correctness/02-PATTERNS.md: canonical mutation, partial-commit, and branch-summary analogs
  </read_first>
  <behavior>
    - "Fork returns SessionForked, changes owner/session-tree identity as intended, preserves owner runtime and subscriber continuity, and keeps product-event sequences monotonic."
    - "Switch returns ActiveLeafSwitched, updates the selected active leaf, persists/replays it, and reports the same typed errors for invalid targets."
    - "BranchSummary with ReuseExisting returns the prior summary without duplicate durable facts or unexpected semantic events, and reopen/replay yields the same summary authority."
    - "A pre-append injected failure leaves no durable navigation mutation; a post-append manifest/publication failure returns PartialCommit with an operation id and replay sees the durable fact."
  </behavior>
  <action>
Extend/consolidate the existing canonical navigation and branch-summary tests so every path starts with public `CodingAgentOperation` and `CodingAgentSession::run` per D-14. Apply the shared D-12 checklist only where relevant: exact public outcome, immediate state, stable typed error/code/message, semantic events and strictly increasing sequence numbers, durable facts, reopen/replay equivalence, and explicit `PartialCommit`. Keep operation-specific assertions visible per D-13: forked tree/session identity, selected active leaf, and no duplicate branch-summary record/event on reuse. Reuse existing test-only store/failure controls to add the D-15 pre-append/no-mutation and post-append/partial-commit/replay cases; do not add production hooks. Limit the cases to current transaction boundaries per D-16 and do not test torn JSONL, interrupted replacement, power loss, or atomic filesystem redesign.
  </action>
  <verify>
    <automated>cargo test -p pi-coding-agent coding_session::tests::canonical_run_preserves_navigation_and_branch_summary_durability -- --exact --nocapture && cargo test -p pi-coding-agent coding_session::tests::canonical_durable_mutations_distinguish_no_commit_partial_commit_and_replay -- --exact --nocapture && cargo fmt --check</automated>
  </verify>
  <done>Fork, switch, and summary reuse have canonical outcome/state/event/replay evidence, and failure timing proves no mutation versus explicit PartialCommit without new production instrumentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete canonical plugin, profile, and delegation contract evidence</name>
  <files>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/session_service.rs, crates/pi-coding-agent/src/coding_session/delegation_confirmation_service.rs</files>
  <read_first>
    - crates/pi-coding-agent/src/coding_session/mod.rs: plugin load/command tests, default-profile methods/tests, delegation metadata/admission tests, queue helpers, and run dispatch branches
    - crates/pi-coding-agent/src/coding_session/plugin_load_flow.rs: deterministic plugin load outcome and diagnostics
    - crates/pi-coding-agent/src/coding_session/plugin_service.rs: capability-aware command execution and public output projection
    - crates/pi-coding-agent/src/coding_session/profiles.rs: default profile registry/state behavior
    - crates/pi-coding-agent/src/coding_session/delegation_confirmation_service.rs: approve/reject queue mutation, durable decision transaction, and current error mapping
    - crates/pi-coding-agent/src/coding_session/session_service.rs: delegation decision append then manifest update ownership and PartialCommit conversion
    - crates/pi-coding-agent/src/coding_session/session_log/transaction.rs: pre-append failure, append-before-manifest ordering, operation identifiers, InDoubt, and replay authority
    - crates/pi-coding-agent/tests/agent_profile_session.rs: tempfile reopen/profile assertion analog
    - crates/pi-coding-agent/tests/delegation_execution.rs: deterministic pending queue and decision assertion analog
  </read_first>
  <behavior>
    - "PluginLoad returns the curated public load outcome and updates the runtime capability/plugin registry; PluginCommand returns its public string output or the unchanged typed error and releases the operation guard."
    - "SetDefaultAgentProfile returns DefaultAgentProfileChanged, updates snapshot/registry state, and reopens with the same persistent default when persistence applies."
    - "ApproveDelegation retains dynamic target resolution and Async admission, returns DelegationApproved, and applies the expected pending-queue/team decision effect."
    - "RejectDelegation retains SyncMutable control admission, returns DelegationRejected, records the reason/decision effect, and removes or resolves the pending confirmation as before."
    - "ApproveDelegation and RejectDelegation each prove pre-append failure leaves queue, durable log, manifest, and reopened state unchanged; post-append manifest failure returns CodingSessionError::PartialCommit with the same non-empty operation_id carried by the durable decision transaction, and replay/reopen treats the appended decision as authoritative."
    - "Semantic product events are asserted only where the existing operation contract emits them, with monotonic sequence continuity; non-durable plugin command effects are not mislabeled as persisted session facts."
  </behavior>
  <action>
Add a focused owner test group covering all plugin/profile/delegation operations named by D-11 through the public canonical facade. Use the following planner-owned applicability ledger as the binding D-12/D-15 contract; implement it in test structure/comments or assertions without creating a production API:

| Operation | Durable fact | Semantic events | Reopen/replay | Pre-append no-change | Post-append `PartialCommit` + `operation_id` |
|---|---|---|---|---|---|
| ForkSession | yes | yes | yes | yes | yes |
| SwitchActiveLeaf | yes | yes | yes | yes | yes |
| BranchSummary (`ReuseExisting`) | existing summary only; no new append | only if current contract emits | yes | not applicable to reuse/no-append path | not applicable to reuse/no-append path |
| PluginLoad | runtime/plugin registry; assert persistence only if current typed log records it | applicable load diagnostics/events | reopen only if a durable plugin fact exists | only if durable transaction exists | only if durable transaction exists |
| PluginCommand | no session durable fact | command-contract dependent | no | not applicable | not applicable |
| SetDefaultAgentProfile | yes | contract dependent | yes | yes | yes where append precedes manifest update |
| ApproveDelegation | yes, delegation decision | yes where current contract emits | yes | required | required |
| RejectDelegation | yes, delegation decision | yes where current contract emits | yes | required | required |

Reuse existing plugin files, profile roots, faux providers, pending-delegation fixtures, serialized guards, and existing test-only store failure controls per D-14. For both approval and rejection, add deterministic pre-append and post-append cases: snapshot queue/log/manifest before injection; invoke `CodingAgentSession::run`; assert pre-append error leaves immediate and reopened state unchanged; assert post-append returns `CodingSessionError::PartialCommit`, `error.code() == "partial_commit"`, a non-empty `operation_id` matching the durable transaction, and replay/reopen contains the approved or rejected decision and resolved pending state. Preserve approval's dynamic target/Async admission and rejection's static SyncMutable admission. Modify `session_service.rs` or `delegation_confirmation_service.rs` only if the new failing tests prove current error mapping loses PartialCommit, operation_id, or replay authority; make the smallest owning-boundary correction and do not redesign the transaction. Where plugin command is runtime-only, assert capability-aware execution and stable error/guard-release behavior without inventing persistence. Do not broadly migrate Phase 4 suites, compare against compatibility methods, redesign events, or add production failure controls.
  </action>
  <verify>
    <automated>cargo test -p pi-coding-agent coding_session::tests::canonical_run_preserves_plugin_profile_and_delegation_contracts -- --exact --nocapture && cargo test -p pi-coding-agent coding_session::tests::canonical_delegation_decisions_distinguish_no_commit_partial_commit_and_replay -- --exact --nocapture && cargo fmt --check</automated>
  </verify>
  <done>All plugin, profile, approval, and rejection operations have direct canonical outcome plus applicable state/error/event/reopen evidence using deterministic existing fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile Nyquist evidence and run the Phase 2 closure gate</name>
  <files>.planning/phases/02-canonical-facade-correctness/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-canonical-facade-correctness/02-VALIDATION.md: final task map, Wave 0 gaps, sampling rate, and sign-off fields
    - .planning/phases/02-canonical-facade-correctness/02-01-SUMMARY.md: facade/privacy test names and results
    - .planning/phases/02-canonical-facade-correctness/02-02-SUMMARY.md: contract/projection/dispatcher test names and results
    - .planning/PROJECT.md: mandatory formatting, focused crate, workspace, source audit, and diff checks
  </read_first>
  <action>
Update the already reconciled seven-row task map with actual pass/fail status and measured runtimes from execution. Mark Wave 0 items complete only when their named tests exist and pass; set `nyquist_compliant: true`, `wave_0_complete: true`, status complete, and Approval approved only after every focused, crate, workspace, source-audit, and diff command below succeeds. Preserve exact task IDs `02-01-01`, `02-01-02`, `02-02-01`, `02-02-02`, `02-03-01`, `02-03-02`, and `02-03-03`. Run source audits that confirm the 15 public variants remain explicit, production adapter files were not migrated in this phase, no internal contract was added to `api`, and no production failure hook or new compatibility facade appeared. Do not weaken assertions or record a zero-test Cargo command as evidence.
  </action>
  <verify>
    <automated>cargo fmt --check && cargo test -p pi-coding-agent && cargo check -p pi-coding-agent && cargo test --workspace && cargo check --workspace && cargo test -p pi-coding-agent --test public_api --test api_boundary_guards -- --nocapture && test "$(sed -n '/pub enum CodingAgentOperation {/,/^}/p' crates/pi-coding-agent/src/coding_session/public_operation.rs | rg -c '^    [A-Z]')" -eq 15 && sh -c 'for path in crates/pi-coding-agent/src/protocol crates/pi-coding-agent/src/print_mode.rs crates/pi-coding-agent/src/interactive crates/pi-coding-agent/src/lib.rs crates/pi-coding-agent/src/coding_session crates/pi-coding-agent/src/coding_session/session_log/store.rs; do test -e "$path" || exit 2; done; rg -n "CodingAgentOperation::" crates/pi-coding-agent/src/protocol crates/pi-coding-agent/src/print_mode.rs crates/pi-coding-agent/src/interactive; status=$?; test "$status" -eq 1' && rg -n '#\[cfg\(test\)\]' crates/pi-coding-agent/src/coding_session/session_log/store.rs && sh -c 'git diff -U0 -- crates/pi-coding-agent/src/coding_session/session_service.rs crates/pi-coding-agent/src/coding_session/delegation_confirmation_service.rs | rg -n "^\\+.*(fail_if_injected|StoreFailurePoint|inject.*failure)"; status=$?; test "$status" -eq 1' && sh -c 'git diff -U0 -- crates/pi-coding-agent/src/lib.rs crates/pi-coding-agent/src/coding_session | rg --pcre2 -n "^\\+\\s*pub\\s+mod\\s+(?!api\\b)|^\\+.*(compatibility facade|compat facade)"; status=$?; test "$status" -eq 1' && git diff --check</automated>
  </verify>
  <done>All seven validation rows are green with measured runtimes, every FACADE requirement maps to passing evidence, Nyquist sign-off is complete, and all crate/workspace/source/diff gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical operation -> persisted session mutation | A typed operation may append durable facts, update the manifest, publish events, and replace owner state. |
| Durable log -> reopened owner state | Replay is authoritative when manifest/publication fails after append. |
| Runtime/plugin/profile/delegation services -> public outcome/events | Internal capability and queue state must be projected without leaking private contracts or changing semantics. |
| Test failure injection -> shared filesystem/global process state | Deterministic faults and environment/provider guards must remain test-only and isolated from concurrent tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering / Repudiation | navigation/profile/delegation persistence | high | mitigate | Assert outcome, durable facts, typed errors, explicit PartialCommit, and reopen/replay authority through canonical run. |
| T-02-06 | Denial of Service / Tampering | failure injection and global fixture state | high | mitigate | Reuse existing serialized test guards and owner-only fault controls; add no production hook or unsynchronized global mutation. |
| T-02-07 | Information Disclosure / Elevation of Privilege | plugin capability and service projection | high | mitigate | Execute commands through capability-aware PluginService and assert only curated public outcomes/diagnostics. |
| T-02-08 | Repudiation | ProductEvent sequence continuity | medium | mitigate | Assert semantic events and strict sequence monotonicity across owner replacement and applicable mutations. |
</threat_model>

<verification>
Wave 2 begins only after both Wave 1 plans pass `cargo test -p pi-coding-agent` and `cargo check -p pi-coding-agent`. The final task runs formatting, focused facade/boundary tests, the complete crate suite/check, the complete workspace suite/check, source audits recorded in validation, and `git diff --check`.
</verification>

<acceptance_criteria>
- Every FACADE-05 operation named in D-11 has direct canonical `run()` evidence.
- Outcome, operation-specific state, unchanged typed errors, applicable event sequence, persistence/replay, and PartialCommit assertions satisfy D-12 and D-13.
- Persistence-sensitive failure tests distinguish pre-append no mutation from post-append PartialCommit and replay authority per D-15.
- No production test hook, compatibility facade, adapter migration, broad test migration, compatibility deletion, event redesign, or crash-consistency expansion is introduced per D-14 and D-16.
- All focused, crate, workspace, formatting, source-audit, and diff gates pass; `02-VALIDATION.md` is fully signed off.
</acceptance_criteria>

<success_criteria>
FACADE-01 through FACADE-05 are jointly verified from the final tree, and later adapter migration can rely on the stable facade without reopening dispatch, projection, privacy, durability, event-continuity, or PartialCommit questions.
</success_criteria>

## Artifacts this phase produces

- Test `canonical_run_preserves_navigation_and_branch_summary_durability` in `coding_session/mod.rs`.
- Test `canonical_durable_mutations_distinguish_no_commit_partial_commit_and_replay` in `coding_session/mod.rs`.
- Test `canonical_run_preserves_plugin_profile_and_delegation_contracts` in `coding_session/mod.rs`.
- Updated `.planning/phases/02-canonical-facade-correctness/02-VALIDATION.md` with seven final task IDs, measured runtimes, green evidence, and Nyquist approval.
- No production instrumentation, external dependency, schema migration, adapter change, compatibility deletion, or new public internal-runtime symbol.

<output>
Create `.planning/phases/02-canonical-facade-correctness/02-03-SUMMARY.md` when done.
</output>
