---
phase: 07-adapter-migration-and-compatibility-deletion
plan: 05
type: execute
wave: 4
depends_on: [07-04]
files_modified:
  - crates/pi-coding-agent/src/coding_session/event.rs
  - crates/pi-coding-agent/src/coding_session/public_event.rs
  - crates/pi-coding-agent/src/coding_session/event_service.rs
  - crates/pi-coding-agent/src/coding_session/mod.rs
  - crates/pi-coding-agent/tests/event_boundary_guards.rs
  - docs/product-event-contract.md
autonomous: true
requirements: [COMPAT-01, COMPAT-02]
must_haves:
  truths:
    - "Production code and first-party consumers contain no compatibility event accessor or local suppression for the deleted path."
    - "ProductEvent stores only typed payload and independent metadata; raw compatibility storage/conversion helpers are absent or narrowly cfg(test)-gated with documented evidence."
    - "RPC, JSON/print, interactive, replay, overflow, durability, control, recovery, PartialCommit, and the five terminal associations remain behavior-compatible."
  artifacts:
    - path: "crates/pi-coding-agent/src/coding_session/event.rs"
      provides: "Compatibility-free private typed ProductEvent envelope"
    - path: "crates/pi-coding-agent/tests/event_boundary_guards.rs"
      provides: "Final fail-closed compatibility deletion audit"
    - path: "docs/product-event-contract.md"
      provides: "Post-migration contract without obsolete transitional-storage claims"
  key_links:
    - from: "crates/pi-coding-agent/src/coding_session/event_service.rs"
      to: "crates/pi-coding-agent/src/coding_session/event.rs"
      via: "construct, sequence, retain, and broadcast typed ProductEvent only"
      pattern: "ProductEvent"
    - from: "crates/pi-coding-agent/tests/event_boundary_guards.rs"
      to: "crates/pi-coding-agent/src"
      via: "recursive production source audit for deleted compatibility symbols/suppressions"
      pattern: "compatibility"
---

<objective>
Remove the last raw compatibility storage/conversion path, close source guards and documentation, and run the complete Phase 7 verification gate.

Purpose: Finish COMPAT-01/02 with behavior evidence rather than leaving a hidden raw clone or deprecation shim.
Output: Compatibility-free product-event runtime, final guard evidence, updated contract, and full workspace verification.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-adapter-migration-and-compatibility-deletion/07-RESEARCH.md
@.planning/phases/07-adapter-migration-and-compatibility-deletion/07-VALIDATION.md
@.planning/phases/07-adapter-migration-and-compatibility-deletion/07-04-SUMMARY.md
@.planning/phases/06-product-event-inventory-and-typed-contract/06-VERIFICATION.md
@docs/product-event-contract.md
</context>

<scope_exclusions>
- Do not remove transitional serialized `family`/`kind` fields unless existing Phase 6/adapter output assertions explicitly prove removal is compatible; compatibility storage deletion does not authorize wire-contract deletion.
- Do not change `ProductEventReceiver`, public client lifecycle/reconnect/scoped-control APIs, or add terminal associations.
- Do not modify lower crates, durable session formats, external dependencies, ORM/schema files, or unrelated security/reliability work.
</scope_exclusions>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- New public symbols/files: None.
- Deleted private symbols: raw compatibility field/accessor and obsolete raw-to-classification helpers that have no cfg(test) evidence.
- Updated file: `docs/product-event-contract.md` records typed-only internal projection and retained transitional wire fields.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove raw compatibility storage and migrate final white-box fixtures</name>
  <read_first>
    - crates/pi-coding-agent/src/coding_session/event.rs
    - crates/pi-coding-agent/src/coding_session/public_event.rs
    - crates/pi-coding-agent/src/coding_session/event_service.rs
    - crates/pi-coding-agent/src/coding_session/mod.rs
  </read_first>
  <files>crates/pi-coding-agent/src/coding_session/event.rs, crates/pi-coding-agent/src/coding_session/public_event.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/coding_session/mod.rs</files>
  <behavior>
    - ProductEvent contains the typed payload and metadata without a stored raw event clone.
    - White-box event/session tests assert typed fields while preserving all sequence, replay, durability, recovery, delegation, and terminal behavior.
    - Any raw constructor retained solely to emit deterministic events is cfg(test), named for tests, and does not leak through the stable facade.
  </behavior>
  <action>Delete the raw compatibility field/accessor and obsolete helpers from `ProductEvent`, then migrate remaining event-service/session white-box assertions to the owned typed payload. Keep the raw input accepted by `EventService::emit` only where it remains the internal emitter boundary; convert exactly once into the typed envelope and do not store or rebroadcast the raw value. If a deterministic raw fixture helper is genuinely required, gate it under `cfg(test)`, name it explicitly for tests, and document which behavior assertion requires it. Preserve EventService publication order, sequence/replay state, durable metadata, PartialCommit operation IDs, and all five terminal associations.</action>
  <verify>
    <automated>cargo test -p pi-coding-agent --lib coding_session::event --quiet &amp;&amp; cargo test -p pi-coding-agent --lib coding_session::event_service::tests --quiet &amp;&amp; cargo test -p pi-coding-agent --lib coding_session::tests --quiet</automated>
  </verify>
  <acceptance_criteria>No runtime ProductEvent stores or exposes a raw compatibility value, remaining white-box fixtures are typed or explicitly test-gated, and focused event/session behavior passes.</acceptance_criteria>
  <done>No runtime ProductEvent stores or exposes a raw compatibility value, remaining white-box fixtures are typed or explicitly test-gated, and focused event/session behavior passes.</done>
</task>

<task type="auto">
  <name>Task 2: Close deletion guards, contract documentation, and phase gates</name>
  <read_first>
    - crates/pi-coding-agent/tests/event_boundary_guards.rs
    - docs/product-event-contract.md
    - .planning/phases/07-adapter-migration-and-compatibility-deletion/07-VALIDATION.md
    - crates/pi-coding-agent/tests/product_event_contract.rs
  </read_first>
  <files>crates/pi-coding-agent/tests/event_boundary_guards.rs, docs/product-event-contract.md</files>
  <action>Finalize recursive source guards so production protocol, interactive, coding-session, and facade roots reject the deleted compatibility accessor/storage/receiver/broadcast and local suppressions tied to them, while avoiding false positives from guard literals and unrelated documented deprecations. Update the product-event contract to describe typed-only runtime projection and accurately retain any transitional wire identity fields. Run focused adapter/contract/public API suites first, then the crate suite and required workspace-wide gates. Do not weaken or delete the 45-row inventory guard.</action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo test -p pi-coding-agent --test event_boundary_guards --test product_event_contract --test protocol_events --test interactive_event_bridge --test public_api --quiet &amp;&amp; cargo test --workspace --quiet &amp;&amp; cargo check --workspace &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>All focused and workspace gates pass; source audits find no production compatibility path/suppression; documentation, executable 45-event inventory, and observed adapter behavior agree.</acceptance_criteria>
  <done>All focused and workspace gates pass; source audits find no production compatibility path/suppression; documentation, executable 45-event inventory, and observed adapter behavior agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal emitters -> typed EventService stream | Untrusted model/tool/session payloads are converted exactly once and retained for subscribers. |
| typed runtime -> external adapters/facade | Serialization and UI/RPC projection must not expose private raw types or reinterpret payload text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Tampering | final raw-to-typed construction | high | mitigate | Keep exhaustive 45-event conversion, payload assertions, and source guards; remove hidden raw clones and string parsing. |
| T-07-14 | Information Disclosure | public facade and adapters | high | mitigate | Guard against raw receiver/event/storage exports and run external public API plus protocol serialization tests. |
| T-07-15 | Denial of Service | replay/overflow during deletion | high | mitigate | Preserve retained buffer/channel behavior and verify crate/workspace replay and RPC overflow suites before completion. |
| T-07-16 | Repudiation | durability/PartialCommit metadata | high | mitigate | Retain exact operation ID, durability, recovery, and session assertions throughout white-box migration. |
</threat_model>

<verification>
- `cargo fmt --check`
- `cargo test -p pi-coding-agent --test event_boundary_guards --test product_event_contract --test protocol_events --test interactive_event_bridge --test public_api --quiet`
- `cargo test -p pi-coding-agent --quiet`
- `cargo test --workspace --quiet`
- `cargo check --workspace`
- `git diff --check`
</verification>

<success_criteria>
- RPC, interactive, JSON/print, and first-party tests directly consume typed product events.
- Production code has no compatibility accessor consumer, receiver/subscription/storage path, or local suppression for that path.
- Observable adapter output, ordering, replay, overflow recovery, durability, control, navigation, PartialCommit, and five root-terminal associations remain green.
- No public compatibility type leaks through `pi_coding_agent::api`, and no new dependency or schema artifact exists.
</success_criteria>

<output>
Create `.planning/phases/07-adapter-migration-and-compatibility-deletion/07-05-SUMMARY.md` when done.
</output>
