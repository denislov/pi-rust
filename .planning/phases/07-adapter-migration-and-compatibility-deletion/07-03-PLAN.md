---
phase: 07-adapter-migration-and-compatibility-deletion
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - crates/pi-coding-agent/src/interactive/event_bridge.rs
  - crates/pi-coding-agent/src/interactive/loop.rs
  - crates/pi-coding-agent/tests/interactive_event_bridge.rs
autonomous: true
requirements: [COMPAT-01]
must_haves:
  truths:
    - "Interactive projection consumes typed product-event payloads directly and emits the same UiEvent/transcript state."
    - "Usage, tool, delegation, compaction, self-healing, recovery, profile, navigation, and PartialCommit assertions retain exact identifiers and text."
    - "Event-terminal variants that previously projected no UI remain no-ops and do not acquire root-terminal meaning."
  artifacts:
    - path: "crates/pi-coding-agent/src/interactive/event_bridge.rs"
      provides: "Typed product-event to UiEvent projection"
    - path: "crates/pi-coding-agent/src/interactive/loop.rs"
      provides: "Typed recovery, navigation, profile, and PartialCommit assertions"
    - path: "crates/pi-coding-agent/tests/interactive_event_bridge.rs"
      provides: "Offline UI payload and delegation lifecycle regression evidence"
  key_links:
    - from: "crates/pi-coding-agent/src/interactive/loop.rs"
      to: "crates/pi-coding-agent/src/interactive/event_bridge.rs"
      via: "ProductEvent passes through UiProjection without raw conversion"
      pattern: "push_product_event"
---

<objective>
Migrate the interactive UI bridge and controller assertions to typed product events while preserving every visible projection and recovery invariant.

Purpose: Satisfy the human-facing half of COMPAT-01 independently from protocol migration.
Output: Typed UI projection and deterministic behavior tests.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-adapter-migration-and-compatibility-deletion/07-RESEARCH.md
@.planning/phases/07-adapter-migration-and-compatibility-deletion/07-PATTERNS.md
@.planning/phases/07-adapter-migration-and-compatibility-deletion/07-01-SUMMARY.md
@docs/product-event-contract.md
</context>

<scope_exclusions>
- Do not redesign the TUI, transcript model, client lifecycle, cursor protocol, or navigation ownership.
- Do not alter event ordering, recovery marker publication, or control multiplexing.
- Do not add terminal associations for delegation/message/tool/session-write events.
</scope_exclusions>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- New public symbols/files: None.
- New private symbols: None required; the existing bridge entry point is rewritten against typed payloads.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite CodingEventBridge for typed family payloads</name>
  <read_first>
    - crates/pi-coding-agent/src/interactive/event_bridge.rs
    - crates/pi-coding-agent/src/coding_session/public_event.rs
    - crates/pi-coding-agent/tests/interactive_event_bridge.rs
  </read_first>
  <files>crates/pi-coding-agent/src/interactive/event_bridge.rs, crates/pi-coding-agent/tests/interactive_event_bridge.rs</files>
  <behavior>
    - Usage/context-token deltas, tool argument parsing, delegation blocks, compaction resets, self-healing notices, and recovery notices remain identical.
    - Delegation confirmation and lifecycle projections retain operation/tool-call/child IDs, target, task, status, and summary.
    - Existing no-op typed variants emit no UiEvent.
  </behavior>
  <action>Replace `CodingEventBridge`'s raw handler with exhaustive typed family/payload matching through `ProductEvent::event()`. Preserve each existing `UiEvent` shape, accumulation rule, formatting rule, and no-op arm. Update deterministic bridge fixtures to typed payload construction and retain exact field assertions; do not reduce them to event counts.</action>
  <verify>
    <automated>cargo test -p pi-coding-agent --test interactive_event_bridge --quiet</automated>
  </verify>
  <acceptance_criteria>The bridge has no compatibility accessor consumer, and all usage/tool/delegation/compaction/error/recovery projections pass with typed payload assertions.</acceptance_criteria>
  <done>The bridge has no compatibility accessor consumer, and all usage/tool/delegation/compaction/error/recovery projections pass with typed payload assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate interactive loop compatibility assertions</name>
  <read_first>
    - crates/pi-coding-agent/src/interactive/loop.rs
    - crates/pi-coding-agent/src/interactive/event_bridge.rs
    - crates/pi-coding-agent/src/coding_session/error.rs
  </read_first>
  <files>crates/pi-coding-agent/src/interactive/loop.rs</files>
  <behavior>
    - PartialCommit keeps the exact operation ID and message through task completion and UI projection.
    - Failed fork continuity does not publish a replacement SessionOpened event.
    - Profile changes, startup recovery, snapshot reset, and projection cursor behavior match typed variants directly.
  </behavior>
  <action>Rewrite co-located loop tests and any production assertion/projection seam that reads the compatibility accessor to match `ProductEvent::event()` typed variants. Preserve the original receiver used for failed-fork continuity, the session/profile IDs, recovery marker semantics, snapshot cursor reset, and structured PartialCommit attribution. Do not resubscribe to make a continuity test pass.</action>
  <verify>
    <automated>cargo test -p pi-coding-agent --lib interactive::r#loop::tests --quiet</automated>
  </verify>
  <acceptance_criteria>Interactive loop tests use typed payloads and continue proving exact PartialCommit, fork continuity, recovery, profile, navigation, and cursor behavior.</acceptance_criteria>
  <done>Interactive loop tests use typed payloads and continue proving exact PartialCommit, fork continuity, recovery, profile, navigation, and cursor behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model/tool event -> terminal UI | Untrusted text, arguments, summaries, and errors become transcript/UI state. |
| session recovery/navigation -> UI projection | Durable identifiers and recovery facts affect the visible active-session state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Tampering | `CodingEventBridge` | high | mitigate | Preserve typed field-by-field projection and exact UI payload tests; do not parse identity strings or reinterpret content as commands. |
| T-07-08 | Repudiation | PartialCommit/recovery projection | high | mitigate | Retain exact operation and recovery IDs in typed assertions so durable ambiguity remains attributable. |
| T-07-09 | Spoofing | session/profile navigation | medium | mitigate | Keep EventService/session-owned identifiers and the original receiver continuity test; adapters never synthesize ownership transitions. |
</threat_model>

<verification>
- `cargo test -p pi-coding-agent --test interactive_event_bridge --quiet`
- `cargo test -p pi-coding-agent --lib interactive::r#loop::tests --quiet`
- `cargo test -p pi-coding-agent --test interactive_sessions --quiet`
</verification>

<success_criteria>
- Interactive production code has no compatibility accessor consumer.
- Existing visible UI/transcript payloads and no-op behavior are unchanged.
- PartialCommit, recovery, navigation, and cursor assertions remain deterministic and green.
</success_criteria>

<output>
Create `.planning/phases/07-adapter-migration-and-compatibility-deletion/07-03-SUMMARY.md` when done.
</output>
