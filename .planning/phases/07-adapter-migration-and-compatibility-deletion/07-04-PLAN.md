---
phase: 07-adapter-migration-and-compatibility-deletion
plan: 04
type: execute
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - crates/pi-coding-agent/src/coding_session/mod.rs
  - crates/pi-coding-agent/src/coding_session/event_service.rs
  - crates/pi-coding-agent/src/lib.rs
  - crates/pi-coding-agent/tests/public_api.rs
  - crates/pi-coding-agent/tests/event_boundary_guards.rs
autonomous: true
requirements: [COMPAT-01, COMPAT-02]
must_haves:
  truths:
    - "All first-party tests receive typed product events while preserving startup recovery, replay, durability, control, navigation, and operation assertions."
    - "The stable facade exposes the typed public receiver but no legacy CodingAgentEventReceiver or CodingAgentSession::subscribe method."
    - "EventService retains one product-event broadcast, sequence assignment, retained replay, and lag behavior with no duplicate compatibility sender."
  artifacts:
    - path: "crates/pi-coding-agent/src/coding_session/event_service.rs"
      provides: "Single ProductEventReceiver transport with retained replay"
    - path: "crates/pi-coding-agent/tests/public_api.rs"
      provides: "Facade closure and typed receiver behavior evidence"
    - path: "crates/pi-coding-agent/tests/event_boundary_guards.rs"
      provides: "Fail-closed first-party compatibility receiver/source audit"
  key_links:
    - from: "crates/pi-coding-agent/src/coding_session/mod.rs"
      to: "crates/pi-coding-agent/src/coding_session/event_service.rs"
      via: "typed internal/public subscription methods only"
      pattern: "subscribe_product_events"
    - from: "crates/pi-coding-agent/src/lib.rs"
      to: "crates/pi-coding-agent/tests/public_api.rs"
      via: "curated api facade exports typed receiver contracts"
      pattern: "CodingAgentProductEventReceiver"
---

<objective>
Migrate all remaining first-party receiver tests, delete the legacy session/EventService subscription path and duplicate broadcast, and close the public facade around typed events.

Purpose: Complete receiver-side COMPAT-02 after both production adapter families are typed.
Output: One retained typed event transport, migrated behavior tests, and a fail-closed receiver guard.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-adapter-migration-and-compatibility-deletion/07-RESEARCH.md
@.planning/phases/07-adapter-migration-and-compatibility-deletion/07-VALIDATION.md
@.planning/phases/07-adapter-migration-and-compatibility-deletion/07-02-SUMMARY.md
@.planning/phases/07-adapter-migration-and-compatibility-deletion/07-03-SUMMARY.md
</context>

<scope_exclusions>
- Keep `ProductEventReceiver`, retained replay, snapshot recovery, queue overflow, and current channel capacity semantics.
- Do not alter durable session transactions, append/manifest ordering, control handles, or operation dispatch.
- Do not remove raw compatibility storage from ProductEvent in this plan; Plan 07-05 performs the final compiler-guided deletion.
</scope_exclusions>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- New public symbols/files: None.
- Deleted public compatibility symbols: `CodingAgentEventReceiver`, `CodingAgentSession::subscribe`.
- Deleted private compatibility symbols: `EventService::subscribe` and the legacy broadcast sender.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate receiver-based session and facade tests</name>
  <read_first>
    - crates/pi-coding-agent/src/coding_session/mod.rs
    - crates/pi-coding-agent/src/coding_session/event_service.rs
    - crates/pi-coding-agent/tests/public_api.rs
    - crates/pi-coding-agent/src/coding_session/public_projection.rs
    - crates/pi-coding-agent/src/lib.rs
  </read_first>
  <files>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/tests/public_api.rs</files>
  <behavior>
    - Co-located session tests retain exact event ordering, operation IDs, recovery markers, profile changes, delegation durability, forks, and PartialCommit assertions through typed receivers.
    - External public API tests subscribe through `subscribe_product_events_public()` and exhaustively match the 11 typed families.
    - Startup recovery markers are emitted once at the same subscription/query boundaries.
  </behavior>
  <action>Replace every remaining test call to the legacy session subscription with the appropriate internal `ProductEventReceiver` or stable public typed receiver, including every receiver-dependent test in `event_service.rs`. Rewrite raw event assertions to typed family/payload matches while keeping exact IDs, messages, ordering, and negative assertions. Preserve all deterministic fixtures and behavior coverage; do not delete tests to reduce migration work. This task is the pre-deletion gate: all migrated receiver tests must pass before Task 2 removes the legacy path.</action>
  <verify>
    <automated>cargo test -p pi-coding-agent --lib coding_session::tests --quiet &amp;&amp; cargo test -p pi-coding-agent --lib coding_session::event_service::tests --quiet &amp;&amp; cargo test -p pi-coding-agent --test public_api --quiet</automated>
  </verify>
  <acceptance_criteria>No first-party session/public API test depends on the legacy receiver, and the full public facade test plus relevant co-located session tests pass with typed assertions.</acceptance_criteria>
  <done>No first-party session/public API test depends on the legacy receiver, and the full public facade test plus relevant co-located session tests pass with typed assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete the legacy receiver and duplicate broadcast</name>
  <read_first>
    - crates/pi-coding-agent/src/coding_session/event_service.rs
    - crates/pi-coding-agent/src/coding_session/mod.rs
    - crates/pi-coding-agent/src/lib.rs
    - crates/pi-coding-agent/tests/event_boundary_guards.rs
  </read_first>
  <files>crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/tests/event_boundary_guards.rs</files>
  <behavior>
    - EventService assigns one sequence, retains once, and broadcasts one ProductEvent to typed receivers.
    - Typed receivers retain Closed/Lagged error mapping and replay/cursor behavior.
    - Source guards reject the legacy receiver type/session method/EventService sender outside the guard's own string fixtures while transitional ProductEvent compatibility storage remains permitted for Plan 07-05.
  </behavior>
  <action>After Task 1 receiver tests are green, delete `CodingAgentSession::subscribe`, `CodingAgentEventReceiver`, `EventService::subscribe`, the duplicate `broadcast::Sender<CodingAgentEvent>`, its send path, and obsolete facade re-exports/suppressions. Keep `ProductEventReceiver`, `subscribe_product_events_public`, startup recovery emission, publication locking, sequence assignment, retention-before-broadcast ordering, and lag mapping unchanged. Update the boundary guard using scoped source regions/allowlisted guard literals so comments do not self-trigger; at this boundary reject only `CodingAgentEventReceiver`, `CodingAgentSession::subscribe`, `EventService::subscribe`, the duplicate sender/send path, and their path-specific suppressions. Leave `compatibility_event()` and ProductEvent raw storage/accessor checks to Plan 07-05.</action>
  <verify>
    <automated>cargo test -p pi-coding-agent --test event_boundary_guards --test public_api --test product_event_contract --quiet</automated>
  </verify>
  <acceptance_criteria>The legacy receiver and duplicate broadcast no longer compile as public/private production APIs, the scoped guard fails closed on their reintroduction, and typed ordering/replay/public contract tests pass while ProductEvent compatibility storage remains available for Plan 07-05.</acceptance_criteria>
  <done>The legacy receiver and duplicate broadcast no longer compile as public/private production APIs, the scoped guard fails closed on their reintroduction, and typed ordering/replay/public contract tests pass while ProductEvent compatibility storage remains available for Plan 07-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EventService -> live subscribers | Sequenced untrusted payloads cross a bounded broadcast transport. |
| crate internals -> `pi_coding_agent::api` | Receiver/type visibility determines whether internal compatibility types leak publicly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10 | Denial of Service | `ProductEventReceiver` broadcast | high | mitigate | Preserve bounded channel, Lagged mapping, retained replay, and existing capacity/overflow tests while deleting only the duplicate sender. |
| T-07-11 | Information Disclosure | crate facade | high | mitigate | Remove the raw receiver export/method and retain compile/source guards around the curated typed facade. |
| T-07-12 | Repudiation | startup recovery/session tests | high | mitigate | Migrate exact operation/recovery/durability assertions before deletion so event attribution remains auditable. |
</threat_model>

<verification>
- `cargo test -p pi-coding-agent --test public_api --quiet`
- `cargo test -p pi-coding-agent --test product_event_contract --quiet`
- `cargo test -p pi-coding-agent --test event_boundary_guards --quiet`
- `cargo test -p pi-coding-agent --lib coding_session::event_service::tests --quiet`
- `cargo test -p pi-coding-agent --quiet`
</verification>

<success_criteria>
- All first-party receiver consumers use typed product events.
- Legacy receiver/session subscription and duplicate broadcast are deleted.
- Typed sequence, retention, replay, lag, recovery, durability, control, and public API behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/07-adapter-migration-and-compatibility-deletion/07-04-SUMMARY.md` when done.
</output>
