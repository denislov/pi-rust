---
phase: 08-client-connection-replay-and-scoped-control
plan: 07
type: execute
wave: 7
depends_on: [08-06]
files_modified:
  - crates/pi-coding-agent/src/protocol/rpc/state.rs
  - crates/pi-coding-agent/src/protocol/rpc/prompt.rs
  - crates/pi-coding-agent/src/protocol/rpc/commands.rs
  - crates/pi-coding-agent/tests/rpc_mode.rs
  - crates/pi-coding-agent/tests/protocol_events.rs
  - crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs
  - .planning/phases/08-client-connection-replay-and-scoped-control/08-VALIDATION.md
autonomous: true
requirements: [CLIENT-01, CLIENT-02, CLIENT-03, CONTROL-01]
must_haves:
  truths:
    - "D-01: RPC continues to invoke ordinary work only through CodingAgentSession::run(CodingAgentOperation); the connection is state/replay/control authority only."
    - "D-02/D-15: RPC reads restored drafts/submitted state from the public connection and preserves acceptance-driven clearing and existing responses."
    - "D-05/D-09: RPC projects public Replayed/FreshSnapshotRequired results while preserving overflow/error payloads and typed reasons."
    - "D-16/D-21: RPC abort/steer/follow-up use the immutable scoped handle and shared receipt idempotency, not raw/private control mirrors."
    - "All existing JSON, RPC, interactive, event-order, replay, PartialCommit, and canonical-dispatch assertions remain green."
  artifacts:
    - path: "crates/pi-coding-agent/src/protocol/rpc/state.rs"
      provides: "RPC projection over public connection without duplicate client truth"
    - path: "crates/pi-coding-agent/src/protocol/rpc/prompt.rs"
      provides: "Public recovery/ack/control consumption with wire-compatible output"
    - path: "crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs"
      provides: "Fail-closed guard against RPC mirror or second-dispatcher reintroduction"
    - path: ".planning/phases/08-client-connection-replay-and-scoped-control/08-VALIDATION.md"
      provides: "Concrete task map and green Nyquist sign-off"
  key_links:
    - from: "crates/pi-coding-agent/src/protocol/rpc/prompt.rs"
      to: "CodingAgentClientConnection"
      via: "snapshot/reconnect/ack/draft/submitted/control public methods"
      pattern: "CodingAgentClientConnection"
    - from: "crates/pi-coding-agent/src/protocol/rpc/prompt.rs"
      to: "CodingAgentSession::run"
      via: "unchanged canonical ordinary-operation task"
      pattern: "coding_session.run"
---

<objective>
Migrate RPC's local client/replay/control mirrors to the proven public connection contract, then close Phase 8 with boundary and workspace gates.

Purpose: Establish one runtime-owned behavior source while preserving every first-party adapter response and assertion.
Output: Thin RPC projection, deleted duplicate mirrors, fail-closed guards, concrete validation record, and full verification evidence.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-CONTEXT.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-RESEARCH.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-VALIDATION.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-06-SUMMARY.md
@crates/pi-coding-agent/src/protocol/rpc/state.rs
@crates/pi-coding-agent/src/protocol/rpc/prompt.rs
@crates/pi-coding-agent/src/protocol/rpc/commands.rs
@crates/pi-coding-agent/tests/rpc_mode.rs
@crates/pi-coding-agent/tests/protocol_events.rs
@crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs

<interfaces>
RpcState currently mirrors client_id, client_drafts, submitted_operation, adapter_applied_sequence/replayed_through_sequence, and bounded idempotency records. RPC prompt currently calls session.product_events_after and performs sequence dedup before live projection. Replace only authority now supplied by CodingAgentClientConnection; retain unrelated command-level operation idempotency and bounded output event_queue behavior. All ordinary background runners must still call coding_session.run(CodingAgentOperation).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze RPC wire parity around reconnect, state, and control</name>
  <files>crates/pi-coding-agent/tests/rpc_mode.rs, crates/pi-coding-agent/tests/protocol_events.rs</files>
  <read_first>crates/pi-coding-agent/tests/rpc_mode.rs, crates/pi-coding-agent/tests/protocol_events.rs, crates/pi-coding-agent/src/protocol/rpc/state.rs, crates/pi-coding-agent/src/protocol/rpc/prompt.rs</read_first>
  <behavior>
    - Existing response JSON, protocol event ordering, overflow payload, fresh_snapshot marker, and canonical operation outcomes remain byte/field compatible.
    - Reconnect after unacknowledged delivery replays by stable sequence; acknowledged delivery does not; gap and lag keep distinguishable recovery data.
    - Draft and submitted state survive same-id reconnect; rejected control preserves draft; duplicate control id returns one receipt/send.
    - PartialCommit operation identity and existing JSON/print/interactive assertions are not weakened or replaced by compile-only tests.
  </behavior>
  <action>Add/extend RED RPC integration scenarios that express the public contract through the existing wire without changing expected response shapes. Cover D-02/D-05/D-07/D-08/D-09/D-15/D-20/D-21 and retain all current order, overflow, queue, navigation, and PartialCommit checks. Use deterministic offline providers/capacity fixtures and stable sequence assertions; do not add adapter-only semantics to make tests pass.</action>
  <verify><automated>cargo test -p pi-coding-agent --test rpc_mode client_connection --quiet && cargo test -p pi-coding-agent --test protocol_events --quiet</automated></verify>
  <acceptance_criteria>Parity tests fail against RPC-local authority where the new connection contract is not consumed and retain all pre-existing assertions.</acceptance_criteria>
  <done>The migration has a behavior-preserving wire safety net before duplicate state is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize RPC client state, recovery, acknowledgement, and control</name>
  <files>crates/pi-coding-agent/src/protocol/rpc/state.rs, crates/pi-coding-agent/src/protocol/rpc/prompt.rs, crates/pi-coding-agent/src/protocol/rpc/commands.rs, crates/pi-coding-agent/tests/rpc_mode.rs</files>
  <read_first>crates/pi-coding-agent/src/protocol/rpc/state.rs, crates/pi-coding-agent/src/protocol/rpc/prompt.rs, crates/pi-coding-agent/src/protocol/rpc/commands.rs, crates/pi-coding-agent/src/protocol/rpc/event_queue.rs, crates/pi-coding-agent/tests/rpc_mode.rs</read_first>
  <behavior>
    - RpcState holds a stable public CodingAgentClientConnection and adapter rendering state, not a second client registry.
    - Replay/live recovery and acknowledgement delegate to connection methods while adapter dedup still uses event sequence for at-least-once delivery.
    - Control delegates to CodingAgentPromptControl and maps typed receipt/rejection to unchanged RPC responses.
    - Ordinary Prompt and every other canonical operation still execute through session.run.
  </behavior>
  <action>After public-contract tests are green, create/restore the RPC connection at session initialization and migrate snapshot/draft/submitted/replay/ack/control calls to it. Delete RpcState client_drafts and submitted_operation mirrors, replay authority duplicated by adapter_applied_sequence/replayed_through_sequence where the public recovery result now owns it, and control-specific idempotency/target state superseded by stable receipts. Retain only adapter-local output dedup required to apply an at-least-once stream and unrelated ordinary-command idempotency. Map typed gap/lag/control reasons to the existing wire fields/messages. Keep event_queue overflow behavior and every call to CodingAgentSession::run unchanged.</action>
  <verify><automated>cargo test -p pi-coding-agent --test rpc_mode --quiet && cargo test -p pi-coding-agent --test protocol_events --quiet && cargo test -p pi-coding-agent --lib protocol::rpc --quiet</automated></verify>
  <acceptance_criteria>RPC has no duplicate authoritative drafts/submitted/replay/control state, all wire/ordering assertions pass, and canonical run calls remain exhaustive.</acceptance_criteria>
  <done>RPC is a projection of the stateful connection contract rather than a parallel implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Close boundary guards, validation map, and full project gates</name>
  <files>crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs, .planning/phases/08-client-connection-replay-and-scoped-control/08-VALIDATION.md</files>
  <read_first>crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs, crates/pi-coding-agent/tests/api_boundary_guards.rs, .planning/phases/08-client-connection-replay-and-scoped-control/08-VALIDATION.md, .planning/ROADMAP.md</read_first>
  <action>Add recursive source guards requiring public connection projection and prohibiting RPC mirrors, connection dispatchers, duplicate SnapshotState/client maps, and private leaks. Rebuild 08-VALIDATION as exactly 18 rows matching the explicit ordered array `08-01-T1` through `08-07-T3` with the actual per-plan counts, exactly 10 columns, `08-XX` plan cells, numeric frontmatter waves, verbatim normalized automated commands, aligned requirement/threat/behavior, and pending runtime status. The checker must compare extracted PLAN ids and table ids to the explicit array with exact ordered equality and reject extra/missing/duplicate rows, Plan/Wave mismatch, column mismatch, or command mismatch. Add a closure audit that fails when actionable Phase 8 receipt sections contain stale eviction/reuse language or incorrect volatile admission ordering. Run full formatting/tests/checks/source audits/diff check; do not add Phase 9 lifecycle work or claim runtime success.</action>
  <verify><automated>cargo fmt --check && cargo test -p pi-coding-agent --test public_api --test protocol_events --test rpc_mode --test api_boundary_guards --test product_runtime_boundary_guards --quiet && cargo test -p pi-coding-agent --quiet && cargo test --workspace --quiet && cargo check --workspace && git diff --check</automated></verify>
  <acceptance_criteria>All focused/full gates pass; source audits find one public client authority and one ordinary dispatcher; validation records concrete green evidence for all four requirements and high threats.</acceptance_criteria>
  <done>Phase 8 is behavior-compatible, Nyquist-complete, boundary-closed, and ready for Phase 9 lifecycle/association work.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan(s) | Status | Notes |
|--------|----|----------------------|---------|--------|-------|
| GOAL | - | Public reconnectable snapshot, retained replay, cursor recovery, submitted state, drafts, and scoped control | 08-01..08-07 | COVERED | Contract -> state/replay -> coordinator -> connection/lease -> control -> adapter convergence. |
| REQ | CLIENT-01 | Snapshot cursor and retained replay resume | 08-01, 08-03, 08-04, 08-05, 08-07 | COVERED | Atomic coordinator/EventService boundary plus public projection and RPC parity. |
| REQ | CLIENT-02 | Typed stale-cursor/live-lag recovery | 08-01, 08-03, 08-04, 08-05, 08-07 | COVERED | Structured reason and sequence bounds, no opaque fallback. |
| REQ | CLIENT-03 | Submitted operation and client-local drafts | 08-01, 08-02, 08-04, 08-05, 08-06, 08-07 | COVERED | All-kind status, takeover, exact lease commit/drop, acceptance clearing. |
| REQ | CONTROL-01 | Abort/Steer/FollowUp outside ordinary queue | 08-01, 08-06, 08-07 | COVERED | Immutable owner/generation/target capability and receipts. |
| RESEARCH | Architecture | Sole SnapshotState registry and zero-authority facade | 08-02, 08-04, 08-05 | COVERED | One shared coordinator Arc and six explicit writer algorithms. |
| RESEARCH | Replay | Publisher-owned atomic replay/live partition and explicit ack | 08-03, 08-04, 08-05 | COVERED | Sequence is dedup authority; at-least-once contract. |
| RESEARCH | State | Drafts, submitted status, and exact lease lifecycle | 08-01, 08-02, 08-05 | COVERED | Rejection/precommit preserves; post-admission commit clears exactly once. |
| RESEARCH | Control | Scoped non-evicting receipt admission | 08-06 | COVERED | Key/signature lookup precedes new-key capacity and volatile target/channel checks. |
| RESEARCH | Migration | Prove public behavior before removing RPC-local mirrors | 08-01..08-06, then 08-07 | COVERED | Existing wire/event/PartialCommit assertions retained. |
| RESEARCH | Boundary | Curated api exports and private type/second-dispatcher guards | 08-01, 08-05, 08-07 | COVERED | External signatures plus recursive source guard. |
| CONTEXT | D-01..D-04 | Stateful connection, identity/takeover, atomic snapshot, no second dispatcher | 08-01, 08-02, 08-04, 08-05, 08-07 | COVERED | Generation checks and owner coordination. |
| CONTEXT | D-05..D-09 | Typed replay/recovery, atomic handoff, explicit ack, at-least-once, metadata | 08-01, 08-03, 08-04, 08-05, 08-07 | COVERED | Both retained gap and live lag tested. |
| CONTEXT | D-10..D-15 | All-kind submitted state and exact draft semantics | 08-01, 08-02, 08-05, 08-06, 08-07 | COVERED | Phase 9 exhaustive association is intentionally excluded. |
| CONTEXT | D-16..D-21 | Immutable owner control, typed receipt/rejection, stable-id idempotency/order | 08-01, 08-06, 08-07 | COVERED | Abort follows same identity contract. |
| EXCLUSION | Phase 9/Future | detach/close/shutdown, exhaustive associations, adapter-root/diagnostic hardening, runtime rename/daemon/collaboration | NONE | EXCLUDED | Explicitly deferred by CONTEXT/ROADMAP/REQUIREMENTS. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSONL RPC input -> public connection | Untrusted command fields become cursor, draft, operation, and control requests. |
| Public connection -> RPC wire projection | Typed state/recovery/control results are serialized without exposing runtime internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07-RPC-SPOOF | Spoofing / Elevation of Privilege | RPC control commands | high | mitigate | Use connection-owned generation/owner/target validation; tests cover cross-client and stale takeover. |
| T-08-07-MIRROR-DRIFT | Tampering / Repudiation | RpcState | high | mitigate | Delete duplicate authority, add recursive scoped guard, and preserve sequence/wire parity tests. |
| T-08-07-INPUT | Tampering / Denial of Service | RPC cursor/id/text fields | high | mitigate | Typed parsing/validation, bounded queues/caches, explicit gap/lag/rejection projection, deterministic overflow tests. |
| T-08-07-LEAK | Information Disclosure | serialized errors/results | high | mitigate | Serialize stable public values/codes only; boundary guards reject private types and tests preserve current response payloads. |
</threat_model>

## Artifacts this phase produces

- RpcState field replacement: one public `CodingAgentClientConnection` replaces authoritative `client_id`, `client_drafts`, `submitted_operation`, replay markers, and control-specific receipt/target mirrors; unrelated adapter rendering/ordinary-command idempotency fields remain only when behavior requires them.
- RPC prompt/command paths consuming public `snapshot`, `reconnect`, `acknowledge`, draft, submitted-state, and scoped-control methods while retaining `CodingAgentSession::run` ordinary dispatch.
- New/extended deterministic `client_connection` RPC scenarios and protocol parity assertions; no new production API symbol is introduced.
- New `product_runtime_boundary_guards` assertions for single client authority, private-type exclusion, and no second dispatcher.
- Updated `08-VALIDATION.md` concrete task IDs/status/sign-off fields; no new production file, dependency, wire command, or Phase 9 lifecycle symbol is added.

<verification>
The Task 3 automated command is the closure gate. Also audit production adapters for private ClientService/ClientRecord/PromptControlHandle/OperationControl imports and confirm only session.run accepts CodingAgentOperation for ordinary work.
</verification>

<success_criteria>
All first-party behavior remains green with client/replay/control truth centralized under the public connection and all Phase 8 requirements/decisions evidenced by focused tests and guards.
</success_criteria>

<output>
Create `.planning/phases/08-client-connection-replay-and-scoped-control/08-07-SUMMARY.md` when done.
</output>
