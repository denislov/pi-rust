---
phase: 08-client-connection-replay-and-scoped-control
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - crates/pi-coding-agent/src/coding_session/client_projection.rs
  - crates/pi-coding-agent/src/coding_session/client_service.rs
  - crates/pi-coding-agent/src/coding_session/mod.rs
autonomous: true
requirements: [CLIENT-03]
must_haves:
  truths:
    - "D-02/D-03: same-id takeover preserves state, increments generation, and makes every old-generation mutation fail closed."
    - "D-04/D-14: one registry read produces the complete client-local portion of an atomic snapshot."
    - "D-10/D-11: any canonical operation kind can occupy monotonic Accepted -> Running -> Terminal submitted state, retained until acknowledgement."
    - "D-12/D-13/D-15: Prompt replacement and stable-id ordered Steer/FollowUp queues clear only after runtime acceptance."
  artifacts:
    - path: "crates/pi-coding-agent/src/coding_session/client_service.rs"
      provides: "Session-owned generation registry, draft queues, submitted state, acknowledgements, and bounded receipt storage"
    - path: "crates/pi-coding-agent/src/coding_session/client_projection.rs"
      provides: "Private typed client state and transition invariants"
    - path: "crates/pi-coding-agent/src/coding_session/mod.rs"
      provides: "Private ClientService ownership under CodingAgentSession"
  key_links:
    - from: "crates/pi-coding-agent/src/coding_session/mod.rs"
      to: "crates/pi-coding-agent/src/coding_session/client_service.rs"
      via: "CodingAgentSession owns exactly one registry for the live session"
      pattern: "client_service: ClientService"
    - from: "crates/pi-coding-agent/src/coding_session/client_service.rs"
      to: "crates/pi-coding-agent/src/coding_session/client_projection.rs"
      via: "generation-checked transitions over private records"
      pattern: "ClientRecord"
---

<objective>
Implement the session-owned client registry and its deterministic state machines.

Purpose: Replace detached value-only client state with one live-session authority before replay/control wiring.
Output: Private ClientService, generation takeover, ack cursor, typed drafts, submitted transitions, and focused unit tests.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-CONTEXT.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-RESEARCH.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-PATTERNS.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-01-SUMMARY.md
@crates/pi-coding-agent/src/coding_session/client_projection.rs
@crates/pi-coding-agent/src/coding_session/mod.rs

<interfaces>
Use the stable projection vocabulary from 08-01. Existing CodingAgentSession owns EventService, OperationControl, capability snapshots, and startup markers as fields. Follow that service-container pattern; use Arc&lt;Mutex&lt;...&gt;&gt; only for short synchronous registry transitions and never hold a standard mutex across await.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify generation, draft, acknowledgement, and submitted-state invariants</name>
  <files>crates/pi-coding-agent/src/coding_session/client_service.rs, crates/pi-coding-agent/src/coding_session/client_projection.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/client_projection.rs, crates/pi-coding-agent/src/coding_session/capability_snapshot.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/protocol/rpc/state.rs</read_first>
  <behavior>
    - First connect creates generation 1; same-id reconnect returns preserved state at generation n+1; old generation reads/mutations/acks reject with typed stale state.
    - Ack is monotonic and cannot exceed the connection's observed/recoverable sequence; delivery does not mutate it.
    - Prompt set replaces the sole Prompt draft; Steer and FollowUp queues preserve insertion order and stable-id update/delete identity.
    - Submitted state accepts any OperationKind, rejects regression/identity mismatch, and retains Terminal until matching acknowledgement.
    - Rejected acceptance/control callbacks preserve the exact draft; accepted callbacks clear only the accepted draft id/kind.
  </behavior>
  <action>Create RED unit tests using small deterministic records and no runtime/provider. Cover D-02/D-03 takeover and all old-handle mutation paths, D-07 explicit monotonic ack, D-10/D-11 all-kind submitted progression/terminal retention, D-12/D-13 queue structure, and D-15 acceptance-only clearing. Include bounded queue/idempotency lifecycle tests and explicit overflow/rejection behavior chosen under agent discretion.</action>
  <verify><automated>cargo test -p pi-coding-agent --lib client_service --quiet</automated></verify>
  <acceptance_criteria>Tests fail on the detached current ClientConnection model and cover every state transition/rejection before implementation.</acceptance_criteria>
  <done>The registry behavior is fully specified with deterministic state-machine tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the generation-checked ClientService</name>
  <files>crates/pi-coding-agent/src/coding_session/client_projection.rs, crates/pi-coding-agent/src/coding_session/client_service.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/client_projection.rs, crates/pi-coding-agent/src/coding_session/client_service.rs, crates/pi-coding-agent/src/coding_session/error.rs, crates/pi-coding-agent/src/protocol/rpc/state.rs</read_first>
  <behavior>
    - Every mutable entry point validates client id and captured generation before reading target state.
    - FIFO order and stable ids survive takeover; duplicate ids update/retry by documented identity rather than duplicating entries.
    - Accepted/Running/Terminal transitions are monotonic and terminal removal requires an explicit matching acknowledgement.
  </behavior>
  <action>Implement private `ClientService` with a HashMap keyed by ClientConnectionId, monotonically increasing generation, acknowledged sequence, Prompt slot, separate Steer/FollowUp VecDeque values, submitted operation/status, and bounded HashMap+VecDeque receipt/idempotency storage. Return stable typed rejection projections through conversion helpers; do not treat client id as authentication. Validate input length/non-empty constraints before mutation. Preserve client-local state in memory only; do not write drafts/acks to SessionService or the durable event log. Use one lock per atomic transition and release it before any async work.</action>
  <verify><automated>cargo test -p pi-coding-agent --lib client_service --quiet && cargo test -p pi-coding-agent --lib client_projection --quiet</automated></verify>
  <acceptance_criteria>All takeover, stale-handle, ack, draft, submitted-state, capacity, and rejection tests pass; no lock is held across await and no durable schema changes occur.</acceptance_criteria>
  <done>One private service is authoritative for reconnectable client-local state.</done>
</task>

<task type="auto">
  <name>Task 3: Install ClientService under the runtime owner</name>
  <files>crates/pi-coding-agent/src/coding_session/mod.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/client_service.rs, crates/pi-coding-agent/src/coding_session/capability_snapshot.rs</read_first>
  <action>Declare the private module, add exactly one ClientService field to CodingAgentSession, and initialize it in every persistent/transient constructor path. Keep services/registry private and keep connect/run signatures unchanged in this plan; Plan 08-04 owns public handle wiring. Add owner-level construction tests proving each constructor gets isolated client identity state.</action>
  <verify><automated>cargo test -p pi-coding-agent --lib client_service --quiet</automated></verify>
  <acceptance_criteria>Persistent and non-persistent sessions own isolated registries and no static/global registry is introduced.</acceptance_criteria>
  <done>The live session owner contains the private state authority needed by connection and control plans.</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Connection handle -> ClientService | Caller-controlled id/generation/draft/control identifiers request state mutation. |
| ClientService -> session projection | Client-local state must not become durable session truth or leak across sessions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-STALE-HANDLE | Elevation of Privilege / Tampering | ClientService mutation methods | high | mitigate | Compare id+generation first on every read/mutation/ack/control-preparation path; takeover tests enumerate old-handle failures. |
| T-08-02-CROSS-SESSION | Information Disclosure | ClientService ownership | high | mitigate | Per-CodingAgentSession field, no globals, constructor-isolation tests. |
| T-08-RESOURCE-BOUND | Denial of Service | draft queues and receipt records | high | mitigate | Private deterministic caps, validation, FIFO eviction/rejection tests; no unbounded client-controlled collection. |
| T-08-02-ROLLBACK | Tampering / Repudiation | submitted status and ack cursor | high | mitigate | Monotonic transition functions reject regression/identity mismatch and retain terminal until matching ack. |
</threat_model>

## Artifacts this phase produces

- New private file `crates/pi-coding-agent/src/coding_session/client_service.rs`.
- New private symbols: `ClientGeneration`, `ClientHandle`, `ClientRecord`, `ClientService`, `ClientStateRejection`, `SubmittedOperationStatus`, `ControlIdempotencyKey`, and the private FIFO receipt record type.
- New ClientRecord fields: `generation`, `acknowledged_sequence`, `prompt_draft`, `steer_drafts`, `follow_up_drafts`, `submitted_operation`, `control_receipts`, and `control_receipt_order`.
- New `CodingAgentSession.client_service` private field and constructor initialization paths.
- Focused `client_service` state-machine tests; no public symbol or durable session-log field is added here.

<verification>
Run focused client service/projection/owner tests, `cargo fmt --check`, `cargo check -p pi-coding-agent --all-targets`, and `git diff --check`.
</verification>

<success_criteria>
Same-id takeover, explicit acknowledgement, typed drafts, and submitted-state lifecycle have one bounded, fail-closed, session-owned implementation.
</success_criteria>

<output>
Create `.planning/phases/08-client-connection-replay-and-scoped-control/08-02-SUMMARY.md` when done.
</output>
