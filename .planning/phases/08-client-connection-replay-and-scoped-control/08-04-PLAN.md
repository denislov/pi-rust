---
phase: 08-client-connection-replay-and-scoped-control
plan: 04
type: execute
wave: 4
depends_on: [08-02, 08-03]
files_modified:
  - crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs
  - crates/pi-coding-agent/src/coding_session/client_service.rs
  - crates/pi-coding-agent/src/coding_session/mod.rs
  - crates/pi-coding-agent/src/coding_session/operation_control.rs
  - crates/pi-coding-agent/src/coding_session/capability_snapshot.rs
  - crates/pi-coding-agent/src/coding_session/event_service.rs
  - crates/pi-coding-agent/src/coding_session/session_service.rs
  - crates/pi-coding-agent/tests/public_api.rs
autonomous: true
requirements: [CLIENT-01, CLIENT-02, CLIENT-03]
must_haves:
  truths:
    - "D-02/D-04/D-06: SnapshotCoordinator contains the single SnapshotState and every snapshot-visible writer commits through it without mixed revisions."
    - "D-03/D-07/D-11: client generation, acknowledgement, submitted status, session view, capability projection, active operation, cursor, and recovery metadata have one authority and deterministic ordering."
    - "ClientService is a stateless zero-authority facade over the same Arc<SnapshotCoordinator>; CodingAgentSession may store both references but never a duplicate registry or map."
    - "All six reverse-lock writer families use named two-phase algorithms, hold no standard mutex across await, and publish only after the coordinator transaction releases."
  artifacts:
    - path: "crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs"
      provides: "Sole SnapshotState authority and transactional projection methods"
    - path: "crates/pi-coding-agent/src/coding_session/client_service.rs"
      provides: "Zero-authority Arc<SnapshotCoordinator> delegation facade"
    - path: "crates/pi-coding-agent/tests/public_api.rs"
      provides: "Writer-order, deadlock-timeout, and mixed-state assertions"
  key_links:
    - from: "crates/pi-coding-agent/src/coding_session/client_service.rs"
      to: "crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs"
      via: "every concrete client method delegates to one shared coordinator Arc"
      pattern: "Arc<SnapshotCoordinator>"
    - from: "crates/pi-coding-agent/src/coding_session/event_service.rs"
      to: "crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs"
      via: "coordinator transaction assigns cursor and updates recovery/publication projection"
      pattern: "SnapshotCoordinator"
---

<objective>
Install the one authoritative snapshot topology and migrate every reverse-lock writer before exposing the stateful public connection.

Purpose: Make atomic snapshots and replay/live boundaries implementable without duplicate state, nested-lock deadlocks, or mixed revisions.
Output: SnapshotCoordinator/SnapshotState authority, stateless ClientService facade, six exact writer algorithms, and deterministic concurrency tests.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-CONTEXT.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-RESEARCH.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-PATTERNS.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-02-SUMMARY.md
@.planning/phases/08-client-connection-replay-and-scoped-control/08-03-SUMMARY.md
@crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs
@crates/pi-coding-agent/src/coding_session/client_service.rs
@crates/pi-coding-agent/src/coding_session/mod.rs
@crates/pi-coding-agent/src/coding_session/operation_control.rs
@crates/pi-coding-agent/src/coding_session/capability_snapshot.rs
@crates/pi-coding-agent/src/coding_session/event_service.rs
@crates/pi-coding-agent/src/coding_session/session_service.rs
@crates/pi-coding-agent/tests/public_api.rs

<interfaces>
`SnapshotCoordinator { state: Mutex<SnapshotState>, ... }` is the sole authority. `SnapshotState` contains the only client registry/state plus session-view projection, capability source/projection, active operation, event cursor/publication deque, and recovery metadata. `ClientService { coordinator: Arc<SnapshotCoordinator> }` owns no map, generation, queue, receipt, or projection; its concrete methods delegate to the coordinator. `CodingAgentSession` may hold both a coordinator Arc and the facade, but both reach the same state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install the sole SnapshotState and zero-authority client facade</name>
  <files>crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/src/coding_session/client_service.rs, crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/tests/public_api.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/src/coding_session/client_service.rs, crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/client_projection.rs, crates/pi-coding-agent/tests/public_api.rs</read_first>
  <behavior>
    - SnapshotState is the only owner of client records and all snapshot-visible projections; source guards find no second client map or independent ClientService state.
    - ClientService holds only Arc<SnapshotCoordinator> and delegates connect_or_takeover, client_snapshot, acknowledge, set_prompt_draft, enqueue_control_draft, submitted transitions, and control-receipt transitions.
    - One coordinator transaction returns an owned immutable projection; no lock survives conversion, serialization, callback, await, receiver consumption, or user code.
  </behavior>
  <action>Per D-02/D-03/D-04/D-07/D-10 through D-15, implement the authoritative topology exactly as declared in interfaces. Move the registry/state introduced by 08-02 into SnapshotState rather than retaining a second ClientService map. Make ClientService a stateless facade with named delegation methods `connect_or_takeover`, `client_snapshot`, `acknowledge`, `set_prompt_draft`, `enqueue_control_draft`, submitted transition methods, and control receipt transition methods. Initialize one Arc in every session constructor and derive the facade from that same Arc. Add structural and deterministic isolation tests proving there is one registry, one revision, no duplicate state, and no mixed projection.</action>
  <verify><automated>cargo test -p pi-coding-agent --lib snapshot_coordinator --quiet && cargo test -p pi-coding-agent --lib client_service --quiet && cargo test -p pi-coding-agent --test public_api snapshot_topology --quiet</automated></verify>
  <acceptance_criteria>One SnapshotState owns all authoritative projection/client data; ClientService has zero authority and every session constructor shares exactly one coordinator Arc.</acceptance_criteria>
  <done>The topology is singular, explicit, and mechanically guarded before writer migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate all reverse-lock writers with exact two-phase algorithms</name>
  <files>crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/operation_control.rs, crates/pi-coding-agent/src/coding_session/capability_snapshot.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/coding_session/session_service.rs, crates/pi-coding-agent/tests/public_api.rs</files>
  <read_first>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/src/coding_session/operation_control.rs, crates/pi-coding-agent/src/coding_session/capability_snapshot.rs, crates/pi-coding-agent/src/coding_session/event_service.rs, crates/pi-coding-agent/src/coding_session/session_service.rs, crates/pi-coding-agent/src/coding_session/snapshot_coordinator.rs, crates/pi-coding-agent/tests/public_api.rs</read_first>
  <behavior>
    - Startup marker drain takes markers under the startup lock, releases it, then transactionally projects and emits in coordinator order.
    - OperationGuard drop records a pending active-clear token without nested locks; after owner mutation a coordinator-safe synchronous hook transactionally clears projection/event.
    - Capability install computes the next immutable generation outside the coordinator, transactionally commits capability source plus projection, releases, then emits.
    - Navigation/session refresh performs persistence IO and await outside the coordinator, builds an immutable view, transactionally swaps projection, releases, then publishes.
    - EventService builds events outside locks; a coordinator transaction assigns cursor and updates recovery projection/publication deque; broadcast send occurs after release, including clone emit paths.
    - Client mutations validate generation and mutate the sole registry in the coordinator, returning owned results after release.
  </behavior>
  <action>Implement the six numbered algorithms exactly in their writer-owning files. Preserve the total order coordinator transaction before any subordinate short lock, but eliminate every reverse acquisition through the two-phase steps above. Do not hold the coordinator or standard mutex across persistence IO, `.await`, conversion, callback, broadcast send, or receiver work. Bind tests one-to-one to writer algorithms 1 through 6; add bounded Tokio timeout deadlock tests plus concurrent snapshot assertions that forbid mixed active-operation/capability/session/cursor/client revisions.</action>
  <verify><automated>cargo test -p pi-coding-agent --test public_api snapshot_writers --quiet && cargo test -p pi-coding-agent --lib coding_session::event_service::tests --quiet && cargo test -p pi-coding-agent --lib coding_session::operation_control::tests --quiet</automated></verify>
  <acceptance_criteria>All six writers use the specified two-phase route, tests cover each numbered route and clone/drop paths, and timeout/mixed-state assertions pass.</acceptance_criteria>
  <done>Every snapshot-visible mutation enters the single coordinator without reverse-lock deadlocks or publication under lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Product writers -> SnapshotState | Concurrent owner mutations must become one coherent projection revision. |
| Guard/drop/clone paths -> coordinator | Synchronous cleanup and cloned emitters must not invert locks or publish mixed state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-DUPLICATE | Tampering / Repudiation | SnapshotState and ClientService | high | mitigate | One registry, stateless facade, source guard, and mixed-state tests. |
| T-08-04-DEADLOCK | Denial of Service | six reverse-lock writers | high | mitigate | Exact two-phase algorithms, no await/send under lock, bounded timeout tests. |
| T-08-04-ORDER | Tampering | event cursor and projections | high | mitigate | Transactional revision assignment before post-release broadcast and deterministic concurrent assertions. |
</threat_model>

<verification>
Run both task commands, `cargo fmt --check`, `cargo check -p pi-coding-agent --all-targets`, and `git diff --check`.
</verification>

<success_criteria>
The runtime has one snapshot/client authority and every writer can update it coherently without duplicate maps, lock inversion, or publication under a lock.
</success_criteria>

<output>
Create `.planning/phases/08-client-connection-replay-and-scoped-control/08-04-SUMMARY.md` when done.
</output>
