---
phase: 05-boundary-enforcement-and-stage-9-closure
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - .planning/phases/05-boundary-enforcement-and-stage-9-closure/05-STAGE-9-CLOSURE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - docs/superpowers/ARCHITECTURE.md
  - docs/superpowers/specs/2026-07-10-canonical-operation-runtime-convergence-design.md
  - docs/superpowers/plans/2026-07-10-canonical-operation-runtime-convergence-plan.md
autonomous: true
requirements: [CLOSE-01, CLOSE-02, CLOSE-03, CLOSE-04]
must_haves:
  truths:
    - Final source audits find no unexpected deleted-method definitions/calls, compatibility synonyms, or production deprecation suppressions.
    - Focused, crate, workspace, formatting, and diff checks pass from one final tree.
    - "D-18: One authoritative closure report records exact commands, UTC timestamps, statuses, conclusions/counts, source-audit scope with zero-violation results, and commit/worktree identity without complete Cargo logs."
    - "D-19: The historical convergence plan body remains intact, with only a clear superseded status and closure-report link added."
    - "D-20: Current architecture, design, project, requirements, roadmap, and state authorities match verified Stage 9 and link to the closure report."
    - Current authority documents link to the closure report; Stage 10 handoff is bounded to typed ProductEvent payloads and compatibility subscriptions.
  artifacts:
    - .planning/phases/05-boundary-enforcement-and-stage-9-closure/05-STAGE-9-CLOSURE.md
  key_links:
    - Closure evidence is generated only after all guard and API plans pass.
    - Documentation consistently identifies CodingAgentSession::run as the Stage 9 boundary.
---

<objective>
Run the complete Stage 9 verification stack and publish one authoritative closure record with synchronized current documentation.

Purpose: Implement D-17 through D-21 and CLOSE-01 through CLOSE-04 without altering historical evidence or implementing Stage 10.
Output: Structured closure report, updated authority documents, and superseded historical-plan marker.
</objective>

<context>
@.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-CONTEXT.md
@.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-VALIDATION.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-VERIFICATION.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/plans/2026-07-10-canonical-operation-runtime-convergence-plan.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Execute final source, focused, crate, and workspace verification</name>
  <read_first>crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs; crates/pi-coding-agent/tests/api_boundary_guards.rs; crates/pi-coding-agent/tests/public_api.rs; .planning/phases/05-boundary-enforcement-and-stage-9-closure/05-VALIDATION.md</read_first>
  <files>crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs; crates/pi-coding-agent/tests/api_boundary_guards.rs; crates/pi-coding-agent/tests/public_api.rs</files>
  <action>Run the required source audits and verification commands from the final tree: focused boundary/API tests, `cargo fmt --check`, `cargo test -p pi-coding-agent`, `cargo check -p pi-coding-agent`, `cargo test --workspace`, `cargo check --workspace`, and `git diff --check`. Per D-18, record UTC timestamps, exact commands, statuses, meaningful counts/conclusions, source-audit scope and zero-violation results, plus `git rev-parse HEAD` and worktree status without pasting complete Cargo logs or weakening failed checks.</action>
  <verify><automated>cargo test -p pi-coding-agent --test product_runtime_boundary_guards --test api_boundary_guards --test public_api -- --nocapture && cargo fmt --check && cargo test -p pi-coding-agent && cargo check -p pi-coding-agent && cargo test --workspace && cargo check --workspace && git diff --check</automated></verify>
  <acceptance_criteria>Every required command exits successfully; source audits report zero unexpected old paths/suppressions; evidence values are reproducible from the final tree.</acceptance_criteria>
  <done>CLOSE-01, CLOSE-02, and CLOSE-03 verification evidence is complete and ready for the authoritative report.</done>
</task>
<task type="auto">
  <name>Task 2: Publish closure report and synchronize Stage 9 authority documents</name>
  <read_first>.planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/PROJECT.md; .planning/STATE.md; docs/superpowers/ARCHITECTURE.md; docs/superpowers/specs/2026-07-10-canonical-operation-runtime-convergence-design.md; docs/superpowers/plans/2026-07-10-canonical-operation-runtime-convergence-plan.md</read_first>
  <files>.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-STAGE-9-CLOSURE.md; .planning/PROJECT.md; .planning/REQUIREMENTS.md; .planning/ROADMAP.md; .planning/STATE.md; docs/superpowers/ARCHITECTURE.md; docs/superpowers/specs/2026-07-10-canonical-operation-runtime-convergence-design.md; docs/superpowers/plans/2026-07-10-canonical-operation-runtime-convergence-plan.md</files>
  <action>Finish all closure and authority-document edits first, then run the required final-tree verification stack from that exact post-edit worktree: source audits for deleted-method definitions/calls, compatibility synonyms, and production deprecation suppressions; focused boundary/API tests; `cargo fmt --check`; `cargo test -p pi-coding-agent`; `cargo check -p pi-coding-agent`; `cargo test --workspace`; `cargo check --workspace`; and `git diff --check`. Per D-18, capture UTC timestamps, exact commands, exit statuses, meaningful counts/conclusions, source-audit scope and zero-violation results, `git rev-parse HEAD`, and `git status --short` (or an equivalent complete worktree identity) in one concise authoritative report containing the final boundary/deleted-method conclusions, GUARD/CLOSE mapping, and bounded Stage 10 inventory with source locations and behavior constraints; omit complete Cargo logs. Because writing the report changes the tree, record the identity of the tree verified immediately before the report write, then perform a final post-report `git diff --check` and state explicitly in the report that its evidence covers the pre-report-write tree plus the post-write hygiene check; do not claim a clean identity that the report itself invalidates. Per D-20, update current project, requirements, roadmap, state, architecture, and design authorities to link to that report and state the closed `CodingAgentSession::run(CodingAgentOperation)` plus `pi_coding_agent::api` boundary. Per D-19, add a clear superseded marker and report link to the historical plan while preserving its body. Do not create a Stage 10 implementation plan or alter deferred event work.</action>
  <verify><automated>test -s .planning/phases/05-boundary-enforcement-and-stage-9-closure/05-STAGE-9-CLOSURE.md && rg -n "Stage 10|STAGE-9-CLOSURE|superseded|CodingAgentSession::run" .planning docs/superpowers/ARCHITECTURE.md docs/superpowers/specs/2026-07-10-canonical-operation-runtime-convergence-design.md docs/superpowers/plans/2026-07-10-canonical-operation-runtime-convergence-plan.md && cargo fmt --check && cargo test -p pi-coding-agent --test product_runtime_boundary_guards --test api_boundary_guards --test public_api -- --nocapture && cargo test -p pi-coding-agent && cargo check -p pi-coding-agent && cargo test --workspace && cargo check --workspace && git diff --check && rg -n "UTC|rev-parse|git status|cargo test --workspace|cargo check --workspace|cargo fmt --check|source audit|zero" .planning/phases/05-boundary-enforcement-and-stage-9-closure/05-STAGE-9-CLOSURE.md</automated></verify>
  <acceptance_criteria>Exactly one report is identified as Stage 9 authority; all current documents link to it; historical plan content remains intact except for superseded status/link; Stage 10 scope is bounded and explicit.</acceptance_criteria>
  <done>CLOSE-04 is satisfied and the repository is ready for milestone verification/archive.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Verification commands -> closure report | Evidence must describe the exact tree actually tested. |
| Current docs -> historical plan | Authority links must not rewrite historical evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-04 | Tampering | final verification | high | mitigate | Mandatory focused, crate, workspace, format, audit, and diff command stack. |
| T-05-05 | Repudiation | closure evidence | high | mitigate | UTC timestamps, exact commands, statuses, counts, HEAD, and worktree identity. |
| T-05-SC | Tampering | Cargo verification | high | mitigate | Record only successful verified dependency/build state; no unreviewed dependency changes. |
</threat_model>

<verification>Repeat the full command stack after documentation edits and confirm `git diff --check`.</verification>
<success_criteria>Stage 9 is closed with reproducible evidence and current documentation, while Stage 10 remains explicitly deferred.</success_criteria>
<output>Write `.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-03-SUMMARY.md` after execution.</output>
