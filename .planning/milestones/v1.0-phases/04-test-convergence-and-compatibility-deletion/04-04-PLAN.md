---
phase: 04-test-convergence-and-compatibility-deletion
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified: [crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs, crates/pi-coding-agent/tests/api_boundary_guards.rs, .planning/phases/04-test-convergence-and-compatibility-deletion/04-VALIDATION.md, .planning/ROADMAP.md]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, DELETE-01, DELETE-02, DELETE-03, DELETE-04]
must_haves:
  truths: ["Navigation and summary tests use canonical operations and preserve replay, event continuity, target, snapshot, and failure assertions.", "All 16 replaced broad methods, including summarize_branch_for_navigation, are absent while lifecycle/query/subscription/control/static helpers remain and private load_plugins is positively constrained to its four owner-test exceptions.", "Phase 4 formatting, crate/workspace, source-audit, and diff gates pass without Stage 10 or Phase 5 scope."]
  artifacts: ["Owner canonical navigation tests", "Complete retained and absent ledgers", "Updated roadmap and validation mapping"]
  key_links: ["BranchSummary/ForkSession outcomes -> one receiver and replay-authoritative assertions", "Final receiver-aware audit -> deletion and retained API proof"]
---
<objective>Per D-05 through D-08 and D-11, finish navigation/summary convergence, delete `fork_current_session`, `summarize_branch`, and zero-caller `summarize_branch_for_navigation`, and run the complete Phase 4 closure gate while positively enforcing the TEST-04 private load_plugins exception. Security acceptance is ASVS Level 1: all applicable L1 controls are addressed; every high threat has concrete mitigation and automated verification; no high threat remains open.</objective>
<execution_context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-CONTEXT.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-RESEARCH.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-PATTERNS.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-03-SUMMARY.md</execution_context>
<tasks>
<done>Each task's acceptance criteria are the completion conditions for this plan.</done>
<task type="auto"><name>Task 1: Converge owner navigation and summary tests</name><files>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/tests/public_api.rs</files><read_first>04-CONTEXT.md D-01 through D-08 and D-11 through D-15; 04-RESEARCH.md G4 inventory and navigation pitfalls; 04-PATTERNS.md receiver continuity and exact outcome rules; current owner/public summary/fork tests.</read_first><action>Use `BranchSummary`, `ForkSession`, or other canonical operations with exact outcomes for remaining owner/public workflow tests. Preserve one-receiver event continuity, source/target owner identity, session counts, snapshot/projection refresh, no-append and partial-commit behavior, replay/reopen facts, and persistent navigation assertions. Use a direct owner-private path only for a documented D-03 custom metadata/option/fault case, and retain static repository/lifecycle/query/control helpers as separate contracts.</action><acceptance_criteria>No G4 test calls a replaced broad session workflow method; navigation and summary preserve event, replay, target, snapshot, owner, persistence, and error evidence; no API widening or operation-running helper exists.</acceptance_criteria><verify><automated>cargo test -p pi-coding-agent --lib --test public_api -- --nocapture</automated></verify></task>
<task type="auto"><name>Task 2: Delete final methods and run closure gates</name><files>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs, crates/pi-coding-agent/tests/api_boundary_guards.rs, .planning/phases/04-test-convergence-and-compatibility-deletion/04-VALIDATION.md, .planning/ROADMAP.md</files><read_first>04-RESEARCH.md complete deletion inventory/final validation map; 04-PATTERNS.md final ledger rules; current owner and boundary files after G1-G3.</read_first><action>Per D-06 through D-08, audit every replaced method receiver-aware, classify load_plugins custom-option calls, run affected tests, both guards, and crate check, then delete remaining replaced definitions. The dedicated final guard test must enumerate every replaced method and verify, by receiver-aware AST/source classification, zero old method definitions, zero receiver calls, zero local `#[allow(deprecated)]` suppressions, and zero synonymous compatibility wrappers; it must also positively verify retained lifecycle/construction/open/resume, query/list/snapshot, subscription/event, control, and static repository/export/fork/clone/hydration APIs. Explicitly retain run, construction/open/resume, list, static export/fork/clone/hydration/tree helpers, snapshots, queries, subscriptions, controls, and plugin UI/query helpers. Make absent checks reject definitions, calls, local compatibility suppressions, and synonyms without global bare-name bans. Finish cargo fmt/check/test for crate and workspace, source audits, git diff check, and update ROADMAP/VALIDATION; leave parser-complete Phase 5 and Stage 10 event work deferred. The ASVS Level 1 closure rule is all applicable L1 controls addressed, every high threat mitigated and automatically verified, and no high threat open.</action><acceptance_criteria>All replaced broad methods are absent or explicitly justified only as narrow custom owner paths; the final guard has executable coverage for definitions, receiver calls, local deprecation suppressions, synonyms, and retained API positives; callers are canonical or documented exceptions; retained APIs compile; all focused/full gates pass and planning artifacts accurately map four plans; no ASVS L1 high threat remains open.</acceptance_criteria><verify><automated>cargo test -p pi-coding-agent --test product_runtime_boundary_guards final_receiver_aware_compatibility_absence_and_retained_api_guard -- --exact && cargo fmt --check && cargo test -p pi-coding-agent && cargo check -p pi-coding-agent && cargo test --workspace && cargo check --workspace && cargo test -p pi-coding-agent --test product_runtime_boundary_guards --test api_boundary_guards -- --nocapture && git diff --check</automated></verify><done>All remaining replaced methods have receiver-aware zero-caller evidence and are deleted; the dedicated guard proves every absence and retained-positive category, all workspace closure gates pass, and ASVS L1 has no open high-severity threat.</done></task>
<resolved_ledger>
Task 2 MUST apply the resolved RESEARCH ledger as part of its action and acceptance criteria. For DELETE-01, the final absent-definition, receiver-aware call, local-suppression, and synonymous-wrapper ledgers contain all 16 replaced methods and explicitly include `summarize_branch_for_navigation`; prove it has zero callers, then delete it together with `fork_current_session` and `summarize_branch`. For TEST-04, `load_plugins(PluginLoadOptions)` is not broad compatibility and MUST remain owner-private with exactly four justified calls in co-located `coding_session` tests whose explicit candidates/registries cannot be represented by public `PluginLoad`. The positive guard MUST require that private definition and those four D-03 exceptions, while rejecting public export, integration-helper use, generic fault selectors, replacement wrappers, and non-test callers. Task 2 is not done unless both ledgers pass independently before the complete closure command.
</resolved_ledger>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| navigation tests -> durable session | Persistent transitions, events, and replay are authoritative. |
| source audits -> API ledger | Audits distinguish legitimate receivers from replaced owner methods. |
| final gates -> release boundary | Workspace checks establish closure evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-16 | Elevation of Privilege | navigation tests | high | mitigate | Require canonical BranchSummary/ForkSession except documented D-03 cases. |
| T-04-17 | Repudiation/Tampering | navigation durability | high | mitigate | Preserve receiver continuity, IDs, replay, targets, snapshots, and PartialCommit. |
| T-04-18 | Tampering | final ledger | high | mitigate | Receiver-aware absent definitions/calls/synonyms and positive retained ledger. |
| T-04-19 | Denial of Service | over-broad deletion | high | mitigate | Compile-test explicit retained APIs and exclude legitimate service/UI/static receivers. |
| T-04-20 | Elevation of Privilege | wrapper recreation | high | mitigate | Reject renamed facades and local compatibility suppressions. |
| T-04-21 | Repudiation | final verification | medium | mitigate | Require crate/workspace tests, checks, source audits, formatting, and diff hygiene. |
</threat_model>
<verification>Run all task gates, the dedicated receiver-aware final audit covering definitions, receiver calls, local deprecation suppressions, synonyms, and retained lifecycle/query/subscription/control/static APIs, formatting, focused/full crate/workspace tests and checks, and git diff hygiene. Enforce ASVS Level 1: all applicable controls addressed, every high threat concretely mitigated with automated evidence, and no high threat open.</verification>
<success_criteria>All 16 replaced broad methods, including summarize_branch_for_navigation, are gone after migration; retained contracts survive; private load_plugins has exactly four justified owner-test callers and no broader exposure; TEST-04 and DELETE-01 are independently and unambiguously verified.</success_criteria>
<output>Create `.planning/phases/04-test-convergence-and-compatibility-deletion/04-04-SUMMARY.md`.</output>
