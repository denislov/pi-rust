---
phase: 04-test-convergence-and-compatibility-deletion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/tests/agent_invocation.rs, crates/pi-coding-agent/tests/agent_team_flow.rs, crates/pi-coding-agent/tests/delegation_execution.rs, crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs, .planning/phases/04-test-convergence-and-compatibility-deletion/04-VALIDATION.md]
autonomous: true
requirements: [TEST-01, TEST-02, DELETE-01, DELETE-02, DELETE-03, DELETE-04]
must_haves:
  truths: ["Agent, team, and export tests use run with typed outcomes and preserve behavior assertions.", "G1 methods have zero receiver-aware definitions or calls while retained lifecycle/query/control/static helpers remain.", "No operation-running helper or renamed G1 wrapper exists."]
  artifacts: ["Canonical agent/team/export integration tests", "Receiver-aware retained and absent method ledgers"]
  key_links: ["Public operations -> CodingAgentSession::run -> exact outcome extraction", "Zero-caller audit -> focused gates -> G1 definition deletion"]
---
<objective>Per D-01, D-02, and D-05 through D-08, migrate agent, team, and export owner/integration callers through the canonical facade, prove zero callers, then delete `invoke_agent`, `invoke_team`, `export_current`, and `export_current_html`. Security acceptance is ASVS Level 1: every applicable L1 control is addressed, every high threat has concrete mitigation and automated verification, and no high threat remains open.</objective>
<execution_context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-CONTEXT.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-RESEARCH.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-PATTERNS.md</execution_context>
<tasks>
<done>Each task's acceptance criteria are the completion conditions for this plan.</done>
<task type="auto"><name>Task 1: Migrate G1 behavior tests</name><files>crates/pi-coding-agent/tests/agent_invocation.rs, crates/pi-coding-agent/tests/agent_team_flow.rs, crates/pi-coding-agent/tests/delegation_execution.rs, crates/pi-coding-agent/src/coding_session/mod.rs</files><read_first>04-CONTEXT.md D-01 through D-04 and D-12 through D-15; 04-RESEARCH.md G1 inventory; 04-PATTERNS.md canonical outcome extraction; current affected test bodies and owner callers.</read_first><action>Construct the visible public `CodingAgentOperation` at each workflow test and call `session.run(...).await`, matching the exact outcome variant. Preserve profile validation, team ordering, output, events, replay, operation IDs, errors, and export path/view assertions. Keep an owner-private path only for a documented D-03 custom option, metadata, or fault boundary; do not add a session-running helper or widen `api`.</action><acceptance_criteria>All G1 workflow tests use canonical operations or documented owner-private exceptions, and no substantive behavior or durability assertion is removed.</acceptance_criteria><verify><automated>cargo test -p pi-coding-agent --test agent_invocation --test agent_team_flow --test delegation_execution -- --nocapture</automated></verify></task>
<task type="auto"><name>Task 2: Prove zero callers and delete G1 methods</name><files>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs, .planning/phases/04-test-convergence-and-compatibility-deletion/04-VALIDATION.md</files><read_first>04-RESEARCH.md deletion proof and retained ledger; 04-PATTERNS.md receiver-aware absent ledger; current product boundary guard.</read_first><action>Per D-06 through D-08, add receiver-aware zero-definition/zero-call checks for G1 while excluding legitimate distinct receivers. Run focused suites, both boundary suites, and `cargo check -p pi-coding-agent`; only then delete the four definitions. Assert absent old calls and synonym wrappers while retaining construction/open/resume, snapshots, queries, subscriptions, controls, and static helpers. Migrate failures instead of restoring or renaming wrappers.</action><acceptance_criteria>G1 definitions and compatibility calls are absent, retained APIs remain guarded present, and focused tests, boundary guards, and crate check pass after deletion.</acceptance_criteria><verify><automated>cargo test -p pi-coding-agent --test agent_invocation --test agent_team_flow --test delegation_execution --test product_runtime_boundary_guards --test api_boundary_guards -- --nocapture && cargo check -p pi-coding-agent</automated></verify><done>The four G1 definitions have been deleted only after receiver-aware zero-caller evidence; old calls and synonyms fail the guard while retained APIs pass.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| tests -> public API | Workflow tests cross the admitted canonical operation boundary. |
| source guard -> repository | Receiver-aware checks classify legitimate same-named methods. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Elevation of Privilege | G1 tests | high | mitigate | Require typed public operations and documented owner exceptions. |
| T-04-02 | Repudiation/Tampering | G1 assertions | high | mitigate | Retain event, replay, ID, ordering, output, and export evidence. |
| T-04-03 | Elevation of Privilege | test helpers | high | mitigate | Reject operation-running or generic fault helpers. |
| T-04-04 | Tampering | deletion ledger | high | mitigate | Check definitions, receiver calls, and synonyms before/after deletion. |
| T-04-05 | Denial of Service | retained ledger | medium | mitigate | Positively guard lifecycle/query/control/static helpers. |
</threat_model>
<verification>Run focused G1 tests, both boundary targets, receiver-aware source audit, crate check, and `git diff --check`. ASVS L1 acceptance requires all applicable controls addressed, automated evidence for T-04-01 through T-04-04, and zero open high-severity threats.</verification>
<success_criteria>G1 tests prove canonical behavior and four compatibility methods are deleted only after zero-caller proof.</success_criteria>
<output>Create `.planning/phases/04-test-convergence-and-compatibility-deletion/04-01-SUMMARY.md`.</output>
