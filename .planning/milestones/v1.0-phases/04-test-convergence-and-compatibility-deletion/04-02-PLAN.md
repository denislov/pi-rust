---
phase: 04-test-convergence-and-compatibility-deletion
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/tests/agent_profile_runtime.rs, crates/pi-coding-agent/tests/agent_profile_session.rs, crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/tests/support/mod.rs, crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs, crates/pi-coding-agent/tests/api_boundary_guards.rs, .planning/phases/04-test-convergence-and-compatibility-deletion/04-VALIDATION.md]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, DELETE-01, DELETE-02, DELETE-03, DELETE-04]
must_haves:
  truths: ["Prompt, profile, self-healing, and public API tests use run and preserve runtime/persistence/error assertions.", "Shared helpers only extract typed outcomes and never run operations or own sessions.", "reload_plugins and the other G2 broad methods are absent; load_plugins(PluginLoadOptions) remains private solely for exactly four documented co-located owner-test calls whose candidates/registries public PluginLoad cannot express."]
  artifacts: ["Canonical public API/profile tests", "Pure outcome extractors only where needed", "G2 absence and retained ledgers"]
  key_links: ["Public tests -> exact CodingAgentOperationOutcome", "Outcome-only helper -> test assertions without execution authority", "Four co-located owner tests -> private load_plugins custom-option path; no public export, integration helper, generic fault selector, wrapper, or non-test caller"]
---
<objective>Per D-01 through D-04 and D-09 through D-15, migrate prompt/profile/self-heal/public API tests, classify owner plugin option calls, and delete G2 compatibility methods. Security acceptance is ASVS Level 1: every applicable L1 control is addressed, every high threat has concrete mitigation and automated verification, and no high threat remains open.</objective>
<execution_context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-CONTEXT.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-RESEARCH.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-PATTERNS.md
@.planning/phases/04-test-convergence-and-compatibility-deletion/04-01-PLAN.md</execution_context>
<tasks>
<done>Each task's acceptance criteria are the completion conditions for this plan.</done>
<task type="auto"><name>Task 1: Migrate G2 tests and constrain helpers</name><files>crates/pi-coding-agent/tests/agent_profile_runtime.rs, crates/pi-coding-agent/tests/agent_profile_session.rs, crates/pi-coding-agent/tests/public_api.rs, crates/pi-coding-agent/tests/support/mod.rs, crates/pi-coding-agent/src/coding_session/mod.rs</files><read_first>04-CONTEXT.md D-01 through D-04 and D-12 through D-15; 04-RESEARCH.md G2 inventory and resolved load_plugins classification; 04-PATTERNS.md exact outcome and pure extractor shapes; current G2 tests/callers.</read_first><action>Use visible `CodingAgentOperation` values with `run(...).await` and exact Prompt, SetDefaultAgentProfile, SelfHealingEdit, or related outcome matches. Preserve provider/tool/profile behavior, persisted/reopened state, events, replay, errors, and self-healing assertions. Replace old public API compile calls with canonical positive contracts. Any support helper must accept an outcome and return a typed payload only. Per D-03/D-04 and D-12 through D-15, retain private `load_plugins(PluginLoadOptions)` solely for the four existing co-located owner-test calls that supply explicit candidates/registries unavailable through public `PluginLoad`; put a specific insufficiency justification at each call. Require exactly those four owner-test calls and forbid public export, integration-helper use, generic fault selection, replacement wrappers, and every non-test caller. Migrate every other plugin workflow call through public `PluginLoad`.</action><acceptance_criteria>G2 tests use canonical operations except exactly four documented co-located owner-test calls to private load_plugins; public API tests no longer compile removed methods; helpers do not execute workflows or own runtime state; no public/helper/wrapper/non-test path reaches load_plugins; substantive assertions remain.</acceptance_criteria><verify><automated>cargo test -p pi-coding-agent --lib --test agent_profile_runtime --test agent_profile_session --test public_api -- --nocapture</automated></verify></task>
<task type="auto"><name>Task 2: Delete G2 methods after caller proof</name><files>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs, crates/pi-coding-agent/tests/api_boundary_guards.rs, .planning/phases/04-test-convergence-and-compatibility-deletion/04-VALIDATION.md</files><read_first>04-RESEARCH.md G2 deletion targets and resolved private exception; 04-PATTERNS.md retained/absent ledger; current guards after 04-01.</read_first><action>Per D-06/D-07, prove receiver-aware zero callers for prompt, compact, self_healing_edit variants, set_default_agent_profile_id, reload_plugins, and run_plugin_command. Run affected tests, both boundary targets, and crate check, then delete those definitions, including reload_plugins. Reject old calls and synonymous wrappers and preserve retained APIs. Add a separate positive TEST-04 guard for private load_plugins that requires exactly four calls, all inside the co-located `coding_session` test region with D-03 insufficiency justifications, and rejects public re-export, integration-helper use, generic fault selection, any replacement wrapper, and any non-test caller; do not place load_plugins in the DELETE-01 absent-method ledger. Migrate compiler/guard failures; never restore or rename deleted methods.</action><acceptance_criteria>All G2 deletion targets, including reload_plugins, are absent with no compatibility calls/synonyms; load_plugins is not misclassified as broad compatibility and is positively constrained to exactly four justified owner-test calls; public API, helper, non-test, and test-seam guards pass.</acceptance_criteria><verify><automated>cargo test -p pi-coding-agent --lib --test agent_profile_runtime --test agent_profile_session --test public_api --test product_runtime_boundary_guards --test api_boundary_guards -- --nocapture && cargo check -p pi-coding-agent</automated></verify><done>All listed G2 broad methods have zero receiver-aware callers before deletion and are absent afterward; the retained private load_plugins exception has exactly four justified owner-test calls and no public, helper, wrapper, generic-fault, or non-test exposure.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| public tests -> stable API | External-style tests use admitted operations. |
| owner tests -> cfg(test) internals | White-box tests cross only justified custom/fault boundaries. |
| helper -> runtime | Shared helpers must not gain execution authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-06 | Elevation of Privilege | public tests | high | mitigate | Require canonical operation construction and API boundary checks. |
| T-04-07 | Repudiation/Tampering | profile/self-heal tests | high | mitigate | Preserve state, replay, event, provider/tool, and exact error assertions. |
| T-04-08 | Elevation of Privilege | helpers | high | mitigate | Enforce outcome-only helper shape. |
| T-04-09 | Tampering | G2 deletion | high | mitigate | Zero-caller proof and receiver-aware absence checks. |
| T-04-10 | Denial of Service | retained APIs | medium | mitigate | Positive retained contract ledger. |
</threat_model>
<verification>Run focused G2 tests before/after deletion, both guards, receiver-aware audit, crate check, and diff check. ASVS L1 acceptance requires all applicable controls addressed, automated evidence for T-04-06 through T-04-09, and zero open high-severity threats.</verification>
<success_criteria>G2 workflows are tested through run, helpers remain pure, G2 compatibility methods including reload_plugins are absent without shims, and private load_plugins is retained only under the exact four-call TEST-04 owner-test guard.</success_criteria>
<output>Create `.planning/phases/04-test-convergence-and-compatibility-deletion/04-02-SUMMARY.md`.</output>
