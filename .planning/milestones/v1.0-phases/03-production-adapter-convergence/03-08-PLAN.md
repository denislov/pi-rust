---
phase: 03-production-adapter-convergence
plan: 08
type: execute
wave: 8
depends_on:
  - 03-07
files_modified:
  - crates/pi-coding-agent/src/error.rs
  - crates/pi-coding-agent/src/coding_session/error.rs
  - crates/pi-coding-agent/src/coding_session/mod.rs
  - crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs
autonomous: true
gap_closure: true
requirements:
  - INTER-01
  - INTER-02
  - INTER-03
  - INTER-04
must_haves:
  truths:
    - "A CodingSessionError::PartialCommit crossing the adapter boundary remains explicitly classified as a partial commit and retains the exact durable operation_id and message."
    - "Interactive crate unit tests can arm only the existing persistent-session AppendEvents and UpdateManifest fault points through a cfg(test), pub(crate) CodingAgentSession bridge."
    - "Interactive crate unit tests can create one real durable pending delegation fixture through a specialized cfg(test) owner method, so rejection PartialCommit coverage does not fabricate queue internals."
    - "The test bridge is absent from production builds, absent from pi_coding_agent::api, and rejected by the closed session-method ledger if its visibility or test gate drifts."
  artifacts:
    - path: crates/pi-coding-agent/src/error.rs
      provides: "Adapter-visible CliError variant carrying PartialCommit operation identity."
    - path: crates/pi-coding-agent/src/coding_session/error.rs
      provides: "Lossless CodingSessionError to CliError conversion and focused contract tests."
    - path: crates/pi-coding-agent/src/coding_session/mod.rs
      provides: "Narrow owner-local cfg(test) fault-arm methods plus a durable pending-delegation fixture method."
    - path: crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs
      provides: "Structural enforcement that the new helpers remain test-only and outside the stable facade."
  key_links:
    - from: "RejectDelegation CodingSessionError::PartialCommit"
      to: "PromptTaskFailure.error"
      via: "From<CodingSessionError> for CliError preserves operation_id, message, classification, and canonical Display text for a task-level Err"
    - from: "interactive loop unit tests"
      to: "SessionLogStore failure state"
      via: "cfg(test) pub(crate) CodingAgentSession methods select AppendEvents or UpdateManifest without exporting StoreFailurePoint"
---

<objective>
Repair the Phase 3 error and test-seam contracts required by strict interactive failure verification.

Purpose: Remove the lossy PartialCommit conversion before any runner-level assertion is written, then expose the existing deterministic store faults and one real pending-delegation fixture through the narrowest crate-visible test-only owner boundary.
Output: A typed CliError partial-commit variant, lossless conversion tests, two specialized test-only fault-arm methods, one specialized pending-delegation fixture method, and updated boundary guards.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-adapter-convergence/03-CONTEXT.md
@.planning/phases/03-production-adapter-convergence/03-UAT.md
@.planning/debug/phase-03-real-interactive-operation-failure-owner-continuity.md
@.planning/debug/phase-03-real-fork-session-failure-continuity.md
@.planning/phases/03-production-adapter-convergence/03-07-SUMMARY.md
@crates/pi-coding-agent/src/error.rs
@crates/pi-coding-agent/src/coding_session/error.rs
@crates/pi-coding-agent/src/coding_session/mod.rs
@crates/pi-coding-agent/src/coding_session/session_service.rs
@crates/pi-coding-agent/src/coding_session/session_log/store.rs
@crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs

<interfaces>
- `CodingSessionError::PartialCommit { operation_id: String, message: String }` has stable code `partial_commit` and canonical Display text containing both fields.
- `CliError` currently derives `Debug`, `Clone`, `PartialEq`, `Eq`, and `thiserror::Error`; task-level operation `Err` values are converted with `map_err(CliError::from)`, while prompt finalization failures remain inside `PromptTurnOutcome::Failed`.
- `SessionService::fail_store_after_for_tests(StoreFailurePoint, usize)` and `StoreFailurePoint::{AppendEvents, UpdateManifest}` already exist under `#[cfg(test)]` and must remain private to `coding_session` internals.
- Existing coding-session unit fixtures persist a pending delegation through `DelegationConfirmationService::queue_pending(..., true)` before testing rejection UpdateManifest failure; the interactive module needs the same real durable setup without access to the service or queue types.
- `product_runtime_boundary_guards::canonical_operation_facade_has_no_new_workflow_wrappers` owns the exact public/pub(crate) CodingAgentSession method ledger, including test-only helper classification.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve PartialCommit identity at the CliError boundary</name>
  <files>crates/pi-coding-agent/src/error.rs, crates/pi-coding-agent/src/coding_session/error.rs</files>
  <behavior>
    - "Converting PartialCommit with operation_id op_123 and message manifest failed produces a distinct CliError partial-commit variant containing those exact values."
    - "The converted error renders the canonical partial-commit text, including the operation ID, rather than rendering only the storage message."
    - "Existing non-partial CodingSessionError mappings retain their current CliError variants and visible strings."
  </behavior>
  <action>
RED: Add focused conversion tests in `coding_session/error.rs` named `partial_commit_conversion_preserves_operation_identity` and `non_partial_conversion_contract_remains_unchanged`. The first must match the converted `CliError` structurally, compare both owned fields, compare its Display output with the source `CodingSessionError`, and fail against the current lossy conversion. GREEN: add a dedicated structured `CliError` variant in `error.rs` and map only `CodingSessionError::PartialCommit` to it; leave the grouping and strings of all other error cases unchanged. Audit direct exhaustive matches on `CliError` and update only those made non-exhaustive by the new variant. Do not encode the operation ID into an untyped SessionFailure string, because the UAT requires durable classification and identity to survive the task channel.
  </action>
  <verify>
    <automated>cargo test -p pi-coding-agent --lib coding_session::error::tests::partial_commit_conversion_preserves_operation_identity -- --exact --nocapture &amp;&amp; cargo test -p pi-coding-agent --lib coding_session::error::tests::non_partial_conversion_contract_remains_unchanged -- --exact --nocapture</automated>
  </verify>
  <done>`CliError` carries an explicit PartialCommit classification with the exact canonical operation ID/message, while every other conversion remains behavior-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose deterministic store and pending-delegation setup through a test-only owner bridge</name>
  <files>crates/pi-coding-agent/src/coding_session/mod.rs, crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs</files>
  <behavior>
    - "A persistent CodingAgentSession can arm the existing next AppendEvents failure from a sibling crate unit-test module without importing StoreFailurePoint."
    - "A persistent CodingAgentSession can arm the existing next UpdateManifest failure from a sibling crate unit-test module without importing SessionService or SessionLogStore."
    - "A persistent CodingAgentSession can queue a real pending delegation identified by operation_id and tool_call_id, persist it, and reopen with the same pending item before the rejection fault is armed."
    - "All three bridge methods are pub(crate), directly cfg(test)-gated, absent from the stable api facade, and classified as test-only by the closed method ledger."
  </behavior>
  <action>
RED: Extend `session_store_failure_controls_remain_test_only` and the CodingAgentSession method expectation ledger with three action-specific helper names. Add `coding_session::tests::interactive_store_and_pending_delegation_bridge_arms_real_fixtures`, creating persistent tempfile owners to prove both one-shot store helpers reach the existing injector and the delegation helper writes a real pending confirmation that survives reopen. GREEN: add two `#[cfg(test)] pub(crate)` methods on `CodingAgentSession`, one for AppendEvents and one for UpdateManifest, each taking only the successful-call count and delegating to the existing persistent `SessionService::fail_store_after_for_tests`. Add one `#[cfg(test)] pub(crate)` pending-delegation fixture method taking only operation and tool-call IDs; construct the same default-agent pending state used by existing coding-session tests and persist it through the real delegation confirmation service with `persist=true`. Keep `StoreFailurePoint`, SessionService, SessionLogStore, pending state, queue, and service types inside `coding_session`; do not export a generic fault selector, generic queue mutation API, or any production hook. Non-persistent misuse may fail loudly as a broken fixture.
  </action>
  <verify>
    <automated>cargo test -p pi-coding-agent --lib coding_session::tests::interactive_store_and_pending_delegation_bridge_arms_real_fixtures -- --exact --nocapture &amp;&amp; cargo test -p pi-coding-agent --test product_runtime_boundary_guards session_store_failure_controls_remain_test_only -- --exact --nocapture &amp;&amp; cargo test -p pi-coding-agent --test product_runtime_boundary_guards canonical_operation_facade_has_no_new_workflow_wrappers -- --exact --nocapture</automated>
  </verify>
  <done>Interactive crate unit tests have exactly two deterministic persistence fault controls and one real pending-delegation fixture method, and structural guards prove none can become production or public API surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical runtime error -> adapter error | Durable uncertainty crosses into CLI/TUI ownership and must remain attributable to one operation. |
| Test module -> persistent session store | Fault injection is privileged test behavior that must be impossible in production builds and public embeddings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-31 | Repudiation | `From&lt;CodingSessionError&gt; for CliError` | high | mitigate | Preserve the structured PartialCommit operation ID/message and assert structural equality plus canonical Display output. |
| T-03-32 | Tampering / Elevation of Privilege | CodingAgentSession test bridge | high | mitigate | Use only directly cfg(test)-gated pub(crate) specialized methods, persist pending state through the real service, and enforce every helper in the store-control and closed-method source guards. |
| T-03-33 | Information Disclosure | stable `pi_coding_agent::api` | low | accept | The helpers and internal fault-point type remain absent from the stable facade; compiler/source guards make accidental exposure detectable. |
</threat_model>

<verification>
Run the two RED tests before implementation, then the focused GREEN commands above. Finish with `cargo fmt --check`, `cargo test -p pi-coding-agent --lib coding_session::error::tests -- --nocapture`, the two named boundary guards, `cargo check -p pi-coding-agent`, and `git diff --check` before handing the bridge to Plan 03-09.
</verification>

<success_criteria>
- PartialCommit classification, operation ID, message, and canonical visible text survive `CliError::from`.
- The only new test controls are two specialized cfg(test) store-fault methods and one specialized durable pending-delegation fixture method on CodingAgentSession.
- The stable facade, dependency direction, production runtime behavior, and Stage 10 event contract are unchanged.
- Focused unit and boundary tests, formatting, crate check, and diff check pass.
</success_criteria>

<output>
Create `.planning/phases/03-production-adapter-convergence/03-08-SUMMARY.md` when done.
</output>
