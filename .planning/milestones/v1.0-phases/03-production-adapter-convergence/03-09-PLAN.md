---
phase: 03-production-adapter-convergence
plan: 09
type: execute
wave: 9
depends_on:
  - 03-08
files_modified:
  - crates/pi-coding-agent/src/interactive/loop.rs
autonomous: true
gap_closure: true
requirements:
  - INTER-01
  - INTER-02
  - INTER-03
  - INTER-04
must_haves:
  truths:
    - "Real profile and pending delegation-rejection task failures traverse PromptTask spawning, the actual done channel, and finish_prompt; each restores the acquired owner before finish_prompt projects the exact task-level error and leaves the old target unchanged."
    - "A real prompt finalization PartialCommit remains inside Completed(Coding(PromptTurnOutcome::Failed)); its operation_id matches the durable transaction, its failure ProductEvent follows the existing pre-finish projection path, and the completed owner remains usable after finish_prompt."
    - "A real ForkSession AppendEvents failure keeps the source owner, pre-task subscriber, old target, and source session count; no replacement owner or target session survives."
    - "After failed fork restoration, a canonical profile mutation succeeds and the receiver created before task spawn observes its ProductEvent from the same EventService."
  artifacts:
    - path: crates/pi-coding-agent/src/interactive/loop.rs
      provides: "Co-located async unit tests for real PromptTask channel failures, finish_prompt ordering, durable error identity, fork source continuity, and post-restoration usability."
  key_links:
    - from: "PromptTask::spawn_*"
      to: "finish_prompt"
      via: "await the real `task.done` oneshot and pass its PromptTaskCompletion without constructing a fabricated failure envelope"
    - from: "prompt finalization PartialCommit"
      to: "durable events.jsonl operation_id"
      via: "compare PromptTurnOutcome::Failed.error operation_id with the replay-authoritative appended operation transaction while retaining Completed(Coding(...))"
    - from: "pending rejection PartialCommit"
      to: "finish_prompt AgentError projection"
      via: "the real runner maps the task-level Err to structured CliError, PromptTaskFailure carries the owner, and finish_prompt restores before applying AgentError"
    - from: "pre-fork ProductEventReceiver"
      to: "post-restoration canonical mutation"
      via: "the failed fork never swaps CodingAgentSession.event_service, so the original receiver observes DefaultAgentProfileChanged"
---

<objective>
Close both diagnosed Phase 3 UAT gaps with deterministic real-runner tests at the private interactive boundary.

Purpose: Replace fabricated completion evidence with actual spawned tasks, channels, canonical operation failures, finish_prompt restoration, durable identity checks, and post-failure session use.
Output: Focused loop unit tests for profile/rejection/prompt failure continuity and ForkSession failure continuity, followed by the complete Phase 3 gap verification gate.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-production-adapter-convergence/03-CONTEXT.md
@.planning/phases/03-production-adapter-convergence/03-RESEARCH.md
@.planning/phases/03-production-adapter-convergence/03-UAT.md
@.planning/debug/phase-03-real-interactive-operation-failure-owner-continuity.md
@.planning/debug/phase-03-real-fork-session-failure-continuity.md
@.planning/phases/03-production-adapter-convergence/03-07-PLAN.md
@.planning/phases/03-production-adapter-convergence/03-07-SUMMARY.md
@.planning/phases/03-production-adapter-convergence/03-08-SUMMARY.md
@crates/pi-coding-agent/src/interactive/prompt_task.rs
@crates/pi-coding-agent/src/interactive/loop.rs
@crates/pi-coding-agent/src/coding_session/mod.rs
@crates/pi-coding-agent/src/coding_session/error.rs
@crates/pi-coding-agent/src/error.rs

<interfaces>
- `PromptTask::{spawn_prompt, spawn_set_default_agent_profile, spawn_delegation_rejection, spawn_fork_session}` return a task whose `done` field is the actual oneshot receiver and whose event receiver is unbounded.
- `PromptTaskCompletion::Failed(PromptTaskFailure { session, error })` owns the acquired live session for task-level `Err` values; `finish_prompt` assigns that session before applying `UiEvent::AgentError` and does not change `session_target` in the failure arm.
- Prompt finalization errors are behaviorally different: `session.run(Prompt)` succeeds with `CodingAgentOperationOutcome::Prompt(PromptTurnOutcome::Failed { error, .. })`, so the task completes as `Completed(Coding(...))`; the failure ProductEvent is forwarded through `task.events` before `finish_prompt` handles the completed outcome.
- Plan 03-08 provides cfg(test), pub(crate) owner methods for the next AppendEvents and UpdateManifest store failure, a real durable pending-delegation fixture method, and a structured `CliError` PartialCommit variant for task-level conversions.
- `CodingAgentSession::subscribe_product_events()` returns a receiver tied to the owner's EventService, and `run(SetDefaultAgentProfile)` emits `CodingAgentEvent::DefaultAgentProfileChanged` after persistence succeeds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive real task failures and prompt failed outcomes through their distinct completion contracts</name>
  <files>crates/pi-coding-agent/src/interactive/loop.rs</files>
  <behavior>
    - "A real SetDefaultAgentProfile UpdateManifest failure returns the source owner through PromptTask.done, keeps the old session target, projects the exact storage error after restoration, and permits PluginLoad afterward."
    - "A real pending RejectDelegation UpdateManifest failure returns structured CliError::PartialCommit through PromptTaskCompletion::Failed; operation_id/message remain exact, finish_prompt restores before AgentError projection, and PluginLoad succeeds afterward."
    - "A real Prompt commit with an injected UpdateManifest failure returns PromptTaskCompletion::Completed(Coding(...)) containing PromptTurnOutcome::Failed with CodingSessionError::PartialCommit; its operation_id equals the durable operation ID, the existing failure ProductEvent is available before finish_prompt, and PluginLoad succeeds on the restored completed owner."
  </behavior>
  <action>
Replace the existing fabricated four-label test with three async tests named `real_profile_failure_restores_owner_through_prompt_task_done`, `real_rejection_partial_commit_restores_owner_through_prompt_task_done`, and `real_prompt_partial_commit_returns_completed_failed_outcome_through_prompt_task_done`. Add small private helpers only inside the existing `loop.rs` test module for persistent tempfile session creation, PromptRunOptions/faux-provider setup, awaiting the real `done` receiver, draining task events, and checking durable operation IDs without duplicating production orchestration. For profile, arm UpdateManifest and call `PromptTask::spawn_set_default_agent_profile`; assert the Failed completion's owner/error before passing it to `finish_prompt`. For rejection, first use the Plan 03-08 owner fixture method to persist a genuine pending confirmation, then arm UpdateManifest and call `PromptTask::spawn_delegation_rejection` with the same operation/tool IDs. Match `PromptTaskCompletion::Failed` and structured `CliError::PartialCommit`, compare exact operation_id/message to the durable rejection transaction, then prove `finish_prompt` restores the owner before adding the matching AgentError. For prompt, register a deterministic faux response, arm UpdateManifest so transaction events append before manifest failure, and call `PromptTask::spawn_prompt`. Require `PromptTaskCompletion::Completed(PromptTaskResult::Coding(...))` with `PromptTurnOutcome::Failed { error: CodingSessionError::PartialCommit, .. }`; compare its operation ID to the durable transaction and assert the task's failure ProductEvent is drained/projected through the existing event path before `finish_prompt`. Do not convert this prompt outcome into CliError or assert that finish_prompt performs its first error projection. For all three, retain a sentinel target, pass the untouched completion into `finish_prompt`, assert source owner/target continuity, and execute `CodingAgentOperation::PluginLoad` afterward. Do not change production prompt/event ordering or expose test controls through `api`.
  </action>
  <verify>
    <automated>cargo test -p pi-coding-agent --lib interactive::r#loop::tests::real_profile_failure_restores_owner_through_prompt_task_done -- --exact --nocapture &amp;&amp; cargo test -p pi-coding-agent --lib interactive::r#loop::tests::real_rejection_partial_commit_restores_owner_through_prompt_task_done -- --exact --nocapture &amp;&amp; cargo test -p pi-coding-agent --lib interactive::r#loop::tests::real_prompt_partial_commit_returns_completed_failed_outcome_through_prompt_task_done -- --exact --nocapture</automated>
  </verify>
  <done>UAT gap 1 is closed by factual tests for two task-level failures and one prompt outcome-level failure, including durable PartialCommit identity and a successful canonical operation after each owner return.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove real ForkSession failure preserves source owner and subscriber continuity</name>
  <files>crates/pi-coding-agent/src/interactive/loop.rs</files>
  <behavior>
    - "ForkSession with AppendEvents failing on the attempted target returns PromptTaskCompletion::Failed through the actual done channel and carries the source session owner."
    - "Before and after finish_prompt, the source session ID, old ResolvedSessionTarget, and count of Rust-native session directories remain unchanged; no replacement session survives cleanup."
    - "A ProductEvent receiver created before task spawn remains connected after restoration and observes DefaultAgentProfileChanged from a successful canonical mutation on the restored source owner."
  </behavior>
  <action>
Add `real_fork_failure_preserves_source_owner_subscriber_and_target_through_prompt_task_done` in the co-located `loop.rs` test module. Create a persistent tempfile source session, retain its session ID, sentinel old target, initial Rust-native session-directory count, and a product-event receiver obtained before moving the owner. Arm the existing AppendEvents fault through the Plan 03-08 bridge, start `PromptTask::spawn_fork_session` with that exact owner, and await `task.done`; do not call `run_coding_fork_session_task` directly and do not construct PromptTaskCompletion by hand. Assert the failure carries the source ID and exact injected error, then pass it to `finish_prompt` and prove restoration occurs with the target and session count unchanged. Run `SetDefaultAgentProfile` through `CodingAgentSession::run` on the restored owner and drain the pre-task receiver until it observes the matching `CodingAgentEvent::DefaultAgentProfileChanged`; also assert no failed target `SessionOpened` transition was emitted. Keep event ordering, task channel types, fork cleanup behavior, and production code untouched.
  </action>
  <verify>
    <automated>cargo test -p pi-coding-agent --lib interactive::r#loop::tests::real_fork_failure_preserves_source_owner_subscriber_and_target_through_prompt_task_done -- --exact --nocapture</automated>
  </verify>
  <done>UAT gap 2 is closed by a real fork task failure proving source owner, subscriber, target, session-count, exact-error, and post-restoration event continuity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spawned PromptTask -> main interactive loop | The only live mutable owner and its exact operation error cross an async oneshot boundary. |
| Source session store -> attempted fork store | Fork copies durable state into a new directory before swapping owner persistence; failed staging must not replace or duplicate the source. |
| EventService -> pre-task subscriber | Receiver identity must survive failed owner mutation and observe later canonical events without resubscription. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-34 | Tampering / Denial of Service | PromptTask completion channel | high | mitigate | Exercise actual spawn and done paths for all named failures, restore before projection, and run a subsequent canonical operation. |
| T-03-35 | Repudiation | rejection and prompt PartialCommit projection | high | mitigate | Compare rejection's typed CliError and prompt's PromptTurnOutcome error with their durable transaction IDs while asserting each distinct projection path. |
| T-03-36 | Tampering | ForkSession failed transition | high | mitigate | Assert source identity, old target, directory count, and absence of replacement SessionOpened state across finish_prompt. |
| T-03-37 | Denial of Service | pre-task event subscriber | medium | mitigate | Reuse the original receiver after restoration and require observation of a new canonical profile event. |
</threat_model>

<verification>
Run each focused RED test before relying on Plan 03-08 behavior, then make all four focused tests GREEN. Complete the gap with: `cargo fmt --check`; `cargo test -p pi-coding-agent --lib`; `cargo test -p pi-coding-agent --test interactive_mode --test interactive_sessions --test product_runtime_boundary_guards -- --nocapture`; focused guards `session_store_failure_controls_remain_test_only`, `canonical_operation_facade_has_no_new_workflow_wrappers`, and `production_interactive_uses_canonical_operations`; `cargo check -p pi-coding-agent`; `cargo test --workspace`; `cargo check --workspace`; and `git diff --check`.
</verification>

<source_coverage_audit>
| Source | ID/item | Plan coverage | Status | Notes |
|--------|---------|---------------|--------|-------|
| GOAL | Every first-party adapter uses `CodingAgentSession::run` without external-contract regression | 03-08 Tasks 1-2; 03-09 Tasks 1-2 | COVERED | Repairs exact error identity and proves the existing canonical interactive path under real failure. |
| REQ | ADAPT-01 through ADAPT-04 | Existing 03-01 | COVERED | JSON/print scope is already complete and unchanged by these gaps. |
| REQ | RPC-01 through RPC-04 | Existing 03-02 and 03-03 | COVERED | RPC scope is already complete and unchanged by these gaps. |
| REQ | INTER-01 | 03-09 Task 1 | COVERED | Real prompt failed outcome proves the completed background owner contract without changing event projection. |
| REQ | INTER-02 | 03-09 Task 1 | COVERED | Real profile failure and pending rejection PartialCommit preserve behavior and owner continuity. |
| REQ | INTER-03 | 03-09 Task 2 | COVERED | Real canonical fork failure preserves the source target and owner. |
| REQ | INTER-04 | 03-08 Task 1; 03-09 Tasks 1-2 | COVERED | Exact errors, subscriber continuity, event sequence ownership, and UI projection are asserted. |
| REQ | INTER-05 | Existing 03-06 and 03-07 guards | COVERED | No production adapter call changes are introduced. |
| RESEARCH | Preserve adapter-owned task/select/projection topology | 03-09 Tasks 1-2 | COVERED | Tests consume the current spawn/done/finish interfaces without orchestration redesign. |
| RESEARCH | Preserve operation IDs, explicit PartialCommit, and replay authority | 03-08 Task 1; 03-09 Task 1 | COVERED | Rejection uses typed CliError identity; prompt retains PromptTurnOutcome identity; both are compared with durable log authority. |
| RESEARCH | Use deterministic offline fixtures and owner-local test fault controls | 03-08 Task 2; 03-09 Tasks 1-2 | COVERED | Existing tempfile/faux/store infrastructure is reused with no dependency or production hook. |
| CONTEXT | D-01 through D-12 | Existing 03-01 through 03-04 | COVERED | Locked adapter ordering and lower-risk boundaries remain complete. |
| CONTEXT | D-13 | 03-09 Task 1 | COVERED | Mutation menus/projections remain unchanged while real failures are verified. |
| CONTEXT | D-14 and D-15 | 03-09 Task 2 | COVERED | Navigation remains the independently verified highest-risk boundary. |
| CONTEXT | D-16 and D-17 | 03-08 Task 2; 03-09 Tasks 1-2 | COVERED | Fault controls stay private/test-only; lifecycle, query, subscription, and control contracts remain separate. |
| CONTEXT | D-18 | 03-08 Task 1; 03-09 Tasks 1-2 | COVERED | Only task-level PartialCommit conversion changes; prompt Completed/failed-outcome and ProductEvent ordering remain intact and are tested explicitly. |
| CONTEXT | D-19 and D-20 | Both plans | COVERED | No broad method deletion and no replacement operation facade or production helper. |
</source_coverage_audit>

<success_criteria>
- All four real runner tests pass through actual PromptTask spawning and `done`, then through the appropriate Failed or Completed contract and `finish_prompt`.
- Profile and pending rejection task failures restore the same owner before finish_prompt projects the exact task error, preserve the old target, and allow another canonical operation.
- Prompt PartialCommit remains a Completed coding result containing PromptTurnOutcome::Failed, preserves its durable operation ID, keeps the existing pre-finish ProductEvent projection, and returns a usable owner.
- Fork failure preserves source owner, pre-task subscriber, target, session count, exact error, and post-restoration event usability.
- Formatting, focused crate tests/guards, workspace tests/checks, source audits, and diff checks pass with no Stage 10 or compatibility-deletion work pulled forward.
</success_criteria>

<output>
Create `.planning/phases/03-production-adapter-convergence/03-09-SUMMARY.md` when done.
</output>
