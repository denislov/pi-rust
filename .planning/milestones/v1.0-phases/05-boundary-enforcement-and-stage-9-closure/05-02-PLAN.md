---
phase: 05-boundary-enforcement-and-stage-9-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pi-coding-agent/tests/api_boundary_guards.rs
  - crates/pi-coding-agent/tests/public_api.rs
  - crates/pi-coding-agent/tests/fixtures
autonomous: true
requirements: [GUARD-03, GUARD-04]
must_haves:
  truths:
    - External consumers compile against every explicitly inventoried canonical operation, outcome, and support type.
    - External consumers cannot reach internal operations, dispatch metadata, services, plugin options/registries, or Flow contracts through api, crate-root, or doc-hidden paths.
    - "D-15: Compile-fail assertions verify the nature of each failure without binding the suite to complete rustc diagnostic text."
  artifacts:
    - crates/pi-coding-agent/tests/api_boundary_guards.rs
    - crates/pi-coding-agent/tests/public_api.rs
    - crates/pi-coding-agent/tests/fixtures
  key_links:
    - Compile-pass and compile-fail fixtures run offline and independently of production export generation.
---

<objective>
Prove the stable facade as a real external-consumer contract and strengthen compiler-visible privacy enforcement.

Purpose: Implement D-12 through D-16 while preserving Rust visibility and sealed-contract boundaries.
Output: Explicit positive API inventory plus categorized external compile-fail fixtures.
</objective>

<context>
@.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-CONTEXT.md
@.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-RESEARCH.md
@crates/pi-coding-agent/tests/api_boundary_guards.rs
@crates/pi-coding-agent/tests/public_api.rs
@crates/pi-coding-agent/src/lib.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add independent external facade compile fixtures</name>
  <read_first>crates/pi-coding-agent/tests/public_api.rs; crates/pi-coding-agent/tests/api_boundary_guards.rs; crates/pi-coding-agent/src/lib.rs; .planning/phases/05-boundary-enforcement-and-stage-9-closure/05-CONTEXT.md</read_first>
  <files>crates/pi-coding-agent/tests/public_api.rs; crates/pi-coding-agent/tests/api_boundary_guards.rs; crates/pi-coding-agent/tests/fixtures</files>
  <behavior>
    - Positive fixture imports and uses all canonical operation variants, outcome families, and required support types.
    - Negative fixtures cover operation/dispatch, services, plugin options/registries, and Flow categories through api, root, and doc-hidden paths.
    - Assertions classify compile success/failure without matching complete rustc diagnostics.
  </behavior>
  <action>Create a deterministic offline fixture harness using the existing Cargo/rustc test pattern and isolated target directories. Keep the positive inventory explicit and independent of production exports. Organize negative fixtures by D-13 category and attempt every D-14 access path; per D-15, assert broad privacy/unresolved/inaccessible failure categories without binding to complete rustc diagnostic text. Only narrow `src/lib.rs` visibility if a fixture demonstrates a real leak; never expose internals to satisfy a test.</action>
  <verify><automated>cargo test -p pi-coding-agent --test api_boundary_guards --test public_api -- --nocapture</automated></verify>
  <acceptance_criteria>All positive imports compile and execute existing behavior assertions; every forbidden category fails externally through all required paths with stable category checks.</acceptance_criteria>
  <done>GUARD-03 and GUARD-04 are enforced primarily by compiler-visible external fixtures with source scans retained only for inexpressible boundaries.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| External crate -> pi_coding_agent | Consumer code may attempt every public-looking import path. |
| Fixture harness -> compiler | Compile outcomes are the privacy authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Elevation | stable facade | high | mitigate | External compile-fail matrix across api, root, and hidden modules. |
| T-05-02 | Tampering | API inventory | high | mitigate | Explicit independent positive inventory prevents self-approving generated expectations. |
| T-05-SC | Tampering | Cargo fixtures | high | mitigate | Offline isolated targets and existing dependencies only. |
</threat_model>

<verification>Run both focused API targets and `git diff --check`.</verification>
<success_criteria>Canonical facade closure is importable externally and all internal contract categories remain inaccessible.</success_criteria>
<output>Write `.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-02-SUMMARY.md` after execution.</output>
