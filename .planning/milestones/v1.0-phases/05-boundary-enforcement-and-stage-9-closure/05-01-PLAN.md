---
phase: 05-boundary-enforcement-and-stage-9-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs
autonomous: true
requirements: [GUARD-01, GUARD-02]
must_haves:
  truths:
    - "D-02: Adapter roots are recursively discovered while explicitly registered single-file entry points remain covered automatically."
    - "D-03: Production-only scanning excludes code inside cfg(test) items and modules."
    - "D-04: Every known first-party adapter belongs to exactly one valid ownership root, and new top-level adapters require inventory updates."
    - "D-05: Discovery fails closed with path-specific diagnostics for missing roots, empty scans, unreadable paths, duplicate ownership, and unowned known adapters."
    - "D-06: Structural receiver-aware scans, rather than method-name substring matching, distinguish prohibited workflow calls from legitimate same-name calls."
    - "D-07: Legitimate same-name calls are admitted only through one reasoned exception table keyed by ownership/path, receiver shape, and method name."
    - "D-08: Every exception has an exact or maximum occurrence count so copied, moved, or increased calls require inventory review."
  artifacts:
    - crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs
  key_links:
    - Adapter inventory drives both recursive coverage and violation diagnostics.
    - Scanner fixtures exercise formatting and receiver variants independently of production source.
---

<objective>
Harden the first-party adapter boundary guard so regressions in JSON, print, RPC, or interactive production sources fail closed.

Purpose: Implement D-01 through D-11 without recreating compatibility methods or relying on name-only source matching.
Output: An executable, fixture-backed recursive ownership/call/suppression guard.
</objective>

<context>
@.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-CONTEXT.md
@.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-RESEARCH.md
@.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-PATTERNS.md
@crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Centralize recursive adapter ownership and structural scanner fixtures</name>
  <read_first>crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs; .planning/phases/05-boundary-enforcement-and-stage-9-closure/05-CONTEXT.md</read_first>
  <files>crates/pi-coding-agent/tests/product_runtime_boundary_guards.rs</files>
  <behavior>
    - Missing, empty, unreadable, duplicate, and unowned registered paths produce path-specific failures.
    - Multiline, chained, parenthesized, commented, rustfmt-varied receivers are classified structurally.
    - Comments, strings, doc comments, chars, and cfg(test) regions do not create violations.
  </behavior>
  <action>Extend the existing SourceScan and sanitizer framework with one explicit adapter inventory containing recursive roots and single-file entry points (D-02), normalized repository-relative exactly-one ownership assertions (D-04), and path-specific fail-closed handling for missing/empty/unreadable/duplicate/unowned discovery states (D-05). Restrict scanning to production code by excluding cfg(test) items and modules (D-03). Classify calls structurally with receiver awareness instead of method-name substring authority (D-06), and admit legitimate same-name calls only through one scoped exception table keyed by ownership/path, receiver shape, method name, and concrete reason (D-07), with exact or maximum occurrence counts that expose move/copy/increase drift (D-08). Add deterministic in-test source fixtures for every D-09/D-10 variant. Keep unknown receivers, inventory drift, inline suppression directives, and exception-count drift fail closed. Implement D-01 through D-11; do not add a parser dependency or any alternate compatibility entry point.</action>
  <verify><automated>cargo test -p pi-coding-agent --test product_runtime_boundary_guards -- --nocapture</automated></verify>
  <acceptance_criteria>All adapter roots and entry files are covered exactly once; structural fixtures pass; injected prohibited calls, suppressions, ownership gaps, and exception drift fail with repository-relative diagnostics.</acceptance_criteria>
  <done>The boundary guard enforces recursive adapter coverage and receiver-aware canonical-call policy with no inline bypass mechanism.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Production source -> guard | Rust adapter files are untrusted inputs to architectural scanning. |
| Guard inventory -> policy | Exception and ownership tables determine which calls are admitted. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Elevation | adapter boundary scan | high | mitigate | Receiver-aware matching, fail-closed unknown receivers, centralized counted exceptions. |
| T-05-03 | Tampering | sanitizer/fixtures | high | mitigate | Formatting, comments, strings, and cfg(test) fixture matrix with automated integration test. |
| T-05-SC | Tampering | Cargo dependencies | high | mitigate | Use existing Rust test infrastructure; no new dependency without review. |
</threat_model>

<verification>Run the focused guard target and `git diff --check` after implementation.</verification>
<success_criteria>GUARD-01 and GUARD-02 pass with deterministic recursive ownership, structural call, suppression, and exception-drift enforcement.</success_criteria>
<output>Write `.planning/phases/05-boundary-enforcement-and-stage-9-closure/05-01-SUMMARY.md` after execution.</output>
