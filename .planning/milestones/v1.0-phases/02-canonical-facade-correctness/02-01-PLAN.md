---
phase: 02-canonical-facade-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pi-coding-agent/src/lib.rs
  - crates/pi-coding-agent/tests/public_api.rs
  - crates/pi-coding-agent/tests/api_boundary_guards.rs
autonomous: true
requirements:
  - FACADE-01
  - FACADE-04
must_haves:
  truths:
    - "Per D-01 and D-02, a downstream caller can name the complete operation input/outcome/error closure and the existing create/open, observation, subscription, and control contracts through pi_coding_agent::api alone."
    - "Per D-03, construction, observation, subscription, and control remain separate public contracts rather than new CodingAgentOperation variants."
    - "Per D-04, crate-root compatibility exports remain intact while every first-party test changed here imports the stable surface through pi_coding_agent::api."
    - "Per D-05, Rust visibility and downstream API compilation prove the positive boundary, with narrow source guards only for negative structure that stable Rust cannot directly compile as a passing test."
  artifacts:
    - path: crates/pi-coding-agent/src/lib.rs
      provides: "Curated stable facade with the complete caller-facing signature closure and no internal runtime contracts."
    - path: crates/pi-coding-agent/tests/public_api.rs
      provides: "Facade-only signature-closure and canonical usability evidence."
    - path: crates/pi-coding-agent/tests/api_boundary_guards.rs
      provides: "Precise privacy guards for internal operation, dispatch, plugin-option, service, and Flow contracts."
  key_links:
    - from: crates/pi-coding-agent/tests/public_api.rs
      to: crates/pi-coding-agent/src/lib.rs
      via: "One explicit pi_coding_agent::api import block names every caller-facing type in the closure."
    - from: crates/pi-coding-agent/tests/api_boundary_guards.rs
      to: crates/pi-coding-agent/src/lib.rs
      via: "The guard isolates the api module body and rejects exact forbidden internal re-export identifiers."
---

<objective>
Close and enforce the stable public facade before production adapters consume it.

Purpose: Satisfy FACADE-01 and FACADE-04 by proving that the stable facade is complete for caller-facing signatures while internal runtime ownership remains private, exactly as locked by D-01 through D-05.
Output: Minimal confirmed `api` re-export corrections, a facade-only signature-closure test, and narrow privacy guards without a new facade, package, or compatibility layer.
</objective>

<execution_context>
@/home/whai/.codex/gsd-core/workflows/execute-plan.md
@/home/whai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-canonical-facade-correctness/02-CONTEXT.md
@.planning/phases/02-canonical-facade-correctness/02-RESEARCH.md
@.planning/phases/02-canonical-facade-correctness/02-PATTERNS.md
@.planning/phases/02-canonical-facade-correctness/02-VALIDATION.md
@.planning/phases/01-evidence-based-baseline/01-AUDIT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove and close the stable facade signature graph</name>
  <files>crates/pi-coding-agent/src/lib.rs, crates/pi-coding-agent/tests/public_api.rs</files>
  <read_first>
    - crates/pi-coding-agent/src/lib.rs: the `pub mod api` block and crate-root compatibility exports
    - crates/pi-coding-agent/src/coding_session/mod.rs: public `CodingAgentSession` create/open/list/run/snapshot/connect/subscription/control signatures
    - crates/pi-coding-agent/src/coding_session/public_operation.rs: all 15 operation payloads and all public outcome payloads
    - crates/pi-coding-agent/tests/public_api.rs: the existing facade-only import block and canonical run tests
    - .planning/phases/02-canonical-facade-correctness/02-PATTERNS.md: stable facade and public API analogs
  </read_first>
  <behavior>
    - "A single downstream import surface can name every public type transitively exposed by the stable operation/session signatures without crate-root or implementation-module imports."
    - "All 15 CodingAgentOperation variants and every CodingAgentOperationOutcome family remain constructible or type-checkable from the stable facade."
    - "Create/open, snapshot/query, product-event subscription, client connection, capability/view, and control contracts remain available as separate stable contracts."
  </behavior>
  <action>
Create an explicit test-owned signature-closure ledger in `public_api.rs`, then make only the re-export changes that ledger proves necessary in `lib.rs`. Per D-01, import every named caller-facing type directly from `pi_coding_agent::api`; per D-02, include the complete operation input/outcome/error closure and the existing lifecycle/observation/subscription/control closure; per D-03, do not turn lifecycle or control methods into operation variants. Preserve crate-root compatibility exports and their migration status per D-04. Keep the test as real downstream compilation plus focused type construction/naming, not a textual count. Do not export implementation modules, raw plugin load options, operation metadata, services, registries, provider internals, or Flow nodes.
  </action>
  <verify>
    <automated>cargo test -p pi-coding-agent --test public_api stable_api_signature_closure_is_importable -- --nocapture && cargo fmt --check</automated>
  </verify>
  <done>The facade-only test names the full caller-facing closure and passes; any confirmed missing exports are added directly to `api`; no modified test imports a stable contract from crate root or an implementation module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce stable facade privacy without widening production visibility</name>
  <files>crates/pi-coding-agent/tests/api_boundary_guards.rs</files>
  <read_first>
    - crates/pi-coding-agent/tests/api_boundary_guards.rs: existing root visibility, compatibility export, dispatcher, and event receiver guards
    - crates/pi-coding-agent/src/lib.rs: exact stable `api` re-export body after Task 1
    - crates/pi-coding-agent/src/coding_session/operation.rs: private Operation, OperationMetadata, and dispatch contracts
    - crates/pi-coding-agent/src/coding_session/plugin_load_flow.rs: raw PluginLoadOptions ownership
    - crates/pi-coding-agent/src/coding_session/mod.rs: private service fields and module declarations
    - .planning/phases/02-canonical-facade-correctness/02-CONTEXT.md: D-03 and D-05 privacy hierarchy
  </read_first>
  <behavior>
    - "Stable API tests accept the caller-facing projection types while internal operation, metadata, raw plugin options, services, registries, and Flow contracts are absent from the api re-export body."
    - "Migration-only crate-root modules/exports remain available and hidden/deprecated as already required; Phase 2 does not delete them."
  </behavior>
  <action>
Strengthen the existing boundary suite using the strongest available mechanism per D-05: rely on private Rust visibility and downstream type naming for boundaries the compiler expresses, and add narrowly scoped source guards only for negative re-export/module-annotation facts that cannot be represented by a passing integration test. Parse or isolate the `pub mod api` region before checking exact forbidden identifiers so names such as `CodingAgentOperation` do not cause substring false positives. Explicitly cover private `Operation`, `OperationMetadata`, `OperationDispatchMode`, raw `PluginLoadOptions`, runtime/session/event/plugin services, registries, and Flow node/types per D-03. Preserve crate-root compatibility surfaces per D-04 and do not add a compile-fail dependency, production test hook, or replacement facade.
  </action>
  <verify>
    <automated>cargo test -p pi-coding-agent --test api_boundary_guards stable_api_excludes_internal_runtime_contracts -- --nocapture && cargo test -p pi-coding-agent --test public_api -- --nocapture && cargo fmt --check</automated>
  </verify>
  <done>The stable facade is positively usable and the focused negative guard rejects every named internal contract without deleting or renaming compatibility exports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Downstream crate -> `pi_coding_agent::api` | Untrusted or independently versioned caller code may only receive curated product contracts. |
| Stable facade -> private coding-session runtime | Re-exports must not grant access to admission metadata, capability-bearing services, registries, plugin load options, or Flow internals. |
| Crate-root compatibility surface -> stable facade | Migration-only exports remain available temporarily but must not become the source used by new first-party evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure / Elevation of Privilege | `src/lib.rs::api` | high | mitigate | Build the explicit signature closure, export only caller-facing projections, and compile the downstream facade-only test. |
| T-02-02 | Tampering | `tests/api_boundary_guards.rs` | high | mitigate | Isolate the stable facade region and reject exact internal identifiers while retaining Rust visibility as the primary control. |
</threat_model>

<verification>
Task checks are focused and deterministic. After both Wave 1 plans complete, run `cargo test -p pi-coding-agent` and `cargo check -p pi-coding-agent` before beginning Wave 2. No network, external package, adapter migration, compatibility deletion, or production instrumentation is permitted.
</verification>

<acceptance_criteria>
- `cargo test -p pi-coding-agent --test public_api` passes with the full closure imported only from `pi_coding_agent::api`.
- `cargo test -p pi-coding-agent --test api_boundary_guards` passes and includes exact stable-facade privacy enforcement.
- Internal operation/metadata/plugin-option/service/registry/Flow contracts remain non-public through the stable facade.
- Crate-root compatibility exports remain present and Phase 3 adapter sources are untouched.
</acceptance_criteria>

<success_criteria>
FACADE-01 and FACADE-04 are supported by compiler-visible positive evidence plus narrow negative guards, with all D-01 through D-05 constraints preserved.
</success_criteria>

## Artifacts this phase produces

- Modified `crates/pi-coding-agent/src/lib.rs` stable `api` re-export closure, only if the ledger confirms omissions.
- Test `stable_api_signature_closure_is_importable` in `crates/pi-coding-agent/tests/public_api.rs`.
- Test `stable_api_excludes_internal_runtime_contracts` in `crates/pi-coding-agent/tests/api_boundary_guards.rs`.
- No new production module, facade, dependency, or public internal-runtime symbol.

<output>
Create `.planning/phases/02-canonical-facade-correctness/02-01-SUMMARY.md` when done.
</output>
