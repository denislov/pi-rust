# pi-rust TODO

This file is the working checklist for the Flow-centered `pi-rust` architecture.

`pi-rust` is no longer treated as a direct TypeScript `pi` port. The TypeScript `pi` repo remains a behavioral and product reference. PocketFlow remains an architectural reference for explicit graph-shaped orchestration. The Rust project should use both references to build a clearer, more principled agent runtime.

## Update Rule

Update this TODO whenever a task completes meaningful progress against an item below.

Required updates:

- Mark completed items with `[x]`.
- Mark active partial work with `[~]` and add a short note.
- Add new discovered work under the relevant phase instead of leaving it implicit.
- Add a dated progress note when a phase boundary, guide step, or major design decision changes.
- Include TODO updates in the same commit as the implementation or documentation change that changes status.

Do not let this file become historical fiction. If implementation changes the plan, update this file and the relevant guide/spec together.

## Source Documents

- [Flow-centered architecture design](superpowers/specs/2026-06-29-flow-centered-runtime-architecture-design.md)
- [Phase 2 print session target convergence design](superpowers/specs/2026-06-29-phase-2-print-session-target-convergence-design.md)
- [Phase 2 ResolveRequest node design](superpowers/specs/2026-06-30-phase-2-resolve-request-node-design.md)
- [Session finalization convergence design](superpowers/specs/2026-06-30-session-finalization-convergence-design.md)
- [Non-persistent product runtime design](superpowers/specs/2026-06-30-non-persistent-product-runtime-design.md)
- [Interactive CodingEventBridge design](superpowers/specs/2026-06-30-interactive-coding-event-bridge-design.md)
- [Rust-native active leaf commit design](superpowers/specs/2026-06-30-rust-native-active-leaf-commit-design.md)
- [Rust-native session fork and clone design](superpowers/specs/2026-06-30-rust-native-session-fork-clone-design.md)
- [Rust-native manual compaction design](superpowers/specs/2026-06-30-rust-native-manual-compaction-design.md)
- [Rust-native tree navigation design](superpowers/specs/2026-06-30-rust-native-tree-navigation-design.md)
- [Rust-native session format](superpowers/specs/2026-07-01-rust-native-session-format.md)
- [P0 crate boundary hardening design](superpowers/specs/2026-07-01-p0-crate-boundary-hardening-design.md)
- [Flow-centered implementation plan](superpowers/plans/2026-06-29-flow-centered-runtime-architecture-plan.md)
- [Phase 5 plugin kernel completion plan](superpowers/plans/2026-07-01-phase-5-plugin-kernel-completion-plan.md)
- [P0 crate boundary hardening plan](superpowers/plans/2026-07-01-p0-crate-boundary-hardening-plan.md)
- [Runtime control stabilization plan](superpowers/plans/2026-07-03-runtime-control-stabilization-plan.md)
- [Agent profile and team slash invocation plan](superpowers/plans/2026-07-02-agent-profile-team-slash-invocation-plan.md)
- [Self-healing edit workflow design](superpowers/specs/2026-07-04-self-healing-edit-workflow-design.md)
- [Self-healing edit workflow plan](superpowers/plans/2026-07-04-self-healing-edit-workflow-plan.md)
- [Self-healing edit capability plan](superpowers/plans/2026-07-04-self-healing-edit-capability-plan.md)
- [Self-healing edit session entrypoint plan](superpowers/plans/2026-07-04-self-healing-edit-session-entrypoint-plan.md)
- [Self-healing edit check/repair plan](superpowers/plans/2026-07-04-self-healing-edit-check-repair-plan.md)
- [Self-healing edit provider tool migration plan](superpowers/plans/2026-07-04-self-healing-edit-provider-tool-migration-plan.md)
- [Self-healing edit adapter exposure plan](superpowers/plans/2026-07-04-self-healing-edit-adapter-exposure-plan.md)
- [Self-healing edit check-command surface plan](superpowers/plans/2026-07-04-self-healing-edit-check-command-surface-plan.md)
- [Self-healing edit failure observability plan](superpowers/plans/2026-07-04-self-healing-edit-failure-observability-plan.md)
- [Self-healing edit planned repair policy plan](superpowers/plans/2026-07-04-self-healing-edit-planned-repair-policy-plan.md)
- [Self-healing edit model repair plan](superpowers/plans/2026-07-04-self-healing-edit-model-repair-plan.md)
- [Self-healing edit RPC model repair plan](superpowers/plans/2026-07-04-self-healing-edit-rpc-model-repair-plan.md)
- [Self-healing edit interactive model repair plan](superpowers/plans/2026-07-04-self-healing-edit-interactive-model-repair-plan.md)
- [Self-healing edit repair observability plan](superpowers/plans/2026-07-04-self-healing-edit-repair-observability-plan.md)
- [Self-healing edit product events plan](superpowers/plans/2026-07-04-self-healing-edit-product-events-plan.md)
- [Self-healing edit RPC repair-attempt response plan](superpowers/plans/2026-07-04-self-healing-edit-rpc-repair-attempt-response-plan.md)
- [Self-healing edit live repair events plan](superpowers/plans/2026-07-04-self-healing-edit-live-repair-events-plan.md)
- [Lua host workspace metadata plan](superpowers/plans/2026-07-04-lua-host-workspace-metadata-plan.md)
- [Lua host capabilities metadata plan](superpowers/plans/2026-07-04-lua-host-capabilities-metadata-plan.md)
- [Product event adapter Flow-node independence audit plan](superpowers/plans/2026-07-04-product-event-adapter-flow-node-independence-plan.md)
- [pi-agent-core product boundary audit plan](superpowers/plans/2026-07-04-pi-agent-core-product-boundary-audit-plan.md)
- [Deterministic offline test boundary hardening plan](superpowers/plans/2026-07-04-deterministic-offline-test-boundary-plan.md)
- [Agent profiles and teams user guide](agent-profiles.md)
- [Phase 1 guide](superpowers/guides/2026-06-29-phase-1-coding-session-and-session-log-guide.md)
- [Phase 2 guide](superpowers/guides/2026-06-29-phase-2-prompt-turn-flow-guide.md)
- [Phase 3 guide](superpowers/guides/2026-06-29-phase-3-adapter-convergence-guide.md)
- [Phase 4 guide](superpowers/guides/2026-06-29-phase-4-agent-turn-flow-guide.md)
- [Phase 5 guide](superpowers/guides/2026-06-29-phase-5-plugin-kernel-guide.md)
- [Phase 6 guide](superpowers/guides/2026-06-29-phase-6-advanced-flow-workflows-guide.md)
- [Cross-guide interface review](superpowers/guides/2026-06-29-flow-guides-interface-review.md)

## Current North Star

- [x] Establish minimal `pi_agent_core::flow`.
- [x] Design the Flow-centered runtime architecture.
- [x] Write the implementation plan.
- [x] Write detailed phase implementation guides.
- [x] Implement Phase 1: `CodingAgentSession` skeleton and Rust-native session log. Product runtime shell/API boundary, typed session log schema, filesystem store, turn transactions, owner create/open persistence, and replay/fold transcript support are in place.
- [x] Implement Phase 2: `PromptTurnFlow` on headless/json path. Prompt turn options/outcome/context, runtime snapshot boundary, real graph with stable node IDs, AgentEvent-to-product-event mapping, real ResolveRequest/PrepareInput/ResolveRuntime/LoadResources/OpenSession/BuildAgentRuntime/RecordUserInput/RunAgentTurn/FinalizeTurn/EmitCompletion nodes, pending agent-output event recording through TurnTransaction, live product event broadcast during PromptTurnFlow execution, assistant thinking deltas, completed-message-only assistant session persistence, SessionService-owned prompt transaction finalization with `SessionWrite*` product events, persistent and non-persistent `CodingAgentSession::prompt()` for runtime-backed options, successful persistent prompt active-leaf commits, completed user/assistant/tool-call replay hydration, Rust-native session open-or-create/list groundwork, Rust-native clone/fork/manual-compaction service APIs, enabled and no-session/disabled print routing including ForkTarget, and JSON protocol rendering/execution through `CodingAgentEvent`/`CodingAgentSession` are in place.
- [x] Implement Phase 3: converge CLI/RPC/interactive adapters. Concrete `CodingAgentCapabilities`/`CapabilityStatus` model, RPC `get_state` capability reporting, RPC `CodingAgentEvent` adapter, enabled plus disabled-session RPC prompt routing, primary interactive prompt routing, JSON execution routing, Rust-native resume hydration, Rust-native active-session `/session` info and leaf-backed `/tree` navigation, Rust-native active-session `/clone`, `/fork`, and `/compact`, Rust-native active leaf propagation to RPC/interactive state, adapter-provided cwd recording/filtering, and live assistant text/thinking propagation to RPC and interactive adapters are in place.
- [x] Implement Phase 4: introduce `AgentTurnFlow` in `pi-agent-core`. `agent_turn_flow` now owns the low-level runtime entrypoint, `AgentTurnContext` snapshot boundary, `PrepareContextNode`, runtime compaction node, provider stream node, decide-stop-or-tools node, and tool execution node with sequential/parallel execution, hooks, update callbacks, and terminate behavior. `Agent::run()` now delegates to `AgentTurnFlow`, and the old `agent_loop` module remains only as a compatibility wrapper.
- [x] Implement Phase 5: plugin kernel on session/flow boundaries. Internal Rust trait kernel slices are in place: plugin registry, `ToolProvider`, `ToolRegistrationHost`, tool collection with diagnostics, `CommandProvider`, `CommandRegistrationHost`, command collection with diagnostics, `HookProvider`, `HookRegistrationHost`, hook collection and PromptTurnFlow execution with diagnostics/fail-closed errors, `UiProvider`, `UiRegistrationHost`, `KeybindProvider`, `KeybindRegistrationHost`, UI/keybind collection with diagnostics, reserved `FlowExtension`/`FlowExtensionPoint` collection through `FlowExtensionRegistrationHost`, `PluginCapabilities`, plugin capability reporting through `CapabilityService`, and `RuntimeService` plugin-tool merging through the session-owned plugin service.
- [~] Implement Phase 6: advanced Flow workflows. Manual compaction, branch summary, plugin load/reload, Lua plugin tool/command/hook/UI/keybind/dialog slices, plugin command dispatch, plugin reload capability reporting, runtime control stabilization, ExportFlow wrapping, runtime-vs-session compaction boundary cleanup, AgentProfile/TeamProfile registry/default-runtime groundwork, interactive AgentProfile/TeamProfile slash entrypoints, one-off AgentInvocationFlow, supervised AgentTeamFlow, policy-gated delegation request tools, RPC profile/team list commands, RPC default-agent-profile switching, RPC one-off agent/team invocation, semantic RPC protocol events for agent/team lifecycle, default-profile changes, delegation requested/rejected audit events, auto-approved delegation approved/started/completed execution events, confirmation-required delegation events, failed-delegation lifecycle events, child-depth delegation rejection, and event-log-backed pending delegation confirmation queue plus RPC/interactive list/approve/reject commands, and interactive `/delegations` confirmation menu approval/rejection UI, and 24-hour stale pending-confirmation filtering are in place. Recursive child execution/inherited budget accounting is in place. Delegated child capability release policy is in place. Recursive delegation loop detection is in place. Durable nested confirmation queue semantics are in place. Self-healing edit workflow design, first internal Flow slice, check-command execution, bounded repair retry injection, provider-visible builtin edit-tool migration, capability surface, session-owned Rust API entrypoint, typed lifecycle/check-output/repair-attempt event log, RPC command exposure, interactive `/self-healing-edit` slash entrypoint, product-level check-command surfaces for Rust API/RPC/interactive adapters, structured check-failure output for Rust API/RPC, bounded planned repair policy for Rust API/RPC, and bounded session-owned model repair policy for Rust API/RPC/interactive, and semantic self-healing edit product events for RPC/interactive adapters, RPC repair-attempt success responses, and Flow-time live repair-attempt product events are in progress.

## Phase 1: CodingAgentSession and Rust-Native Session Log

Guide: [Phase 1](superpowers/guides/2026-06-29-phase-1-coding-session-and-session-log-guide.md)

- [x] Add `crates/pi-coding-agent/src/coding_session/` module shell.
- [x] Export `CodingAgentSession` and `CodingAgentEvent` through `pi_coding_agent::api`.
- [x] Add `CodingSessionError` typed product error boundary.
- [x] Add base `CodingAgentEvent`.
- [x] Add `EventService`.
- [x] Add Rust-native session manifest model.
- [x] Add typed `SessionEventEnvelope` and `SessionEventData`.
- [x] Add deterministic ID and clock test boundaries.
- [x] Add `SessionLogStore`.
- [x] Add `TurnTransaction`.
- [x] Add replay/fold transcript view.
- [x] Add `CodingAgentSession` owner skeleton.
- [x] Add Phase 1 tests. Public API, coding session shell/error, owner create/open persistence, manifest round-trip, event JSON shape, stable event kind names, deterministic ID, fixed clock, tempdir-backed store, canonical event-log replay, and transaction commit/abort/fail/finalization coverage added.
- [x] Run Phase 1 focused checks. `cargo fmt --check`, `cargo test -p pi-coding-agent coding_session`, `cargo test -p pi-coding-agent public_api`, `cargo check --workspace`, and `cargo test --workspace` pass for the completed Phase 1 slice.

## Phase 2: PromptTurnFlow on Headless/JSON

Guide: [Phase 2](superpowers/guides/2026-06-29-phase-2-prompt-turn-flow-guide.md)

- [x] Add `PromptTurnOptions`.
- [x] Add `PromptTurnOutcome`.
- [x] Add `PromptTurnContext`.
- [x] Add `RuntimeSnapshot`.
- [x] Add `PromptTurnFlow` graph.
- [x] Add prompt flow nodes. Stable Phase 2 graph boundaries now have real node behavior: ResolveRequest validates runtime-backed prompt options and marks request resolution, PrepareInput normalizes/validates prompt invocation into prepared persisted input, ResolveRuntime owns runtime snapshot attachment from PromptTurnOptions into PromptTurnContext, LoadResources attaches the runtime resource snapshot into turn state before agent construction, OpenSession validates that the owner prepared session id/replay/transaction state before agent construction, BuildAgentRuntime builds the Agent from RuntimeSnapshot and hydrated replay only after resources are loaded, RecordUserInput records prepared prompt input, RunAgentTurn drives an existing Agent stream, FinalizeTurn validates final-turn readiness without flushing session events, and EmitCompletion appends the success completion product event from the graph.
- [x] Map `AgentEvent` to `CodingAgentEvent`.
- [x] Add `RunAgentTurn` node using existing `Agent::run()`.
- [x] Record agent output into session events through `TurnTransaction`.
- [x] Converge prompt transaction finalization under `SessionService` and emit `SessionWrite*` product events. `SessionService` now owns prompt commit/fail/abort/skip finalization, `FinalizeTurn` validates readiness without flushing session events, and `CodingAgentSession` emits session write events before final prompt outcome events.
- [x] Add `CodingAgentSession::prompt()`.
- [x] Implement non-persistent product runtime for no-session/disabled prompt convergence. `CodingAgentSession::non_persistent()` now owns prompt execution without durable session storage, `PromptTurnFlow` accepts non-persistent replay boundaries, non-persistent prompts emit `SessionWriteSkipped`, and owner-lifetime transcript hydration works for follow-up prompts on the same owner.
- [x] Route print mode through `CodingAgentSession`. Enabled default/New/OpenTarget/OpenOrCreateId/ContinueMostRecent print session targets use persistent `CodingAgentSession` and Rust-native session logs; enabled ForkTarget forks the Rust-native source session through `SessionService` before running the prompt; no-session/disabled print uses non-persistent `CodingAgentSession`.
- [x] Route JSON mode through `CodingAgentEvent`.
- [x] Remove the old `session_runner` transitional wrapper. Print, JSON, RPC, interactive prompt execution, manual compaction, clone/fork/tree, and no-session runs route through `CodingAgentSession`/`CodingAgentEvent`; old JSONL session targets are rejected instead of executed.
- [x] Add Phase 2 tests. Prompt turn option/outcome/context, runtime snapshot, graph path/node ID, misconfigured flow, FlowService, RuntimeService agent building and replay hydration, real ResolveRequest validation/idempotency/order coverage, real PrepareInput normalization/error coverage, real ResolveRuntime attachment/error coverage, real LoadResources attachment/error coverage, real OpenSession persistent/non-persistent boundary validation coverage, real BuildAgentRuntime resource-precondition and replay hydration coverage, real FinalizeTurn validation-without-flush coverage, real EmitCompletion completion/idempotency/error coverage, AgentEvent mapping including assistant thinking deltas, RecordUserInput prepared-event recording, RunAgentTurn/faux-provider flow execution, completed-message-only assistant session-event persistence, thinking replay hydration coverage, SessionService commit/fail/skip product-event finalization coverage, owner-level session write ordering, active-leaf commit consistency, and failed-operation finalization coverage, persistent and non-persistent `CodingAgentSession::prompt()` success/config-error/failure-event-deduplication with user+assistant replay and reopened/owner-lifetime provider-context hydration, Rust-native session open-or-create/list coverage, public API smoke coverage, default/New/OpenTarget id/path/OpenOrCreateId/ContinueMostRecent print-mode Rust-native session-log coverage, disabled/no-session print no-file coverage, ForkTarget Rust-native fork routing coverage, direct `CodingAgentEvent` protocol adapter coverage, and JSON mode success/tool/failure/enabled-session Rust-native persistence coverage added.
- [x] Run Phase 2 focused checks. Closure audit verified the completed Phase 2 prompt/headless/json surface with `cargo fmt --check`, `cargo check --workspace`, `cargo test -p pi-coding-agent`, `cargo test --workspace`, and `git diff --check`.

## Phase 3: Adapter Convergence

Guide: [Phase 3](superpowers/guides/2026-06-29-phase-3-adapter-convergence-guide.md)

- [x] Add concrete `CodingAgentCapabilities`. `CapabilityStatus` now reports available/unsupported/busy/disabled states across prompt, abort, steer, follow-up, compact, fork, clone/switch/export session, tools, shell, and plugins.
- [x] Route RPC prompt command through `CodingAgentSession`. Enabled-session RPC prompts run through persistent `CodingAgentSession`, stream `CodingAgentEvent` through `RpcCodingEventAdapter`, and persist Rust-native session logs; disabled-session RPC prompts run through non-persistent `CodingAgentSession`, emit product-event-derived protocol events, and leave `get_state.sessionId` as `in-memory` with no session file.
- [x] Add RPC adapter from `CodingAgentEvent` to protocol events. `RpcCodingEventAdapter` wraps the product-event protocol adapter at the RPC boundary and has prompt stream/failure mapping coverage; it is ready to wire into RPC prompt migration.
- [x] Add RPC capability reporting. RPC `get_state` now includes protocol-stable capability status objects derived from the concrete capability model, including idle prompt availability and running prompt busy state.
- [x] Route interactive prompt tasks through `CodingAgentSession`. Interactive text/content prompts run through persistent or non-persistent `CodingAgentSession`, active persistent Rust-native manual compaction runs through `CodingAgentSession`, both stream `CodingAgentEvent` through `CodingEventBridge`, preserve owner-lifetime session state across prompts, and reject old JSONL session targets instead of falling back to the removed runner.
- [x] Add interactive bridge from `CodingAgentEvent` to `UiEvent`. `CodingEventBridge` now maps product assistant/tool/failure/abort/compaction events into existing `UiEvent` values while leaving old `AgentEvent` interactive paths in place.
- [x] Move migrated session actions to `SessionService`. Interactive startup `--resume` and `/resume` session selection use `SessionService`-owned Rust-native replay hydration for transcript/session label/active leaf; newly created Rust-native prompt sessions are tracked as active session choices; `/session` reports Rust-native session details; `/tree` uses a `SessionService`-owned leaf-backed tree view and forks selected historical leaves into independent Rust-native sessions; Rust-native `/clone` and `/fork` create independent Rust-native sessions from committed leaves through `SessionService`; Rust-native `/compact` persists typed compaction events and folds compacted replay through `SessionService`; old JSONL sessions are ignored rather than hydrated.
- [x] Stop creating and executing old session JSONL from product paths. Print, JSON, RPC, interactive prompt/manual-compaction paths now use Rust-native session logs or non-persistent product runtime; explicit old JSONL import/export/session targets are rejected.
- [x] Pass adapter cwd into Rust-native session creation/list filtering. `CodingAgentSessionOptions` now carries adapter-provided cwd, `SessionService` records that cwd in `session.created`, Rust-native session list filtering uses it for workspace-scoped choices, and print/RPC/interactive prompt paths pass their configured cwd into `CodingAgentSession`.
- [x] Add Phase 3 tests. CapabilityService idle/busy coverage, public API smoke coverage, RPC `get_state` idle/running capability reporting coverage, RPC product-event adapter prompt stream/failure/thinking coverage, enabled RPC Rust-native session persistence/state and active-leaf coverage, disabled RPC non-persistent no-file coverage, interactive `CodingEventBridge` assistant text/thinking/tool/failure/abort/compaction/ignored-event coverage, interactive transcript tool/assistant ordering regression coverage, interactive primary prompt Rust-native persistence/no-old-JSONL boundary coverage, interactive owner-lifetime multi-prompt persistence coverage, interactive Rust-native startup `/resume` and selector hydration coverage, Rust-native active `/session` info with committed leaf and leaf-backed `/tree` navigation coverage, Rust-native fork/clone session creation coverage, Rust-native manual compaction persistence/replay coverage, Rust-native adapter cwd recording/list filtering coverage, legacy JSONL resume rejection coverage, coding-running RPC abort/steer/follow-up control coverage, and interactive running Ctrl-C abort plus submit-to-steer coverage added.
- [x] Run Phase 3 focused checks. Closure audit verified the completed Phase 3 adapter-convergence surface with `cargo fmt --check`, `cargo check --workspace`, `cargo test -p pi-coding-agent`, `cargo test --workspace`, and `git diff --check`.

## Phase 4: AgentTurnFlow

Guide: [Phase 4](superpowers/guides/2026-06-29-phase-4-agent-turn-flow-guide.md)

- [x] Add `pi-agent-core/src/agent_turn_flow/` module.
- [x] Add `AgentTurnContext`. Initial context construction snapshots current agent config, messages, tools, resources, queues, cancellation token, and empty turn-local accumulators without draining queues or changing loop behavior.
- [x] Extract prepare-context node. `PrepareContextNode` now builds a `ProviderRequestSnapshot` from `AgentTurnContext` using the same context conversion and stream-option logic as the existing loop.
- [x] Extract runtime compaction node. `MaybeCompactRuntimeContextNode` now applies context-window-gated runtime compaction to `AgentTurnContext`, calls the existing summarizer, updates compacted context messages, and records low-level `SessionCompacted` events without writing product session state.
- [x] Extract provider stream node. `ProviderStreamNode` now consumes prepared provider requests from `AgentTurnContext`, streams through the existing provider boundary, buffers low-level `LlmEvent` events, stores the final assistant message, and maps streams without `Done` into `AgentError` events.
- [x] Extract decide-stop-or-tools node. `DecideStopOrToolsNode` now appends the assistant message to turn context, emits `AgentDone`/`AgentError` for terminal stop reasons, extracts pending tool calls in assistant order, and returns `done`/`tools`/`continue`/`error`/`aborted` actions without executing tools.
- [x] Extract tool execution node. `ExecuteToolsNode` now emits `ToolCallStart`/`ToolCallEnd`, executes matching tools, records unknown tools as error results, honors sequential vs parallel execution selection including per-tool sequential overrides, applies before/after tool hooks, emits sequential tool update callbacks as `ToolCallUpdate`, appends `ToolResult` messages in assistant order, returns `continue`, and returns `done` with `AgentDone` when all tool results terminate.
- [x] Preserve `AgentEvent` behavior. Existing `agent_loop`, `parallel_tools`, `hooks`, and `compaction` regression tests continue to exercise the public low-level event stream after `Agent::run()` switched to `AgentTurnFlow`.
- [x] Make `Agent::run()` delegate to `AgentTurnFlow`. `Agent::run()` now calls `AgentTurnFlow::run_state`, and `agent_loop::run_loop` is a compatibility wrapper over the same runtime entrypoint.
- [x] Add Phase 4 tests. `agent_turn_flow` integration coverage verifies `AgentTurnContext` construction from existing `Agent` state without queue drainage, `PrepareContextNode` provider-request construction against the current `Agent` snapshot behavior, runtime compaction node context/event updates, provider stream success/error event buffering, decide-stop-or-tools text/tool actions, tool execution success/unknown-tool behavior, parallel tool end-event/message ordering, before/after tool hooks, update callback events, terminate completion, and the runtime entrypoint boundary.
- [x] Run Phase 4 focused checks. Latest verified subset: `cargo fmt --check`, `cargo test -p pi-agent-core agent_turn_flow_runtime_entrypoint_exists`, `cargo test -p pi-agent-core --test agent_loop`, `cargo test -p pi-agent-core --test agent_turn_flow`, `cargo test -p pi-agent-core --test compaction`, `cargo test -p pi-agent-core --test parallel_tools`, `cargo test -p pi-agent-core --test hooks`, `cargo test -p pi-agent-core`, `cargo test -p pi-coding-agent`, and `cargo check --workspace`.

## Phase 5: Plugin Kernel

Guide: [Phase 5](superpowers/guides/2026-06-29-phase-5-plugin-kernel-guide.md)

- [x] Add plugin registry module. Internal `crate::plugins::PluginRegistry` stores Rust `ToolProvider`, `CommandProvider`, `HookProvider`, `UiProvider`, `KeybindProvider`, and reserved `FlowExtension` instances with plugin metadata/source types.
- [x] Add capability-scoped plugin hosts. `ToolRegistrationHost`, `CommandRegistrationHost`, `HookRegistrationHost`, `UiRegistrationHost`, `KeybindRegistrationHost`, and `FlowExtensionRegistrationHost` establish the scoped-host pattern without exposing `CodingAgentSession`, `SessionService`, auth, filesystem, shell, or Flow internals.
- [x] Add plugin capability reporting. Internal `PluginCapabilities` reports tool/command/hook/UI/keybind/FlowExtension provider counts and diagnostic counts from `PluginService`, and `CapabilityService` now reports the plugin kernel as available instead of unsupported.
- [x] Add `ToolProvider`. Internal Rust trait exists and returns `AgentTool` values through `PluginService::collect_tools()`.
- [x] Add `CommandProvider`. Internal Rust trait, `CommandDefinition`, registration host, registry storage, `PluginService::collect_commands()`, and isolated `PluginService::run_command()` execution exist; slash/RPC adapter dispatch and adapter capability exposure remain later work outside the Phase 5 kernel handoff.
- [x] Add `HookProvider`. Internal Rust trait, `HookRegistration`, `PromptHookPoint`, `HookFailurePolicy`, scoped `PromptHookContext`, `HookOutcome`, registration host, registry storage, collection, and isolated execution through `PluginService::run_prompt_hook()` exist; public/plugin author API hardening remains later work outside the Phase 5 kernel handoff.
- [x] Add minimal `UiProvider` and `KeybindProvider` boundaries. Internal UI action/dialog definitions, keybind definitions, scoped hosts, registry storage, provider traits, provider counts, and `PluginService` collection with diagnostics are in place; rendering and real key handling remain later integration work.
- [x] Reserve first-party `FlowExtension`. Internal `FlowExtension` exposes only a closed `FlowExtensionPoint` enum and provider-owned point collection through `FlowExtensionRegistrationHost`/`PluginService`; it does not expose Flow graph rewrites, node insertion, Lua registration, session storage, auth, shell, or filesystem access.
- [x] Integrate plugin tools through `RuntimeService`. `RuntimeService::build_agent_runtime_with_plugins()` merges plugin tools into the provider-visible Agent tool context; adapter-level plugin tool filtering remains later work outside the Phase 5 kernel handoff.
- [x] Integrate prompt hooks through `PromptTurnFlow`. Hook calls are wired at prompt prepare, input prepared, resources loaded, before/after agent turn, before session commit readiness, and after prompt completion. `AfterSessionCommit` currently runs before owner-level durable session finalization results are observable; durable post-commit observation remains later work outside the Phase 5 kernel handoff.
- [x] Add plugin failure isolation. Tool/command/hook/UI/keybind/FlowExtension provider `PluginError` values, collection-boundary panics, and hook execution panics/errors are captured as plugin diagnostics or fail-closed `CodingSessionError::Plugin` values while healthy providers still contribute capabilities.
- [x] Add Phase 5 tests. Unit coverage verifies tool collection, command collection, hook collection, UI action collection, keybinding collection, FlowExtension point collection/failure isolation, prompt hook execution diagnostics, fail-open hook errors, fail-closed hook errors, plugin capability reporting, returned-error isolation, panic isolation, and RuntimeService provider-context merging.
- [x] Run Phase 5 focused checks. Latest verified subset: `cargo fmt --check`, `cargo test -p pi-coding-agent plugin`, `cargo test -p pi-coding-agent coding_session`, `cargo check --workspace`, `cargo test -p pi-coding-agent`, and `git diff --check` pass for the completed Phase 5 plugin kernel handoff.

## Phase 6: Advanced Flow Workflows

Guide: [Phase 6](superpowers/guides/2026-06-29-phase-6-advanced-flow-workflows-guide.md)
Plan: [Runtime control stabilization](superpowers/plans/2026-07-03-runtime-control-stabilization-plan.md)
Plan: [Agent profile and team slash invocation](superpowers/plans/2026-07-02-agent-profile-team-slash-invocation-plan.md)
Design: [Agent and team menu invocation](superpowers/specs/2026-07-04-agent-team-menu-invocation-design.md)
Design: [Self-healing edit workflow](superpowers/specs/2026-07-04-self-healing-edit-workflow-design.md)
Plan: [Self-healing edit workflow](superpowers/plans/2026-07-04-self-healing-edit-workflow-plan.md)
Plan: [Self-healing edit session entrypoint](superpowers/plans/2026-07-04-self-healing-edit-session-entrypoint-plan.md)
Plan: [Self-healing edit check/repair](superpowers/plans/2026-07-04-self-healing-edit-check-repair-plan.md)
Plan: [Self-healing edit provider tool migration](superpowers/plans/2026-07-04-self-healing-edit-provider-tool-migration-plan.md)
Plan: [Self-healing edit adapter exposure](superpowers/plans/2026-07-04-self-healing-edit-adapter-exposure-plan.md)
Plan: [Self-healing edit check-command surface](superpowers/plans/2026-07-04-self-healing-edit-check-command-surface-plan.md)
Plan: [Self-healing edit failure observability](superpowers/plans/2026-07-04-self-healing-edit-failure-observability-plan.md)
Plan: [Self-healing edit planned repair policy](superpowers/plans/2026-07-04-self-healing-edit-planned-repair-policy-plan.md)
Plan: [Self-healing edit model repair](superpowers/plans/2026-07-04-self-healing-edit-model-repair-plan.md)
Plan: [Self-healing edit RPC model repair](superpowers/plans/2026-07-04-self-healing-edit-rpc-model-repair-plan.md)
Plan: [Self-healing edit interactive model repair](superpowers/plans/2026-07-04-self-healing-edit-interactive-model-repair-plan.md)
Plan: [Self-healing edit repair observability](superpowers/plans/2026-07-04-self-healing-edit-repair-observability-plan.md)
Plan: [Self-healing edit product events](superpowers/plans/2026-07-04-self-healing-edit-product-events-plan.md)
Plan: [Self-healing edit RPC repair-attempt response](superpowers/plans/2026-07-04-self-healing-edit-rpc-repair-attempt-response-plan.md)
Plan: [Lua host workspace metadata](superpowers/plans/2026-07-04-lua-host-workspace-metadata-plan.md)
Plan: [Lua host capabilities metadata](superpowers/plans/2026-07-04-lua-host-capabilities-metadata-plan.md)

- [x] Stabilize runtime control and owner boundaries before the next advanced workflow. Stage 1 operation-control has a RAII operation guard plus owner-issued prompt control handle/channel scaffolding. Stage 2 PromptTurnFlow plumbing passes owner-issued abort/steer/follow-up commands into the running Agent. Stage 3 adapter convergence routes RPC running `abort`/`steer`/`follow_up`, RPC `prompt.streamingBehavior` steer/followUp, interactive Ctrl-C prompt abort, interactive running Enter-to-steer, and interactive running Shift+Enter-to-follow-up into the same control path. Abort produces `PromptTurnOutcome::Aborted`, persistent `operation.aborted`, and provider cancellation from RPC/interactive prompts; interactive running input now preserves typed text while the prompt is running and routes steer/follow-up distinctly. Stage 4 capability cleanup is in place: capability construction is semantic `from_runtime_state`, prompt control gates are driven by `OperationKind::Prompt`, and stale AgentTurnFlow blocker reasons have been removed from session/RPC capability status. Stage 5 owner slimming moved active-operation state plus prompt-control receiver lifecycle behind `OperationControl`, so `CodingAgentSession` no longer holds raw prompt-control receiver/channel details. Stage 6 ExportFlow wrapper is in place with stable `start_export`/`load_session_replay`/`select_export_view`/`render_export`/`write_export`/`emit_completion` nodes and current-session `OperationKind::Export` boundaries. Stage 7 runtime-vs-session compaction cleanup separates low-level runtime compaction from manual session compaction at the product event boundary. Stage 8 readiness is the stop point before subagent/supervisor and self-healing edit workflows.
- [~] Add AgentProfile/TeamProfile slash invocation before implementing controlled model-requested delegation. Stage 1 profile registry groundwork is in place: typed `AgentProfile`/`TeamProfile`/`ProfileId`/diagnostic models, built-in default profile, TOML loading from user/project roots, schema validation, fail-open invalid-file diagnostics, deterministic built-in < user < project duplicate precedence, and public API coverage. Stage 2 default session profile semantics are in place: `CodingAgentSessionOptions` accepts a default profile id, Rust-native `session.json` persists `default_agent_profile_id`, open/resume restores it, `CodingAgentSessionView` exposes it, and `CodingAgentSession::set_default_agent_profile_id()` updates it with a product event without creating a hidden live agent. Stage 3 default runtime application is in place: ordinary prompts load the session-owned profile registry, resolve the current default `AgentProfile`, apply profile model/system prompt/tool/skill policy before `PromptTurnFlow`, and emit diagnostics for unavailable profile tools/skills while preserving the built-in default behavior. Stage 4 interactive slash entrypoints are in place: built-in `/agents`, `/agent`, `/teams`, and `/team` suggestions exist, `/agents` and `/teams` list resolved profiles, `/agent` opens the discovered-profile menu with `Info`, `Use`, and `Run`, `/team` opens the discovered-team menu with `Info` and `Run`, `/agent:<agent-id> <task>` queues one-off AgentInvocation, `/team:<team-id> <task>` queues one-off AgentTeam execution without changing the default profile, old `/agent use <id>`, `/agent <id> <task>`, and `/team <id> <task>` space-argument forms report menu/colon usage instead of mutating state, `@agent`/`@team` remain ordinary prompt text instead of mention syntax, and built-in slash commands win over same-named plugin command aliases. Stage 5 one-off agent invocation is in place: `CodingAgentSession::invoke_agent()` runs `AgentInvocationFlow`, child `PromptTurnFlow` execution uses non-persistent child session state with correlated parent/child operation ids, profile runtime policy is applied through the existing runtime boundary, product events cover invocation start, child output, diagnostics, completion, and failure while preserving parent transcript isolation, and interactive Ctrl-C now cancels the running child prompt through an owner-issued prompt-control handle. Stage 6 supervised team invocation is in place: `CodingAgentSession::invoke_team()` runs `AgentTeamFlow` with stable nodes, resolves team members and deterministic/profile-backed supervisors through the session registry, executes isolated child prompt flows, records team/member lineage events, reports unknown-supervisor and child runtime failures as team failures, and routes interactive `/team:<team-id> <task>` through `PromptTask`. Stage 7a controlled delegation request tooling is in place: active `AgentProfile` delegation policy can expose `delegate_agent`/`delegate_team` request tools, accepted requests return policy-gated structured envelopes, disallowed target ids and zero-depth policy are rejected deterministically, and raw session/runtime/provider internals remain hidden. Stage 7b delegation audit events are in place: delegation tool envelopes now map to `DelegationRequested`/`DelegationRejected` product events while preserving ordinary tool lifecycle events. Stage 7c-a delegation request queue groundwork is in place: accepted `DelegationRequested` events are captured as typed `PromptTurnContext` delegation requests for future owner-authorized execution. Stage 7c-b delegation authorization decision groundwork is in place: prompt success now computes typed `Approved`/`RequiresConfirmation`/`Rejected` decisions from the active profile policy, enforcing depth exhaustion, `max_parallel_children`, allowed target policy, and confirmation mode without running child work. Stage 7c-c auto-approved delegation execution is in place: approved requests emit `DelegationApproved`/`DelegationStarted`/`DelegationCompleted`, execute through owner-created `AgentInvocationFlow`/`AgentTeamFlow` child operations, and keep child prompt state non-persistent. Stage 7c-d confirmation-required delegation observability is in place: requests held for confirmation now emit `DelegationConfirmationRequired` product events and semantic protocol events without running child work. Stage 7c-e child-failure delegation lifecycle is in place: approved child agent/team flow failures emit `DelegationFailed` product events and semantic protocol events correlated with the original delegation tool call and child operation id while preserving existing diagnostic reporting. Stage 7c-f child-depth authorization boundary is in place: `AgentInvocationOptions`/`AgentTeamOptions` carry delegation depth, owner-approved child flows run at depth 1, and delegated child prompt queues emit explicit rejection/confirmation-required events instead of silently dropping nested requests. Stage 7c-g pending confirmation queue is in place: confirmation-held requests are stored on the session owner, public APIs expose pending confirmations plus approve/reject entrypoints, approval executes the held child delegation, and rejection emits `DelegationRejected` without running child work. Stage 7c-h durable pending confirmation is in place: persistent sessions record confirmation request/approval/rejection as typed session events, replay rebuilds unresolved pending confirmations on reopen, and approval/rejection write durable resolution events before mutating the owner queue. Stage 8 RPC/interactive exposure is in place for the explicit profile/team/delegation command surface: `list_agent_profiles`/`list_team_profiles` expose resolved registries, `set_default_agent_profile` mutates the session default and emits a semantic profile-change protocol event, `invoke_agent` runs one-off `AgentInvocationFlow`, `invoke_team` runs supervised `AgentTeamFlow`, `list_delegation_confirmations`/`approve_delegation`/`reject_delegation` resolve held delegation requests through the current session owner, and interactive `/delegations` opens a confirmation menu backed by the same owner queue, while `/delegation approve`/`/delegation reject` remain direct commands. Agent/team lifecycle, default-profile-change, and delegation requested/rejected plus confirmation-required/approved/started/completed/failed product events map to semantic RPC protocol events; `CodingEventBridge` renders delegation lifecycle notices in the interactive transcript. Twenty-four-hour stale pending-confirmation filtering is in place; recursive child execution/inherited budget accounting now runs auto-approved nested agent/team delegation with inherited depth budget; delegated child capability release policy is in place; recursive delegation loop detection rejects cycles back to ancestor agent/team targets; nested confirmation-required delegation is queued at the session owner and durable across reopen with inherited depth and lineage preserved. Explicit user invocation should use `/agent` and `/team` menus or `/agent:<id>` and `/team:<id>` slash shortcuts rather than `@agent`/`@team`; single-agent session startup should set the session default profile directly; team profiles must declare supervisor semantics; model-requested delegation must go through `CodingAgentSession`-owned authorization and bounded delegation tools.
- [x] Replace interactive AgentProfile/TeamProfile space-argument slash invocation with menu-driven `/agent` and `/team` plus colon run shortcuts. Bare `/agent` now opens a discovered-profile menu with `Info`, `Use`, and `Run`; bare `/team` opens a discovered-team menu with `Info` and `Run`; menu `Run` enters pending task mode and queues the existing one-off AgentInvocation/AgentTeam requests after task submission. `/agent:<id> <task>` and `/team:<id> <task>` remain direct `Run` equivalents and render those forms in the transcript. Old `/agent use <id>`, `/agent <id> <task>`, and `/team <id> <task>` interactive forms now report menu/colon usage instead of mutating state. RPC/Rust API invocation forms stay preserved for automation.
- [x] Add `ManualCompactionFlow`. P5 introduces `ManualCompactionOptions`, `ManualCompactionContext`, `ManualCompactionOutcome`, and a stable-node `ManualCompactionFlow`; `CodingAgentSession::compact()` now delegates summary selection/model execution/session-event recording to the flow and still commits/fails through `SessionService`.
- [x] Add explicit runtime vs session compaction boundaries. Manual session compaction is a `pi-coding-agent` workflow separate from `pi-agent-core` runtime compaction, prompt-created `operation.committed { new_leaf_id }` remains the new-leaf path, `active_leaf.changed` owns existing-leaf movement, low-level `AgentEvent::SessionCompacted` maps to `RuntimeCompactionCompleted`, and manual session compaction now emits `SessionCompactionCompleted` with protocol `CompactionReason::Manual`.
- [x] Add `BranchSummaryFlow`. Stable-node `BranchSummaryFlow`, `BranchSummaryOptions`/context/outcome types, no-op handling, `branch.summary.created` event/replay support, and `TurnTransaction::record_branch_summary_created` are in place. Replay tracks committed prompt leaf parent/range metadata, tree view honors `active_leaf.changed`, and `CodingAgentSession::summarize_branch()` plus interactive `/branch-summary <source-leaf-id> <target-leaf-id> [instructions]` can select and persist explicit abandoned branch ranges; runtime-backed runs summarize that abandoned range through the provider model with deterministic transcript rendering retained as a scaffold fallback. Interactive `/tree` navigation summarizes the abandoned active leaf before forking to the selected target leaf, hydrates the fork with the created summary, and now reuses an existing source/target branch summary instead of calling the model and rewriting the event log. `SessionService::switch_active_leaf()` owns explicit active-leaf switches to existing committed leaves, separating existing-leaf movement from prompt-created `new_leaf_id`.
- [x] Add Rust-native `ExportFlow`. Session-owned Rust-native export now has public export view types, export capability reporting, interactive adapter reuse for active Rust-native sessions, a stable-node `ExportFlow` wrapper, `FlowService::run_export()`, split render/write helpers, and `CodingAgentSession` current/static export routing through the flow while preserving JSONL export rejection.
- [~] Add `PluginLoadFlow`. Internal slices now cover `PluginLoadOptions`/context/outcome, stable-node `PluginLoadFlow`, in-memory candidate manifest validation, explicit project/user discovery roots for `plugin.toml`, default session-owned project/user root wiring from `cwd` plus `PI_RUST_DIR`, fail-open diagnostics for unreadable/invalid plugin manifests, provider registry merging, `FlowService` execution, session-owner installation/reload through `CodingAgentSession::load_plugins()`/`reload_plugins()` with product diagnostics plus `CapabilityChanged`, interactive `/reload` routing that reloads resources then runs session plugin reload with visible diagnostics, RPC `reload` command routing that returns loaded plugin ids plus diagnostics, a minimal Lua plugin runtime that reads `entry`, executes `register(host)`, registers Lua tool providers, runs Lua tool functions through the existing `AgentTool` path, registers Lua command providers through `host:command(...)`, executes Lua command functions through `PluginService::run_command()`, registers Lua hook providers through `host:hook(...)`, executes Lua hook `run(ctx)` functions through `PluginService::run_prompt_hook()`, persistent plugin loads now record a durable `plugin.load.completed` session event between `operation.started` and `operation.committed`, RPC `plugin_command` dispatch now executes commands from the loaded session plugin service with JSON args/output, interactive `/plugin-command <command-id> [json-args]` dispatch runs loaded plugin commands through the same session-owned service, interactive reload syncs loaded plugin command definitions into slash suggestions so commands can be run directly as slash aliases such as `/lua.say_hello [json-args]`, Lua plugins can register UI actions through `host:ui_action(...)`, dialogs through `host:dialog(...)`, and keybindings through `host:keybind(...)` into the existing `UiProvider`/`KeybindProvider` collections, interactive reload/command completion syncs loaded UI actions/dialogs/keybindings into the interactive root so idle plugin keybindings can dispatch transcript-visible pending UI actions, UI actions whose `action_id` matches a loaded plugin command reuse the existing interactive plugin command runner with empty JSON args, UI actions whose `action_id` matches a loaded dialog id open the controlled interactive dialog runner, opened dialogs prefill `/plugin-command <dialog.action_id>` so direct submit or user-edited JSON args run through the existing command path, and Lua dialog field metadata (`id`/`label`/`description`/`type`/`default`/`required`) now flows into the interactive runner to display fields, prefill default JSON args, validate required fields plus basic JSON types before command execution, support native text-field form editing with field focus navigation, toggle boolean fields from the native form, move focus back to the failing field after dialog validation errors, and render validation errors inline under the relevant form field, filter invalid typed characters from integer/number native form fields before submission, support Lua dialog `select`/`choice`/`enum` options with native Space-to-cycle form behavior plus option validation, and expose stable Lua host API helpers for `host:api_version()`, manifest-scoped `host:plugin()` metadata, path-scoped `host:workspace()` metadata, and feature-scoped `host:capabilities()` metadata without exposing internal session services, raw operation contexts, filesystem handles, shell access, or provider/runtime objects. Richer type-specific controls beyond select fields and richer Lua host APIs remain follow-up.
- [ ] Design and prototype subagent/supervisor flows after runtime control stabilization passes its readiness gate, using the AgentProfile/TeamProfile slash invocation plan as the user-facing entrypoint and session-owned authorization boundary.
- [~] Design and prototype self-healing edit workflow after runtime control stabilization passes its readiness gate. Design and implementation plans are in place; the internal `SelfHealingEditFlow` wraps existing edit behavior with stable Flow nodes and an `ExecutionEnv`-backed operation path, optional check commands now run through injected or session-owned check runners, check failures can drive bounded repair retries through an injected repair strategy, provider-visible builtin `edit` tool calls now route through the workflow while preserving direct low-level `edit_execute()` compatibility, `CodingAgentSession::self_healing_edit()` runs the workflow as a persistent session-owned Rust API with typed lifecycle events plus optional persisted check output, `CodingAgentSession::self_healing_edit_with_options(SelfHealingEditRequest)` is the canonical product API for check-command configuration, bounded planned repair attempts, and bounded session-owned model repair through `SelfHealingEditModelRepairOptions`, check-command failures now return `CodingSessionError::SelfHealingEditFailed` with diagnostics plus structured `SelfHealingEditCheckOutput`, RPC exposes `self_healing_edit.checkCommand`, `repairAttempts`, and bounded `modelRepair`, returns structured `checkOutput` on success, includes structured `checkOutput`/diagnostics in failed check responses, and can execute bounded planned and model repair attempts without exposing raw runner/runtime/provider internals, and interactive mode exposes `/self-healing-edit <path> <oldText> => <newText> [--check <command>] [--model-repair] [--model-repair-attempts N]` through a queued session-owned task with bounded model repair mapped through the current prompt context. Repair attempts are exposed on `SelfHealingEditOutcome` and persisted as typed `self_healing_edit.repair_attempted` events. Self-healing edit lifecycle, repair-attempt, completion, and failure now emit semantic `CodingAgentEvent` values mapped to RPC protocol events and interactive system notices. Repair-attempt product events are emitted from a Flow-time observer as each repair attempt completes, while durable repair-attempt event-log recording remains owned by `CodingAgentSession`. RPC success responses include structured `repairAttempts` details.
- [~] Add workflow capability integration. Persistent-session compact, branch-summary, export, plugin-reload, and self-healing edit capabilities now gate their Rust-native workflow surfaces; persistent workflow capabilities report busy during active operations and disabled without persistent Rust-native session state. Runtime prompt controls now report disabled while no prompt is running and available only for `OperationKind::Prompt`; RPC uses the same runtime-state capability source instead of carrying stale AgentTurnFlow blockers. Delegation capability now reports available for owner-authorized bounded delegation and busy during active owner operations, while concrete delegation tools remain gated by the active profile policy. Plugin loading emits `CapabilityChanged` after session-owned plugin service replacement. Broader workflow capability names for subagent/supervisor remain follow-up.
- [~] Add workflow session event integration. `ManualCompactionFlow` records `session.compaction.started/completed` through `TurnTransaction` and failure records `operation.failed`; branch summary now has a durable `branch.summary.created` event, replay transcript item, runtime hydration, export rendering, transaction recorder, explicit abandoned-range selection from replay leaf metadata, and fork/clone copying that preserves complete branch-summary operations targeted at the requested leaf. Plugin load now records durable `plugin.load.completed` events through a session-owned `PluginLoad` transaction, and plugin diagnostics from that event are available to replay diagnostics. Self-healing edit completion events now optionally persist final check command output when a check command is configured, and repair attempts persist as typed `self_healing_edit.repair_attempted` entries with path, attempt number, replacements, diagnostics, and post-attempt check output. Active leaf/new leaf event taxonomy now distinguishes prompt-created `operation.committed { new_leaf_id }` leaves from service-owned `active_leaf.changed` switches to existing leaves.
- [~] Add Phase 6 tests. `manual_compaction_flow` unit coverage and session-level compact success/failure regression coverage are in place; branch summary now has event serialization, replay leaf metadata, tree-view parent selection, transaction recorder, stable-node FlowService, no-op Flow, explicit abandoned-range summary coverage, provider-backed summary-model coverage, public owner-level `CodingAgentSession::summarize_branch()` persistence/API coverage, automatic navigation summary reuse coverage, `/branch-summary` slash-command pending-request coverage, tree-navigation summarize-before-fork coverage, fork-copy branch-summary operation coverage, public API capability coverage, RPC capability serialization coverage, and SessionService active-leaf switch API coverage. Plugin load now has stable-node FlowService coverage, valid registry merge coverage, invalid manifest diagnostic coverage, project/user manifest discovery coverage, default owner reload-root coverage, Lua missing-entry diagnostic coverage, Lua manifest tool-provider loading and tool execution coverage, Lua manifest command-provider loading and command execution coverage, Lua manifest hook-provider loading and hook execution coverage, Lua manifest UI action/dialog/keybind provider loading coverage, isolated plugin command execution failure coverage, owner-level plugin-service installation coverage, persistent `plugin.load.completed` event-log coverage, interactive `/reload` manifest-diagnostic coverage, RPC `reload` manifest-diagnostic coverage, RPC `plugin_command` Lua command dispatch coverage, interactive `/plugin-command` Lua command dispatch coverage, interactive plugin command slash alias/suggestion coverage, root-level interactive plugin keybinding dispatch coverage, root-level UI-action-to-plugin-command runner coverage, root-level UI-action-to-dialog runner coverage, interactive Lua dialog keybinding coverage, interactive dialog submit scaffold coverage, dialog field/default JSON submission scaffold coverage, dialog required/type validation coverage, native dialog text-field form/focus coverage, boolean field toggle coverage, validation-error focus coverage, and inline dialog validation display coverage, integer numeric-field input filtering coverage, select-field option cycling/validation coverage, Lua host API metadata, workspace metadata, and capabilities metadata coverage, and public/RPC `pluginReload` capability coverage, and runtime control capability cleanup coverage for idle/running prompt-control gates plus public API/RPC serialization. Operation-control owner-slimming coverage now verifies handle issuance, receiver transfer, busy rejection, and receiver clearing at the operation-control boundary. ExportFlow coverage verifies stable node ids, FlowService HTML export behavior, current-session export operation-boundary busy rejection, JSONL rejection, session transcript HTML output, and interactive Rust-native export reuse. Runtime/session compaction boundary coverage verifies manual session compaction emits `SessionCompactionCompleted`, protocol mapping reports `CompactionReason::Manual`, and interactive bridge rendering stays compatible. Profile/team/delegation capability coverage now verifies public API symbols and RPC `get_state` serialization for `agentProfiles`, `teamProfiles`, and delegation available/busy status. RPC profile/team command coverage now verifies `list_agent_profiles`/`list_team_profiles` registry serialization, project profile discovery, default profile marking, delegation policy fields, supervisor fields, list-read session side-effect avoidance, `set_default_agent_profile` success/list reflection plus semantic profile-change protocol event streaming, unknown-profile rejection, busy rejection while a prompt is running, `invoke_agent` and `invoke_team` response/event streaming, semantic agent/team lifecycle protocol events, semantic delegation requested/rejected protocol event serialization, pending delegation confirmation list/approve/reject command behavior, unknown target rejection, and `agent_invocation`/`agent_team` busy capability reporting. Interactive runtime-control coverage now verifies running Shift+Enter follow-up submission at the root/input layer and through the scripted interactive provider path while preserving running Enter-to-steer behavior. AgentProfile runtime coverage now verifies ordinary prompts apply the selected default profile's model, system prompt, tool allowlist, and unavailable tool/skill diagnostics through session-owned runtime construction. AgentProfile/TeamProfile user documentation now covers discovery roots, schema, precedence, slash/RPC commands, protocol events, and current delegation safety boundaries. Interactive AgentProfile slash coverage now verifies built-in `/agents`/`/agent`/`/teams`/`/team` suggestions, `/agents` listing with current default marking, `/agent` menu root/Use/Run behavior, `/team` menu root/Run behavior, `/agent use` usage rejection without profile selection, one-off `/agent:<id> <task>` and `/team:<id> <task>` request queuing, old one-off `/agent <id> <task>` and `/team <id> <task>` usage rejection, `@agent`/`@team` ordinary prompt submission, built-in-command precedence over same-named plugin aliases, and scripted one-off agent/team invocation rendering. AgentInvocationFlow coverage verifies stable node ids, target profile runtime policy application, parent transcript isolation, unknown-profile failure events, child failure event de-duplication, and interactive Ctrl-C cancellation of a running child prompt through owner-issued prompt control. AgentTeamFlow coverage verifies stable node ids, two-member success, profile-backed supervisor execution, unknown-member failure events, unknown-supervisor failure events, child runtime failure events, parent transcript isolation, and coherent child operation lineage; interactive `/team:<id> <task>` coverage verifies scripted team invocation rendering. Controlled delegation request-tool coverage now verifies allowed requests, disallowed target rejection, zero-depth rejection, dynamic parameter schema fields, provider-visible delegation tools under profile policy, retired unavailable-delegation warnings, `DelegationRequested`/`DelegationRejected` mapping from tool envelopes while preserving ordinary tool lifecycle events, typed `PromptTurnContext` queueing for accepted delegation requests while rejected requests stay out of the execution queue, typed authorization decisions for approved, confirmation-required, child-limit-rejected, and depth-rejected queued requests, auto-approved delegation execution coverage verifies owner-created child agent/team flow execution plus approved/started/completed product/protocol events, confirmation-required coverage verifies held requests emit semantic events without child execution and appear in the owner pending queue, session owner confirmation approval/rejection coverage verifies approved pending requests execute child work while rejected requests emit `DelegationRejected`, child-failure coverage verifies approved failed child agent delegation emits `DelegationFailed` plus protocol serialization without a false completion, and nested child-depth coverage verifies delegated child requests are explicitly rejected when `max_depth` is exhausted. Durable delegation confirmation coverage now verifies stable request/approval/rejection event serialization, replay-derived pending queues, reopen listing, restored approval execution, and restored rejection without child execution. Interactive delegation confirmation coverage now verifies slash-command queueing, event-bridge notices, root-level `/delegations` menu approve/reject command generation, and scripted approve execution through the menu-backed owner queue. Stale persistent delegation confirmation coverage now verifies replay can recover an old request while session-owner pending lookup filters it and refuses approval; recursive child execution/inherited budget accounting coverage now verifies approved nested delegation executes until inherited `max_depth` is exhausted; delegated child capability release coverage now verifies child prompts do not inherit parent runtime tools without an explicit target-profile allowlist; recursive delegation loop detection coverage now verifies child requests back to an ancestor profile are rejected without starting child work; durable nested confirmation queue coverage now verifies nested confirmation requests queue at the session owner, survive persistent session reopen, and approve into child work with inherited depth and lineage preserved; self-healing edit internal Flow coverage now verifies stable node ids, successful edit output, validation failure without writes, `ExecutionEnv`-backed operations, successful check-command output recording, failed check diagnostics without repair, bounded repair retry success, persisted check-output lifecycle events, provider-visible builtin edit workflow metadata, provider-visible edit error compatibility, and public API importability for check-output/repair-attempt types, and model repair event-log coverage for `self_healing_edit.repair_attempted`, session product-event emission coverage, protocol self-healing edit event serialization coverage, interactive self-healing edit system-notice coverage, RPC repair-attempt response coverage, Flow-time repair observer coverage, and session duplicate-guard coverage for repair-attempt product events.

## Cross-Cutting TODO

- [x] Update `docs/roadmap/cross-cutting.md` to remove TS session compatibility as a current invariant. Cross-cutting docs now name Rust-native `session.json` + typed `events.jsonl` as the current format and treat TS JSONL as historical/rejected input.
- [x] Add a dedicated Rust-native session format doc once Phase 1 schema stabilizes. `docs/superpowers/specs/2026-07-01-rust-native-session-format.md` records the current manifest, event envelope, event kinds, replay invariants, and versioning guidance.
- [x] Prefer retiring migrated legacy paths over preserving compatibility. Current roadmap/cross-cutting/architecture docs now state that old TypeScript and old Rust runner paths are behavioral references or explicit rejection targets, not compatibility requirements.
- [~] Plan and harden `pi-ai` scoped runtime/auth/public API boundary. Boundary policy, stable facade modules, public API smoke tests, a Rust-native `AiClient`, scoped `ProviderRegistry`, injectable `ProviderAuthResolver`, and a compatibility wrapper boundary for global `register()`/`stream_model()` are in place; catalog/register invariant planning and further migration away from global runtime state continue through P1-P4. Keep `pi-ai` free of coding-agent product/session/adapter semantics.
- [~] Plan and harden `pi-agent-core` low-level runtime/public API boundary. Boundary policy, stable facade modules, public API smoke tests, and explicit `AgentTool::validate()` / `Agent::try_add_tool()` tool definition validation are in place; deeper runtime/tool invariants continue through P1-P4. Target narrowed public exports around `Agent`, `AgentConfig`, `AgentTool`, `AgentEvent`, hooks, and `ExecutionEnv`; keep `AgentTurnFlow` internals and legacy JSONL session modules out of the stable product contract; route product event lifecycle, session compaction, branch summary, and tree ownership through `pi-coding-agent`.
- [~] Plan and harden `pi-coding-agent` product owner/public API/workflow boundary. Boundary policy, stable facade modules, and public API smoke tests are in place; deeper runtime/auth/tool/UI invariants continue through P1-P4. Target `pi_coding_agent::api` as the stable facade, keep root modules and adapter internals migration-private, route shared product operations through `CodingAgentSession` services/flows, express adapter features through `CodingAgentCapabilities`/protocol versions, and advance plugin command/UI/keybind execution plus Phase 6 workflows without reviving old JSONL/session-runner compatibility.
- [~] Plan and harden `pi-tui` generic TUI/public API/interactive adapter boundary. Boundary policy, stable facade modules, public API smoke tests, and trait-object-backed `Component` downcast behavior are in place; deeper runtime/tool/UI invariants continue through P1-P4. Target a Rust-native generic terminal UI crate, move `app.*` keybindings and product UI semantics into `pi-coding-agent`, define the stable public facade/module policy, route plugin UI/keybind execution through capability-scoped `pi-coding-agent` adapters, and keep terminal tests deterministic with opt-in smoke coverage.
- [x] Keep `pi-agent-core` free of coding-agent product ownership. Source and dependency audit confirmed `pi-agent-core` has no `pi-coding-agent` dependency and no imports/usages of `CodingAgentSession`, `CodingAgentEvent`, coding-session services, protocol adapters, or interactive adapter ownership; product/session/workflow ownership remains in `pi-coding-agent`.
- [~] Keep `CodingAgentSession` as owner/coordinator, not a monolithic implementation class. Runtime control stabilization makes owner slimming an explicit gate; operation-control guard/control handle splitting is in place, prompt-control receiver lifecycle now lives behind `OperationControl`, and remaining workflow glue splits are pending.
- [~] Keep plugin/Lua APIs from depending on internal operation contexts. Lua host APIs currently expose version, manifest metadata, path-scoped workspace metadata, and feature-scoped capabilities metadata through explicit helpers, without raw session services, operation contexts, filesystem handles, shell access, or provider/runtime objects; broader API audit remains pending.
- [x] Keep product event adapters independent from concrete Flow node IDs. Protocol/interactive adapter audit found Flow node IDs confined to Flow implementation and tests; protocol regression coverage now serializes representative `CodingAgentEvent` streams and rejects Flow-node-shaped protocol fields.
- [~] Keep all tests deterministic and offline unless explicitly marked as smoke/opt-in. Latest slice audited high-risk test signals, hardened `pi-ai` env-key/provider missing-key tests with serialized restoring env guards, and removed wall-clock temp directory suffixes from `pi-tui` autocomplete tests. Remaining follow-up: audit and harden `pi-coding-agent` process-wide env-mutation tests plus any timing-sensitive assertions before closing this invariant.

## Progress Log

- 2026-07-04: Phase 2 closure audit completed. `PromptTurnFlow`, `CodingAgentSession::prompt()`, print/json routing, Rust-native session persistence, and the old `session_runner` cleanup are now marked complete in the active TODO.
- 2026-07-04: Phase 3 closure audit completed. RPC/interactive adapter convergence, Rust-native session actions, product-event bridges, cwd-scoped session filtering, and related test/check gates are now marked complete in the active TODO.
- 2026-07-04: Deterministic/offline test-boundary hardening started. `pi-ai` env-key/provider missing-key tests now use serialized restoring env guards, `pi-tui` autocomplete temp roots no longer depend on wall-clock uniqueness, and the cross-cutting item remains open for the discovered `pi-coding-agent` env-mutation follow-up.
- 2026-06-29: Minimal `pi_agent_core::flow` exists.
- 2026-06-29: Flow-centered runtime architecture design committed.
- 2026-06-29: Flow-centered implementation plan committed.
- 2026-06-29: Six phase implementation guides and interface review committed.
- 2026-06-29: Phase 1 `CodingAgentSession` shell, product event/error boundary, EventService, API exports, and focused smoke tests added.
- 2026-06-29: Phase 1 Rust-native session manifest, typed session event envelope/data, and deterministic ID/clock test boundaries added.
- 2026-06-29: Phase 1 `SessionLogStore` added for Rust-native session directories, manifest I/O, JSONL event append/read, and manifest updates.
- 2026-06-29: Phase 1 `TurnTransaction` added for typed operation event buffering, commit/abort/fail finalization, lifecycle cancellation, and post-commit active leaf updates.
- 2026-06-29: Phase 1 replay and owner persistence boundary completed: `CodingAgentSession` create/open now use Rust-native session directories, create appends `session.created`, and `SessionLogStore` can replay canonical `events.jsonl` into a transcript.
- 2026-06-29: Phase 1 focused checks passed: `cargo fmt --check`, `cargo test -p pi-coding-agent coding_session`, `cargo test -p pi-coding-agent public_api`, `cargo check --workspace`, and `cargo test --workspace`.
- 2026-06-29: Phase 2 started with `PromptTurnOptions`, `PromptTurnOutcome`, `PromptTurnContext`, `RuntimeSnapshot`, coding-session unit tests, public API smoke coverage, and passing fmt/check/workspace tests for the slice.
- 2026-06-29: Phase 2 `PromptTurnFlow` graph skeleton added with stable node IDs, no-op boundary nodes, FlowService construction/run entrypoints, graph-focused coding-session tests, and passing fmt/check/workspace tests for the slice.
- 2026-06-29: Phase 2 AgentEvent-to-CodingAgentEvent mapping added for turn/provider/assistant/tool/error/compaction events, with EventService emission coverage and focused coding-session checks passing for the slice.
- 2026-06-29: Phase 2 RunAgentTurn node now drives a staged Agent runtime with the existing Agent stream APIs, records final assistant messages and mapped product events in PromptTurnContext, and has faux-provider graph coverage.
- 2026-06-29: Phase 2 RunAgentTurn now records assistant deltas/completion and tool lifecycle observations into pending Rust-native session events through TurnTransaction without flushing before FinalizeTurn.
- 2026-06-29: Phase 2 `CodingAgentSession::prompt()` added for runtime-backed prompt options: RuntimeService builds Agent from RuntimeSnapshot, PromptTurnFlow runs through RunAgentTurn and FinalizeTurn, product events are emitted, and committed Rust-native session events replay successfully in focused tests.
- 2026-06-29: Phase 2 print mode routed explicit enabled `New` session targets through `CodingAgentSession` and wrote Rust-native `session.json`/`events.jsonl`; later target convergence removed old CLI runner fallback.
- 2026-06-29: Phase 2 JSON mode began rendering protocol output from `CodingAgentEvent` via `CodingProtocolEventAdapter`; later JSON execution convergence moved execution under `CodingAgentSession`.
- 2026-06-29: Phase 2 RecordUserInput node now persists prompt input through TurnTransaction before RunAgentTurn; `CodingAgentSession::prompt()` replay now includes both user input and assistant output.
- 2026-06-29: Phase 2 replay hydration now feeds completed Rust-native user/assistant transcript items into the Agent runtime before the next prompt; reopened-session tests verify prior context reaches the provider.
- 2026-06-29: Phase 2 tool-call replay hydration now persists tool arguments in Rust-native `tool.call.started` events, folds them into transcript tool-call items, and restores completed/failed assistant tool-call plus tool-result groups before reopened-session prompts.
- 2026-06-29: Phase 2 BuildAgentRuntime is now a real PromptTurnFlow node: `CodingAgentSession::prompt()` passes replay into context, the graph builds/hydrates the Agent from RuntimeSnapshot, and graph/service tests exercise the node-owned runtime boundary.
- 2026-06-29: Phase 2 Rust-native session target groundwork added: `SessionLogStore` can try-open by explicit id and list native session manifests, `SessionService`/`CodingAgentSession` expose open-or-create plus list summaries, and public API smoke coverage verifies the new owner-level entrypoints.
- 2026-06-29: Phase 2 ResolveRuntime is now a real PromptTurnFlow node: `PromptTurnContext` no longer eagerly carries runtime from options, the node attaches RuntimeSnapshot at the graph boundary, and focused tests cover successful attachment plus missing-runtime errors.
- 2026-06-29: Phase 2 PrepareInput is now a real PromptTurnFlow node: prompt invocations are normalized into prepared persisted input before recording, RecordUserInput requires that prepared input, and focused tests cover normal text input, empty-input rejection, and misordered record-node execution.
- 2026-06-29: Phase 2 LoadResources is now a real PromptTurnFlow node: the graph attaches the runtime `AgentResources` snapshot into turn state, BuildAgentRuntime now requires the resources stage, and focused tests cover successful resource loading, missing-runtime errors, and skipped-resource build failures.
- 2026-06-29: Phase 2 EmitCompletion is now a real PromptTurnFlow node: successful graph runs append `PromptCompleted` through `PromptTurnContext`, owner-level outcome emission is a missing-event fallback, and focused tests cover completion emission, idempotency, and missing-final-message errors.
- 2026-06-29: Phase 2 print session target convergence design added. The design prioritizes Rust-native `CodingAgentSession` ownership for migrated print session targets and treats temporary unmigrated paths as cleanup targets, not a compatibility requirement.
- 2026-06-29: Phase 2 print session target convergence implemented: enabled default/New/OpenTarget id/path/OpenOrCreateId/ContinueMostRecent print session targets route through `CodingAgentSession`, ForkTarget now fails explicitly until Rust-native fork semantics exist, OpenSession validates owner-prepared session state, and focused print/session/coding-session checks pass.
- 2026-06-30: Phase 2 ResolveRequest node design added. The design keeps adapter parsing outside the graph while making the flow validate runtime-backed `PromptTurnOptions` before input preparation and runtime attachment.
- 2026-06-30: Phase 2 ResolveRequest is now a real PromptTurnFlow node: it validates runtime-backed prompt options, rejects empty text/content and manual compaction, marks request resolution idempotently, and makes PrepareInput/ResolveRuntime fail clearly if run before request resolution.
- 2026-06-30: Phase 3 started with a concrete `CodingAgentCapabilities`/`CapabilityStatus` model. `CodingAgentSession::capabilities()` now reports prompt availability/busy state and explicit unsupported reasons for unmigrated adapter/session/plugin capabilities.
- 2026-06-30: Phase 3 RPC capability reporting added: `get_state` includes protocol-stable capability status objects so clients can distinguish available, disabled, unsupported, and busy operations before RPC prompt execution migrates to `CodingAgentSession`.
- 2026-06-30: Phase 3 RPC product-event adapter added. `RpcCodingEventAdapter` provides the RPC boundary for converting `CodingAgentEvent` into existing protocol events.
- 2026-06-30: Phase 3 enabled-session RPC prompt migration started: RPC prompts with `SessionMode::Enabled` run through `CodingAgentSession`, stream product events through `RpcCodingEventAdapter`, and persist Rust-native `session.json`/`events.jsonl`; disabled/no-session RPC prompts moved to non-persistent product runtime later.
- 2026-06-30: Session finalization convergence design added. The design moves prompt transaction commit/fail/abort ownership back under `SessionService` and makes `SessionWrite*` product events part of the persistent prompt event stream before final prompt outcome events.
- 2026-06-30: Non-persistent product runtime design added. The design keeps `CodingAgentSession` as product owner when durable persistence is disabled, uses the same `PromptTurnFlow`, emits `SessionWriteSkipped`, and gives no-session print/RPC plus later JSON execution convergence a path into product runtime ownership.
- 2026-06-30: Session finalization convergence implemented. `SessionService` now owns prompt transaction commit/fail/abort/skip finalization, `FinalizeTurn` only validates readiness, owner-level prompt events emit `SessionWritePending`/`SessionWriteCommitted` before `PromptCompleted` or `PromptFailed`, and focused coding-session/print/RPC/protocol checks pass.
- 2026-06-30: Non-persistent product runtime implemented for core owner, print, and RPC prompt paths. `CodingAgentSession::non_persistent()` runs `PromptTurnFlow` without durable session storage, emits `SessionWriteSkipped`, supports owner-lifetime transcript hydration, routes no-session/disabled print through product runtime, and routes disabled-session RPC through `RpcCodingEventAdapter` with no session file.
- 2026-06-30: Interactive CodingEventBridge design added. The bridge is scoped to translating `CodingAgentEvent` into existing `UiEvent` values and was later wired into interactive prompt task ownership.
- 2026-06-30: Interactive CodingEventBridge implemented. Product assistant/tool/failure/abort/compaction events map into existing `UiEvent` values with deterministic bridge coverage and now back the interactive prompt path.
- 2026-06-30: Interactive primary prompt migration implemented. Ordinary interactive text/content prompts run through `CodingAgentSession`, use `CodingEventBridge` for UI events, persist enabled-session prompts as Rust-native `session.json`/`events.jsonl`, reuse the owner across multiple prompts, keep no-session prompts non-persistent, and now reject old JSONL session targets.
- 2026-06-30: Interactive Rust-native resume hydration added. `SessionService` exposes an internal replay-backed hydration view, interactive startup `--resume` and `/resume` use Rust-native `session.json`/`events.jsonl` transcript hydration, and old JSONL resume files are ignored.
- 2026-06-30: Adapter-provided cwd now flows into Rust-native session creation and list filtering. `CodingAgentSessionOptions` carries cwd, `SessionService` records it in `session.created`, print/RPC/interactive prompt paths pass their configured cwd, and interactive Rust-native choices are workspace-cwd scoped instead of only session-root scoped.
- 2026-06-30: JSON execution now runs through the product runtime path via `CodingAgentSession::prompt()`. JSON mode still emits the existing protocol wire through `CodingProtocolEventAdapter`, enabled sessions write Rust-native logs, and focused JSON/protocol event checks pass.
- 2026-06-30: Interactive Rust-native session actions advanced: newly created and resumed Rust-native sessions are tracked as active `SessionChoice`s, `/session` reports Rust-native session details, `/tree` opens a read-only tree projection from `SessionService` hydration, and Rust-native fork/clone/compact/tree navigation now fail with explicit unsupported boundaries instead of falling through to legacy JSONL assumptions.
- 2026-06-30: Rust-native active leaf commit design added. The design makes successful persistent prompt commits allocate and persist a real `leaf_id` through `SessionService`/`TurnTransaction` before implementing fork, clone, compact, or tree navigation actions.
- 2026-06-30: Rust-native active leaf commits implemented. Successful persistent `CodingAgentSession::prompt()` calls now allocate a `leaf_id` in `SessionService`, commit it through `TurnTransaction`, refresh session hydration/list summaries, return it in `PromptTurnOutcome`, and propagate it to RPC/interactive active-session state.
- 2026-07-01: Prompt execution now broadcasts `CodingAgentEvent` values live while `PromptTurnFlow` runs, including assistant thinking deltas for TUI/RPC adapters. Rust-native assistant persistence no longer writes per-delta `message.delta` events; `message.completed` now stores final content blocks, including thinking blocks, under event schema version 2.
- 2026-06-30: Rust-native session fork/clone design added. The design adds `SessionService`-owned clone/fork APIs that create independent Rust-native sessions from committed leaves by rewriting durable source events and recording typed provenance events.
- 2026-06-30: Rust-native session fork/clone implemented for interactive active sessions. `SessionService` now creates independent cloned/forked Rust-native sessions from committed leaves, records `session.cloned`/`session.forked` provenance, rewrites copied event history to the new session id, updates active leaf manifests, and interactive `/clone` and `/fork` select the new session instead of reporting unsupported.
- 2026-06-30: Rust-native manual compaction design added. The design keeps manual compaction separate from `PromptTurnFlow`, persists typed Rust-native compaction events through `SessionService`, folds compaction during replay, and wires interactive `/compact` for active Rust-native sessions without any old JSONL compaction fallback.
- 2026-06-30: Rust-native manual compaction implemented for active interactive sessions. `CodingAgentSession::compact()` now summarizes replayed Rust-native context through the existing compaction summarizer, writes `session.compaction.started`/`session.compaction.completed` events, folds replay into a compaction summary plus kept tail, emits product compaction events through `CodingEventBridge`, and `/compact` uses only the Rust-native product runtime for Rust-native sessions.
- 2026-06-30: Print-mode `ForkTarget` now routes through Rust-native session ownership. Enabled print mode opens the source Rust-native session, forks it through `SessionService`, opens the new forked session, hydrates source transcript into the provider context, records `session.forked`, and keeps the source session unchanged.
- 2026-06-30: Rust-native tree navigation implemented as leaf-backed fork-on-select. `SessionService::tree_view` builds `/tree` nodes from real committed `leaf_id` values, interactive `/tree` opens that view for Rust-native sessions, selecting the current leaf reports no-op, and selecting a historical leaf forks a new Rust-native session at that leaf instead of using temporary projection ids.
- 2026-07-01: Old JSONL execution cleanup removed the `session_runner` module and public exports, routed interactive prompt/compact/session actions exclusively through `CodingAgentSession`, stopped advertising `/import`, rejects JSONL import/export/session targets, and keeps regression coverage that legacy JSONL resume files are ignored.
- 2026-07-01: Interactive transcript rendering now treats tool starts and new agent turns as assistant stream boundaries, preventing post-tool assistant deltas from rewriting the previous assistant block and preserving assistant/tool/assistant visual order in TUI output.
- 2026-07-01: Phase 4 started with the `pi-agent-core::agent_turn_flow` module and `AgentTurnContext` snapshot boundary. `Agent::run()` still delegates to the existing loop while future extraction nodes get a stable low-level state container.
- 2026-07-01: Phase 4 `PrepareContextNode` added. It builds provider request snapshots from `AgentTurnContext` with existing conversion/stream-option helpers, giving the next runtime-compaction extraction a node-shaped boundary without changing `Agent::run()`.
- 2026-07-01: Phase 4 runtime compaction node added. `MaybeCompactRuntimeContextNode` runs auto-compaction against `AgentTurnContext`, preserves existing summarization/threshold behavior, emits low-level `SessionCompacted` into the context event buffer, and still leaves `Agent::run()` on the existing loop.
- 2026-07-01: Phase 4 provider stream node added. `ProviderStreamNode` streams prepared provider requests from `AgentTurnContext`, buffers `LlmEvent` and provider error events, stores the final assistant message for downstream decision nodes, and still leaves `Agent::run()` on the existing loop.
- 2026-07-01: Phase 4 decide-stop-or-tools node added. `DecideStopOrToolsNode` consumes the streamed assistant message, appends it to context messages, emits terminal low-level events for done/error/aborted cases, extracts pending tool calls for the future tool execution node, and still leaves `Agent::run()` on the existing loop.
- 2026-07-01: Phase 4 initial tool execution node added. `ExecuteToolsNode` consumes pending tool calls from `AgentTurnContext`, emits start/end tool events, records successful and unknown-tool results, appends tool-result messages, and still leaves parallel execution, tool hooks, update callbacks, terminate behavior, and `Agent::run()` delegation for follow-up slices.
- 2026-07-01: Phase 4 `ExecuteToolsNode` now honors sequential vs parallel tool execution selection, emits parallel tool end events in completion order, and still appends tool-result messages in assistant order; hook, update callback, terminate, and `Agent::run()` delegation parity remain follow-up work.
- 2026-07-01: Phase 4 tool execution node parity advanced: `ExecuteToolsNode` applies before/after tool hooks, emits sequential tool update callbacks, and finishes with `AgentDone` when all tool results terminate. Remaining Phase 4 focus is preserving full `AgentEvent` behavior and delegating `Agent::run()` to `AgentTurnFlow`.
- 2026-07-01: Phase 4 runtime delegation completed. `AgentTurnFlow` now exposes the low-level runtime entrypoint, `Agent::run()` delegates to it, and the old `agent_loop` module is reduced to a compatibility wrapper over the AgentTurnFlow runtime.
- 2026-07-01: Documentation cleanup aligned roadmap/cross-cutting/architecture docs with the Flow-centered runtime state, removed TS session JSONL compatibility as a current invariant, and added the Rust-native session format spec.
- 2026-07-01: Phase 5 started with the internal Rust plugin kernel slice: `crate::plugins` now has plugin metadata/source types, `PluginRegistry`, `ToolProvider`, and `ToolRegistrationHost`; `PluginService` collects plugin tools with error/panic isolation; `PromptTurnContext` carries the session-owned plugin service into `RuntimeService`; focused plugin tests pass.
- 2026-07-01: Phase 5 command boundary started. `CommandProvider`, `CommandDefinition`, `CommandRegistrationHost`, registry storage, and `PluginService::collect_commands()` now exist as internal staging APIs with diagnostics for provider failures; command execution/adapters remain follow-up work.
- 2026-07-01: Phase 5 completion plan added. The remaining flow is capability reporting, prompt hook provider integration, UI/keybind boundaries, reserved FlowExtension points, and a guide-level completion audit.
- 2026-07-01: Phase 5 plugin capability reporting added. `PluginService::capabilities()` now summarizes provider/diagnostic counts through `PluginCapabilities`, and `CodingAgentSession`/RPC capability reporting marks the plugin kernel available.
- 2026-07-01: Phase 5 hook provider boundary started. `HookProvider`, `HookRegistration`, `PromptHookPoint`, `HookFailurePolicy`, `HookRegistrationHost`, registry storage, hook collection, capability counts, and provider failure diagnostics are in place.
- 2026-07-01: Phase 5 prompt hook execution added. `PromptTurnFlow` now invokes scoped plugin hooks at prompt preparation, resource loading, agent turn, finalization readiness, and completion boundaries; fail-open hook outcomes/errors emit coding diagnostics, and fail-closed hook failures stop the flow with a plugin error. Durable post-commit hook results remain a later owner-level integration concern.
- 2026-07-01: Phase 5 UI/keybind boundaries added. `UiProvider`, `UiActionDefinition`, `UiDialogDefinition`, `KeybindProvider`, and `KeybindDefinition` now exist as internal capability-scoped plugin boundaries with registry storage, collection through `PluginService`, provider counts, and returned-error diagnostics; rendering and real key dispatch remain out of this slice.
- 2026-07-01: Phase 5 FlowExtension boundary reserved. `FlowExtension` now exposes only named `FlowExtensionPoint` values, `FlowExtensionRegistrationHost`, registry storage, capability counts, and collection/failure-isolation through `PluginService`; arbitrary Flow graph rewrites and Lua mutation hooks remain unavailable.
- 2026-07-01: Phase 5 plugin kernel completed. The handoff now has registry storage for tool/command/hook/UI/keybind/FlowExtension providers, scoped registration hosts, runtime tool merging, PromptTurnFlow hook execution, reserved FlowExtension points without graph rewrites, plugin capability reporting, and plugin failure isolation through diagnostics or typed errors.
- 2026-07-02: `pi-ai` TypeScript parity report reframed as historical context. Current planning now prioritizes Rust-native scoped provider runtime, centralized provider auth resolution, global registry compatibility boundaries, and catalog/register invariant work over TS SDK or provider-count parity.
- 2026-07-02: `pi-agent-core` TypeScript parity report reframed as historical context. Current planning now treats `pi-agent-core` as the low-level `AgentTurnFlow` runtime, prioritizes public API hardening, tool validation contracts, `ExecutionEnv` usage, and clear delegation of product session/workflow ownership to `pi-coding-agent` over TS `AgentHarness` parity.
- 2026-07-02: `pi-coding-agent` TypeScript parity report reframed as historical context. Current planning now treats `CodingAgentSession`/services/flows/`CodingAgentEvent` as the Rust-native product owner surface, prioritizes stable `api` facade hardening, adapter capability/version contracts, plugin product integration, and Phase 6 workflows over TS SDK/root export or old JSONL runner parity.
- 2026-07-02: `pi-tui` TypeScript parity report reframed as historical context. Current planning now treats `pi-tui` as a Rust-native generic terminal UI crate, prioritizes public API and component trait hardening, removal of coding-agent `app.*` semantics from the base crate, controlled plugin UI/keybind adapter integration, and deterministic terminal regression coverage over TS `TUI` lifecycle/API shape parity.
- 2026-07-01: P0 crate boundary hardening started. The design and implementation plan now define stable facade direction, migration-private surfaces, non-goals, and verification gates for `pi-ai`, `pi-agent-core`, `pi-coding-agent`, and `pi-tui` before P1-P4 code hardening proceeds.
- 2026-07-01: P0 facade slice added. `pi-ai`, `pi-agent-core`, and `pi-tui` now expose stable `api` facade modules with public API smoke tests; existing `pi-coding-agent::api` coverage was verified, and deeper scoped runtime/auth/tool/UI invariants remain assigned to P1-P4.
- 2026-07-01: P1 export boundary slice added. Rust-native session HTML export is now owned by `CodingAgentSession`/`SessionService`, public export view types are exposed through `pi_coding_agent::api`, export capability reports available for persistent sessions and disabled for non-persistent sessions, and interactive `/export` reuses the session-owned exporter when an active Rust-native session exists.
- 2026-07-01: P2 `pi-ai` scoped runtime/auth slice added. `AiClient` owns an isolated `ProviderRegistry`, uses injectable `ProviderAuthResolver` for API-key resolution, leaves global `register()`/`stream_model()` as compatibility wrappers over a global registry, and has public API coverage proving scoped providers do not leak into global state.
- 2026-07-01: P3 `pi-tui` component downcast slice added. `Component` now inherits `Any`, `dyn Component` owns shared `as_any`/`as_any_mut` helpers, built-in components and the coding-agent root no longer need repeated downcast methods, and a regression test covers generic components without manual downcast implementations.
- 2026-07-01: P4 `pi-agent-core` tool definition boundary slice added. `AgentTool::validate()` reports typed field errors for invalid tool definitions, `Agent::try_add_tool()` validates before mutating runtime tool state, the stable API facade exports `AgentToolDefinitionError`, and public API coverage verifies invalid tools do not enter provider context.
- 2026-07-01: P5 `ManualCompactionFlow` slice added. Manual session compaction now has operation options/context/outcome, a stable-node Flow graph, session-event recording through the compaction transaction, `FlowService` execution helpers, and session-level success/failure regression coverage proving summary failures do not fold replay or move the active leaf.
- 2026-07-01: P6 branch summary owner command slice added. Public `CodingAgentSession::summarize_branch()` now runs `BranchSummaryFlow` for explicit source/target leaves, commits `branch.summary.created` without changing tree navigation semantics, and interactive `/branch-summary <source-leaf-id> <target-leaf-id> [instructions]` starts the Rust-native summary task.
- 2026-07-02: P6 tree navigation branch-summary slice added. Interactive `/tree` navigation now summarizes the abandoned active leaf before forking to the selected target, hydrates the new fork with the created summary, and preserves complete branch-summary operations when copying target-leaf history.
- 2026-07-02: P6 branch-summary capability slice added. `CodingAgentCapabilities` and RPC `get_state` now expose `branchSummary`, disable it for non-persistent sessions, and report persistent workflow capabilities as busy while an operation is active.
- 2026-07-02: P6 active-leaf taxonomy slice added. `SessionService::switch_active_leaf()` validates committed leaves, appends `active_leaf.changed`, updates `session.json.active_leaf_id`, and keeps prompt-created `operation.committed { new_leaf_id }` as the only new-leaf creation path.
- 2026-07-02: P6 branch-summary automatic navigation policy slice added. Automatic `/tree` navigation now reuses an existing complete source/target branch summary without consuming the provider model or appending duplicate `branch.summary.created` events before forking.
- 2026-07-02: P6 PluginLoadFlow first slice added. Internal plugin load graph validates in-memory plugin manifests, merges valid plugin registries, reports invalid manifests as diagnostics, installs the resulting `PluginService` on `CodingAgentSession`, and emits `CapabilityChanged` for loaded capability changes.
- 2026-07-02: P6 PluginLoadFlow manifest discovery slice added. `PluginLoadOptions` can now carry explicit project/user discovery roots; the flow discovers sorted `plugin.toml` files from roots or one-level plugin directories, parses TOML id/name/version/runtime metadata, reports unreadable/invalid manifests as diagnostics, and defers discovered Lua plugins without executing them.
- 2026-07-02: P6 PluginLoadFlow owner reload slice added. `CodingAgentSession` now derives default plugin discovery roots from session `cwd` and `PI_RUST_DIR`, preserves them across forked session owners, and exposes an internal `reload_plugins()` entrypoint that emits diagnostics and capability-change events through the session owner.
- 2026-07-02: P6 interactive plugin reload slice added. Interactive `/reload` now rebuilds keybindings/resources, starts an async session plugin reload task, keeps the reloaded `CodingAgentSession` for subsequent prompts, and reports plugin reload diagnostics in the TUI transcript.
- 2026-07-02: P6 RPC plugin reload slice added. RPC now accepts `reload`, runs `CodingAgentSession::reload_plugins()` through the current or newly opened session owner, and returns loaded plugin ids, diagnostics, and capability-change status in the command response.
- 2026-07-02: P6 Lua plugin tool slice added. Lua plugin manifests can now declare a relative `entry`; `PluginLoadFlow` reads the entry, executes `register(host)` in an embedded Lua runtime, exposes `host:tool(...)` registrations as `AgentTool`s, and keeps missing-entry or load failures as fail-open plugin diagnostics.
- 2026-07-02: P6 plugin load session-event slice added. Persistent `CodingAgentSession::load_plugins()` now opens a `PluginLoad` transaction, records `plugin.load.completed` with loaded plugin ids, diagnostics, and capability-change status, commits it as a non-leaf operation, and folds persisted plugin diagnostics into replay diagnostics.
- 2026-07-02: P6 Lua plugin command slice added. `CommandProvider` now has isolated execution through `PluginService::run_command()`, Lua plugins can register `host:command(...)` definitions, and Lua command `run(input)` functions execute through the loaded plugin service with diagnostics for execution failures.
- 2026-07-02: P6 RPC plugin command dispatch slice added. RPC now accepts `plugin_command`, reuses the current loaded `CodingAgentSession` plugin service or loads plugins for a newly opened session, executes command providers with JSON args, and returns command id plus text output in the response.
- 2026-07-02: P6 interactive plugin command dispatch slice added. Interactive mode now recognizes `/plugin-command <command-id> [json-args]`, executes loaded plugin command providers through the session-owned `CodingAgentSession`, and reports command output in the transcript.
- 2026-07-02: P6 Lua plugin hook slice added. Lua plugins can now register prompt hooks with `host:hook(...)`; `PluginLoadFlow` exposes them as `HookProvider`s, and Lua hook `run(ctx)` functions execute through the loaded plugin service with scoped prompt context and hook diagnostics.
- 2026-07-02: P6 interactive plugin command slash alias slice added. Interactive reload now syncs loaded plugin command definitions into slash suggestions, and loaded plugin commands can be run directly as slash aliases such as `/lua.say_hello {"name":"alias"}` with JSON args.
- 2026-07-02: P6 Lua plugin UI/keybind registration slice added. Lua plugins can now register UI actions with `host:ui_action(...)` and keybindings with `host:keybind(...)`; `PluginLoadFlow` exposes them through the loaded plugin service's existing UI/keybind provider collections.
- 2026-07-02: P6 interactive plugin keybinding dispatch slice added. Interactive plugin reload/command completion now syncs loaded plugin UI actions and keybindings into the root, and idle-mode plugin keybindings dispatch to transcript-visible plugin UI actions.
- 2026-07-02: P6 controlled plugin UI action runner slice added. Plugin keybindings whose UI action id maps to a loaded plugin command now reuse the existing interactive plugin command runner with empty JSON args, while non-command UI actions remain transcript-visible pending actions.
- 2026-07-02: P6 plugin dialog runner slice added. Lua plugins can register dialogs through `host:dialog(...)`; session reload syncs dialog definitions into the interactive root; plugin UI actions targeting a dialog id now open a controlled transcript-visible dialog runner from keybindings.
- 2026-07-02: P6 plugin dialog submit scaffold added. Opening a plugin dialog now pre-fills the existing `/plugin-command <dialog.action_id>` path in the editor, so direct submit runs the dialog action with empty JSON args and users can edit JSON args before submitting.
- 2026-07-02: P6 plugin dialog field scaffold added. Lua `host:dialog(...)` can declare field metadata and defaults; interactive dialog opening displays field labels/descriptions and pre-fills the existing plugin-command JSON args with default values.
- 2026-07-02: P6 plugin dialog validation scaffold added. Dialog field metadata now carries `required`; interactive plugin-command submission validates required values and basic JSON field types before executing the dialog action.
- 2026-07-02: P6 plugin dialog native form slice added. Open dialogs now keep an active form state, route typed text into the focused field, support field focus navigation, and submit structured JSON through the existing plugin-command path.
- 2026-07-02: P6 plugin dialog validation UX slice added. Dialog validation now returns field identity to the interactive root, so failed required/type checks keep the form open and move focus back to the failing field; boolean fields have direct native toggle coverage.
- 2026-07-02: P6 plugin dialog inline validation slice added. Active dialog forms now retain the latest field validation error and render it inline under the failing field while still recording the transcript-level diagnostic.
- 2026-07-02: P6 plugin dialog numeric field UX slice added. Integer/number dialog form fields now filter invalid typed characters before submission while retaining plugin-command validation as the final boundary.
- 2026-07-02: P6 plugin dialog select field UX slice added. Lua dialog fields can now declare `options`/`choices` for `select`/`choice`/`enum` fields; the interactive form cycles options with Space and rejects values outside the declared option set before plugin command execution.
- 2026-07-02: P6 Lua host API metadata slice added. Lua plugin hosts now expose `host:api_version()` and `host:plugin()` so plugins can read the stable host API version and their manifest id/name/version/source without receiving raw session, auth, filesystem, shell, or Flow internals.
- 2026-07-02: P6 plugin reload capability slice added. `CodingAgentCapabilities` and RPC `get_state` now expose `plugin_reload`/`pluginReload` as a persistent-session workflow capability, including disabled and busy states alongside compact/branch-summary/export.
- 2026-07-02: Runtime control stabilization plan added. Phase 6 is now explicitly gated on CodingAgentSession-owned running prompt abort/steer/follow-up controls, RPC/interactive control convergence, capability cleanup, owner slimming, ExportFlow wrapping, and runtime-vs-session compaction boundary cleanup before subagent/supervisor or self-healing edit work starts.
- 2026-07-02: Runtime control Stage 1 implementation slice added. `CodingAgentSession` active-operation tracking now uses a drop-based operation guard, operation-control tests cover error-return cleanup and busy reporting, and prompt abort/steer/follow-up handle/channel scaffolding is ready for PromptTurnFlow plumbing.
- 2026-07-02: Runtime control Stage 2 PromptTurnFlow slice added. `PromptTurnContext` now carries an owner-issued control receiver, `RunAgentTurn` prioritizes queued control commands while streaming, follow-up/steer commands enter the running Agent, and abort commands cancel the Agent and finalize persistent prompts as `operation.aborted`.
- 2026-07-02: Runtime control Stage 3 RPC adapter slice added. RPC running prompts now hold a `PromptControlHandle`, `get_state` reports running abort/steer/followUp controls as available, `abort` cancels the running Agent, `steer`/`follow_up` send control messages, and `prompt.streamingBehavior` steer/followUp maps to the same control path.
- 2026-07-02: Runtime control Stage 3 interactive abort slice added. Interactive Ctrl-C on a running prompt now sends an owner-issued prompt abort control, cancels the provider token, lets `PromptTurnFlow` finalize `PromptAborted`, and keeps idle Ctrl-C exit behavior intact.
- 2026-07-02: Runtime control Stage 3 interactive steer slice added. Interactive input now accepts submitted text while a prompt is running, routes Enter to an owner-issued steer control through `PromptTask`, and verifies the follow-on provider call receives the steer text.
- 2026-07-02: Runtime control Stage 4 capability cleanup slice added. `CodingAgentCapabilities` now builds from semantic runtime state, prompt-control capabilities are available only for running prompt operations, idle controls report `no prompt is running`, and RPC capability serialization no longer carries stale AgentTurnFlow blocker reasons.
- 2026-07-02: Runtime control Stage 5 owner-slimming slice added. `OperationControl` now wraps active-operation state and prompt-control receiver ownership, while `CodingAgentSession` only asks it to issue handles, begin operations, expose the active kind, and transfer prompt receivers into `PromptTurnContext`.
- 2026-07-02: Runtime control Stage 6 ExportFlow wrapper slice added. Rust-native export now runs through `ExportFlow`/`FlowService`, splits replay selection/render/write nodes, routes `CodingAgentSession` current/static export APIs through the flow, preserves JSONL rejection, and guards current-session export with `OperationKind::Export`.
- 2026-07-02: Runtime-vs-session compaction boundary cleanup added. Manual session compaction now emits `SessionCompactionCompleted`, low-level runtime auto-compaction keeps `RuntimeCompactionCompleted`, protocol mapping uses `Manual` vs `Threshold` compaction reasons, and interactive compaction notices remain unchanged.
- 2026-07-02: Runtime control Stage 3 interactive follow-up slice added. Running interactive prompts now route Shift+Enter to the owner-issued follow-up control while preserving Enter-to-steer behavior, with root/input and scripted provider-driver coverage.
- 2026-07-02: AgentProfile/TeamProfile slash invocation future plan added. Explicit calls are planned as `/agent` and `/team` slash commands rather than `@agent`/`@team`; the default `AgentProfile` is session runtime configuration rather than a hidden live session agent; single-agent session startup should set the selected profile as the session default directly; `TeamProfile` requires supervisor semantics; model-requested delegation must go through `CodingAgentSession`-owned authorization and bounded delegation tools.
- 2026-07-02: AgentProfile/TeamProfile Stage 1 registry slice added. `pi-coding-agent` now exposes typed profile models and a `ProfileRegistry` through the public API, loads built-in/user/project TOML agent and team profiles with schema validation, keeps invalid files as diagnostics, and applies deterministic built-in < user < project duplicate precedence.
- 2026-07-02: AgentProfile/TeamProfile Stage 2 default session profile slice added. `CodingAgentSessionOptions` can select the initial default profile, Rust-native `session.json` persists `default_agent_profile_id`, opening a session restores it, `CodingAgentSessionView` exposes it, and `CodingAgentSession::set_default_agent_profile_id()` updates the owner/manifest while emitting a product event without creating a hidden live agent.
- 2026-07-02: AgentProfile/TeamProfile Stage 3 runtime application slice added. `CodingAgentSession` now owns a profile registry derived from session cwd and `PI_RUST_DIR`, ordinary prompts apply the current default `AgentProfile` to model/system prompt/tool/skill policy before `PromptTurnFlow`, plugin and built-in tools share the same allowlist filtering, and unavailable profile tools/skills surface as prompt diagnostics/events.
- 2026-07-02: AgentProfile/TeamProfile Stage 4a interactive slash entrypoint slice added. Built-in `/agents`, `/agent`, `/teams`, and `/team` suggestions are available; `/agents` and `/teams` list resolved profile registry entries; `/agent use <agent-id>` switches the session default profile for subsequent prompt/plugin/session tasks; and one-off `/agent <agent-id> <task>` plus `/team <team-id> <task>` remain explicitly pending AgentInvocationFlow/AgentTeamFlow implementation.
- 2026-07-03: AgentProfile/TeamProfile Stage 5 one-off agent invocation slice added. `AgentInvocationFlow` now runs `/agent <agent-id> <task>` through `CodingAgentSession::invoke_agent()` with resolved profile runtime policy, correlated parent/child operation ids, non-persistent child prompt execution that cannot commit parent transcript state, product events for start/diagnostics/completion/failure, and interactive slash routing through `PromptTask`.
- 2026-07-03: AgentProfile/TeamProfile Stage 6 supervised team invocation slice added. `AgentTeamFlow` now runs `/team <team-id> <task>` through `CodingAgentSession::invoke_team()` with resolved team/member profiles, deterministic or profile-backed supervisor semantics, isolated child prompt execution, parent/member operation lineage events, parent transcript isolation, and interactive slash routing through `PromptTask`; RPC/capability exposure and controlled model-requested delegation remain follow-up.
- 2026-07-03: AgentProfile/TeamProfile Stage 7a delegation request-tool slice added. Active profile delegation policy now exposes `delegate_agent`/`delegate_team` tools through session-owned runtime construction, returns structured request/rejection envelopes, enforces allowed agent/team ids plus zero-depth rejection at the request boundary, and keeps raw session/runtime/provider internals hidden; child flow execution, confirmation, recursive budget accounting, lifecycle events, and RPC/capability exposure remain follow-up.
- 2026-07-03: AgentProfile/TeamProfile Stage 7b delegation audit-event slice added. Delegation tool result envelopes now map to semantic `DelegationRequested`/`DelegationRejected` product events while preserving ordinary tool lifecycle events; approved/started/completed lifecycle events, child execution, confirmation, recursive budget accounting, and RPC/capability exposure remain follow-up.
- 2026-07-03: AgentProfile/TeamProfile Stage 8a capability surface slice added. `CodingAgentCapabilities` and RPC `get_state.capabilities` now expose `agentProfiles`, `teamProfiles`, and `delegation`; profile/team operations report available or busy, while delegation reports unsupported until bounded child execution exists.
- 2026-07-03: AgentProfile/TeamProfile Stage 8b RPC profile/team list command slice added. RPC mode now accepts `list_agent_profiles` and `list_team_profiles`, lazily uses the session-owned profile registry, returns current default-agent marking plus registry diagnostics and typed profile/team policy fields, and rejects profile listing while an operation is running; RPC default-profile switching and one-off agent/team invocation commands remain follow-up.
- 2026-07-03: AgentProfile/TeamProfile Stage 8c RPC default profile switch slice added. RPC mode now accepts `set_default_agent_profile`, validates target agent profiles through `CodingAgentSession`, updates the session-owned default profile, lets subsequent `list_agent_profiles` show the new default, rejects unknown profiles and running-operation changes, and keeps read-only profile listing from creating sessions before a mutation command.
- 2026-07-03: AgentProfile/TeamProfile Stage 8d RPC agent invocation slice added. RPC mode now accepts `invoke_agent`, validates the target profile and non-empty task, runs one-off `AgentInvocationFlow` on a background coding operation, streams child prompt events through the existing RPC protocol adapter, reports running capabilities with `agent_invocation`, and keeps RPC team invocation as the next follow-up.
- 2026-07-03: AgentProfile/TeamProfile Stage 8e RPC team invocation slice added. RPC mode now accepts `invoke_team`, validates the target team and non-empty task, runs supervised `AgentTeamFlow` on the shared background coding operation path, streams member/supervisor child prompt events through the existing RPC protocol adapter, reports running capabilities with `agent_team`, and leaves richer semantic lifecycle protocol events for a follow-up slice.
- 2026-07-03: AgentProfile/TeamProfile Stage 8f semantic protocol-event slice added. `CodingProtocolEventAdapter` now maps agent invocation and agent team lifecycle product events into RPC/JSON protocol events such as `agent_invocation_start`, `agent_invocation_end`, `agent_team_start`, `agent_team_member_start`, `agent_team_member_end`, and `agent_team_end`, while leaving delegation/profile-change semantic protocol events for follow-up work.
- 2026-07-03: AgentProfile/TeamProfile Stage 8g profile-change/delegation protocol-event slice added. `CodingProtocolEventAdapter` now maps `DefaultAgentProfileChanged`, `DelegationRequested`, and `DelegationRejected` into semantic protocol events, and RPC `set_default_agent_profile` streams `default_agent_profile_changed` after the command response; approved/started/completed delegation child execution remains follow-up.
- 2026-07-03: AgentProfile/TeamProfile Stage 9a user documentation slice added. `docs/agent-profiles.md` now documents profile/team discovery roots, TOML schema, precedence, interactive slash commands, RPC commands, semantic protocol events, and the current delegation request/rejection safety boundary.
- 2026-07-03: AgentProfile/TeamProfile Stage 7c-a delegation request queue slice added. `PromptTurnContext` now stores accepted `DelegationRequested` events as typed `DelegationRequest` values, giving future owner-authorized child execution a structured queue instead of requiring JSON/tool-result reparsing; rejected delegation stays audit-only.
- 2026-07-03: AgentProfile/TeamProfile Stage 7c-b delegation authorization decision slice added. `delegation` now computes typed `Approved`/`RequiresConfirmation`/`Rejected` decisions for queued requests from active profile policy, and `PromptTurnContext` records those decisions on prompt success without executing child flows or emitting approved/started/completed lifecycle events.
- 2026-07-03: AgentInvocationFlow task-normalization slice added. One-off agent invocation now derives the child prompt invocation from `AgentInvocationOptions::task` inside the flow, preventing stale parent prompt text from leaking through reused `PromptTurnOptions` before delegation reuses the same child flow boundary.
- 2026-07-03: AgentProfile/TeamProfile Stage 7c-c auto-approved delegation execution slice added. Prompt success now turns approved delegation decisions into session-owned child `AgentInvocationFlow`/`AgentTeamFlow` executions without using public busy-gated invocation entrypoints, emits `DelegationApproved`/`DelegationStarted`/`DelegationCompleted` product and protocol events, and verifies child agent/team executions receive the delegated task through non-persistent child prompt state.
- 2026-07-03: Delegation capability convergence slice added. `CodingAgentCapabilities` and RPC `get_state.capabilities` now report delegation as available when the owner can run bounded policy-gated delegation and busy during active owner operations, while concrete delegation tool exposure remains gated by the active `AgentProfile` policy.
- 2026-07-03: Delegation confirmation-required observability slice added. Confirmation-held delegation requests now emit `DelegationConfirmationRequired` product and protocol events with the policy reason and do not run child work until future confirmation UX exists.
- 2026-07-03: Delegation child-failure lifecycle slice added. Approved child flow failures now emit `DelegationFailed` product/protocol events correlated to the original delegation tool call and child operation id, with regression coverage for failed child profile resolution.
- 2026-07-03: Delegation child-depth boundary slice added. Agent/team child flow options now carry delegation depth, owner-approved delegation children run at depth 1, and nested child prompt delegation requests are authorized into explicit rejection/confirmation-required events instead of being silently dropped.
- 2026-07-03: Delegation pending confirmation approval slice added. Confirmation-held requests are retained in a session-owned pending queue, public APIs expose list/approve/reject entrypoints, RPC accepts `list_delegation_confirmations`, `approve_delegation`, and `reject_delegation`, approval runs the held child delegation through the owner flow, and rejection emits `DelegationRejected` without child execution.
- 2026-07-03: Interactive delegation confirmation UX slice added. Interactive mode now surfaces delegation confirmation and lifecycle notices, `/delegations` lists held requests, `/delegation approve` executes held child work through the session owner, `/delegation reject` emits rejection events without child execution, and scripted coverage verifies approving a confirmation-held delegation from the TUI path.
- 2026-07-03: Durable delegation confirmation slice added. Persistent sessions now record confirmation request/approval/rejection as typed session events, replay derives unresolved pending confirmations on open, approval/rejection write durable resolution events before mutating the owner queue, and reopen tests verify list/approve/reject behavior survives session reload.
- 2026-07-04: AgentProfile/TeamProfile first-class surface cleanup advanced. Interactive tests now lock `@agent`/`@team` as ordinary prompt text and verify built-in slash commands win over same-named plugin aliases; `AgentInvocationFlow` child prompts can receive owner-issued prompt control so interactive Ctrl-C cancels one-off `/agent` work; and `AgentTeamFlow` tests now cover unknown-supervisor plus child runtime failure events.
- 2026-07-04: Agent/team menu invocation design added. The accepted TUI direction replaces space-argument interactive `/agent use <id>`, `/agent <id> <task>`, and `/team <id> <task>` with `/agent` and `/team` menus backed by discovered profiles, including `Info`, `Use`, and `Run` agent actions plus `Info` and `Run` team actions; `/agent:<id> <task>` and `/team:<id> <task>` remain supported as direct `Run` shortcuts while RPC/Rust API invocation forms stay unchanged.
- 2026-07-04: Agent/team menu invocation TUI slice completed. Bare `/agent` now opens a menu with `Info`, `Use`, and `Run`; bare `/team` opens a menu with `Info` and `Run`; menu `Run` enters pending task mode before queuing one-off agent/team work; `/agent:<id> <task>` and `/team:<id> <task>` remain direct `Run` shortcuts and render in the transcript; old `/agent use <id>`, `/agent <id> <task>`, and `/team <id> <task>` forms now report menu/colon usage instead of mutating state.
- 2026-07-04: Delegation confirmation menu TUI slice completed. `/delegations` and `/delegation list` now open a pending-confirmation menu with selected-request details, `Enter`/`a` approval, and `r` rejection while reusing the existing session-owned approval/rejection commands; scripted interactive coverage now approves a held delegation through the menu instead of hand-typing the tool call id.
- 2026-07-04: Delegation stale confirmation policy slice completed. Pending confirmation requests older than 24 hours are hidden from public pending lists and cannot be approved, using the durable request event `created_at` when reopening persistent sessions; tests verify stale replay setup plus owner-level filtering/approval rejection while current pending reopen/approve/reject behavior remains intact.
- 2026-07-04: Recursive delegation budget slice completed. Authorization decisions now carry child delegation depth, owner-approved and recursively approved child agent/team flows inherit that depth instead of resetting to depth 1, and coverage verifies a parent-to-coder-to-reviewer chain executes while the next QA delegation is rejected when inherited `max_depth` is exhausted.
- 2026-07-04: Delegated child capability release slice completed. Delegated child agent/team prompt preparation now treats empty target-profile `tools`/`skills` as no release of parent runtime/plugin tools or skills, while explicit target-profile allowlists can release named capabilities; regression coverage verifies a parent-only runtime tool is visible to the delegating parent but not inherited by an unallowlisted child.
- 2026-07-04: Recursive delegation loop-detection slice completed. Delegation authorization now tracks typed agent/team lineage through owner-approved and recursively approved child flows, rejects requests that target an ancestor profile before starting child work, and preserves inherited depth-budget behavior for non-cyclic recursive delegation.
- 2026-07-04: Durable nested delegation confirmation queue slice completed. AgentInvocationFlow and AgentTeamFlow now surface confirmation-required nested requests to the session owner instead of rejecting them, persistent confirmation runtime seeds carry typed delegation lineage, replay keeps delegation confirmation request/resolution events even when their source child operation is non-persistent, and reopened sessions can approve nested pending requests into child work.
- 2026-07-04: Self-healing edit workflow design, implementation plan, and first internal Flow slice added. `SelfHealingEditFlow` now wraps existing edit behavior behind stable Flow nodes, extracts typed edit outcome data, and verifies `ExecutionEnv`-backed file operations before later check/repair, capability integration, durable events, and adapter exposure.
- 2026-07-04: Self-healing edit capability surface slice added. `CodingAgentCapabilities` and RPC `get_state.capabilities.selfHealingEdit` now report persistent-session workflow availability, disabled non-persistent state, and busy operation status, with `OperationKind::SelfHealingEdit` reserved for the upcoming session-owned edit workflow entrypoint.
- 2026-07-04: Self-healing edit session entrypoint slice added. `CodingAgentSession::self_healing_edit()` now exposes the workflow through the Rust API for persistent sessions, gates it with `OperationKind::SelfHealingEdit`, runs the existing `SelfHealingEditFlow`, records `self_healing_edit.started`/`self_healing_edit.completed` typed session events plus operation final markers, and fails non-persistent sessions without mutating files.
- 2026-07-04: Self-healing edit check/repair slice added. `SelfHealingEditFlow` now supports optional check-command execution through `ExecutionEnv`, records final check output on successful outcomes, fails checked edits when no repair strategy is configured, supports bounded repair retries through injected repair strategies, and persists optional check output in `self_healing_edit.completed` events.
- 2026-07-04: Self-healing edit provider-tool migration slice added. Provider-visible builtin `edit` tool calls now run through `SelfHealingEditFlow`, return original diff/patch details plus `selfHealingEdit` workflow metadata, preserve legacy edit error text, and keep direct `edit_execute()`/`edit_execute_with_operations()` as low-level compatibility and test entrypoints.
- 2026-07-04: Self-healing edit adapter exposure slice added. RPC now accepts `self_healing_edit` with structured edit replacements and returns the typed workflow outcome, while interactive mode adds `/self-healing-edit <path> <oldText> => <newText>` as a queued session-owned task that applies the same persistent workflow and updates the active session owner.
- 2026-07-04: P6 Lua host capabilities metadata slice added. Lua plugin hosts now expose `host:capabilities()` as feature-scoped boolean metadata for supported host methods, while tests verify neither the host table nor the returned metadata exposes raw session, runtime, provider, filesystem, shell, or operation-context internals.
- 2026-07-04: Product-event adapter Flow-node independence audit completed. Protocol and interactive adapter searches found no `FlowOutcome.last_node` or concrete Flow node consumption, and `protocol_events` now guards representative serialized product events against Flow-node-shaped protocol fields.
- 2026-07-04: `pi-agent-core` product-boundary audit completed. Source search and Cargo metadata confirm the low-level core crate does not depend on `pi-coding-agent` or import coding-session/product event/protocol/interactive ownership types; focused `pi-agent-core` check remains the boundary verification gate.
