- 配置加载采用分层合并模式，项目级设置优先于全局默认值，缺失字段自动回退到硬编码默认值。
- 敏感凭证（如 API Key）支持从 CLI 参数、环境变量或加密存储文件中按优先级解析，并支持 `$VAR` 语法动态替换。
- 会话存储采用追加型 JSONL 格式，每条记录包含唯一 ID 与父节点引用以构建消息树，并在读取时自动执行 schema 迁移。
- 工具执行支持全局与细粒度控制，可根据工具元数据动态选择并行（`FuturesUnordered`）或顺序执行策略。